# START-HERE — FINeprint

> 새 세션은 이 문서부터 읽는다. 마지막 갱신 2026-08-31 (세션 종료 시점).

## 한 문장

**내 상황과 내가 중요하게 여기는 것에 맞는 예금·적금을 골라주는 시스템을 만든다.**

시스템은 선호를 추측하지 않는다. 사용자가 알려주고, 시스템은 계산만 책임진다.

정식 정의는 [`docs/spec/problem.md`](./docs/spec/problem.md), 부품별 설계는
[`docs/spec/design.md`](./docs/spec/design.md)에 있다.

> **2026-08-26에 타깃을 바꿨다.** 원래는 "AI 답변이 공시로 뒷받침되는지 검사하는
> 검증기"였다. 그건 선행 프로젝트 `finance_verifier`가 이미 한 일이라, 사용자가 실제로
> 겪는 문제(내게 맞는 상품 고르기)로 옮겼다. 사유와 무엇이 살아남는지는
> [`docs/decisions/0009-pivot-to-recommender.md`](./docs/decisions/0009-pivot-to-recommender.md).
> 옛 정의는 [`docs/spec/problem-v1-verifier.md`](./docs/spec/problem-v1-verifier.md)에 보존했다.

## 현재 상태

```
추출·계산기·되묻기·질문 루프·화면 계약 검사·후보 집합(스코프)·
**선호 가중치와 정렬**까지 완료.

2026-08-28 에 사람이 질문 루프를 두 번 직접 답했고, 그 결과로 결정 다섯이 나왔다
(`0025`~`0028`). 화면이 거짓말한 자리 셋을 찾아 고쳤고(A2 확정 라벨 · A3 사유 문장 ·
A6 진행 카운터), 임계를 숫자 대신 **문구로 묻게** 바꿨고(`0027`), 질문을 **후보
집합에서만** 만들게 했다(`0028` — "우리은행 주거래인데 목돈" 시나리오가 39개 → 7개).

**2026-08-31 에 두 가지가 끝났다.**

M3 재측정 — 스코프 걸린 세션(질문 7개)을 사람이 완주했고 "모르겠다" 0개 · 44초였다.
자기보고에서 **반증 조건 둘이 발동**해 `0029`(성과를 "내 금리 범위" 로)와
이슈 #22(가입 채널 표시 · A9)가 나왔다.

그리고 그 자기보고가 **선호 가중치(`0030` · 이슈 #24)** 를 열었다 — 사용자가 스코프 밖
**+2.67%p** 를 보고도 안 넓혔고("주거래도 아니고 연고도 없다"), 그건 기관 접근성이
금리보다 앞선 축이라는 **관측**이었다. 가중치 단위를 **금리 %p 환산**으로 두고
(`영업점만 -0.50%p`) 세후 가중합으로 정렬한다. **기본값은 없다** — `--prefs` 를
안 주면 가중치 0개이고 정렬은 `net_hi` 순 그대로다.

그 뒤에 두 개가 더 닫혔다. **둘 다 다른 작업 중에 걸려 나온 것이다.**

`0031`(이슈 #25) — A11 이 2,388건 불통과로 뜬 것을 쫓아가니 **화면이 상품을 틀리게
세고 있었다**(저축은행 92개 vs 참값 183개). **행 = 추출 품질의 단위 · 상품 = 화면의
단위**로 명시했다.

`0032`(이슈 #28) — 세율표를 확인하려고 조문을 읽다가 **1년 묵은 값**을 찾았다
(조합등예탁금 1.4% → 2026년 가입분 5%). 이제 세율은 **조문 좌표에 핀을 박고**
`fetch_law.py --check` 가 바뀌었는지만 본다. **세율을 자동으로 읽지는 않는다.**

**2026-08-31 늦게 두 개가 더 닫혔다.**

`0033`(E7) — **세후 정렬은 세전 정렬과 순서가 같다.** 재려던 것이 구조적으로 0이었다 —
공시 API 에 **상품별 과세 유형이 없어서** 비과세 여부가 사용자 상태로만 들어오고, 한
실행에서 전 상품이 같은 세율을 받는다. 은행권 79행·저축은행 297행 × 비과세 켜고 끄고
**넷 다 `세전순 == 세후순`**. `problem.md` §4 의 예시는 상품설명서(A2)가 있어야 성립한다.

`0034`(I3) — **최고금리 단독 표기 제한은 실재한다.** `0026` 이 *"실재한다고 보지만
조문을 확인하지 않았다"* 로 열어 둔 반증 조건을 닫았다. 감독규정 §17①3가가 예금성
상품 광고에 **"이자율ㆍ수익률 각각의 범위 및 산출기준"** 을 요구한다 — `0017` 강제
1(범위로 쓴다)의 근거다. 다만 **강제 2(남은 조건 수)는 조문 문구가 아니라 우리
구현**이고, 의무 주체가 판매업자라 **우리가 적용 대상인지는 열린 결정**이다(이슈 #31).
덤으로 **세전·세후 라벨 요구**를 찾았다(공정위 예규).

`0035` — 사람이 **A·B 병기**로 그 열린 결정의 화면 쪽을 닫았다. 화면이 자기가 무엇인지
밝히고(**A13**), 적용 대상 여부와 무관하게 판매업자 기준을 지킨다(**A12** — 세후 라벨).
**A12 를 만들다가 질문 루프 화면에 `세후` 가 한 번도 없던 것을 찾았다** — `0034` 가
*"이미 지키고 있다"* 고 적은 것은 `calculate.py` 목록만 본 말이었다. 남은 것은
**F3(가입 링크)이 중개인가**다(#31).

**전체 과업 지도는 [`docs/spec/roadmap.md`](./docs/spec/roadmap.md) 다** — 무엇이 남았고
무엇이 선행 조건인지가 거기 있다. 에픽 이슈 #16~#20 이 그 문서를 가리킨다.
**크리티컬 패스에서 D7 선호 · E6 정렬 · E5 채널 · E3 세율표 · F1 비교 리포트가
닫혔고, 남은 것은 F4(UI) 하나**다.

저축은행 게이트 결정은 이슈 #10 에 열려 있다. **#31(규제 적용 대상)은 닫혔다** —
`0037` 로 **링크만 주고 가입은 받지 않으며 수익화하지 않는다**를 정했고, 분기점이
링크가 아니라 **수익**이었다(판매업·자문업 본문이 둘 다 *"이익을 얻을 목적"* 을
요건으로 둔다). **수익 모델이 생기면 다시 연다.**
```

| 항목 | 상태 |
|---|---|
| 데이터 | 은행 97상품(조건 253행) · 저축은행 668상품(조건 1,090행) · 조건문 177종 |
| 조건 추출 | 동작 — 규칙 파서 v3 + 제한 스키마 LLM. **v2 프롬프트(원리 기반)** 채택 (`0021`) |
| 계산기 | 동작 — `src/analysis/calculate.py`. 사용자 상태로 실수령 금리 범위 + 층 라벨 |
| **되묻기** | **동작** (`0015`·`0016`) — 판정 못 한 조건을 전부 질문으로 낸다. 못 채우면 사유 6종을 문장으로 |
| **되묻기 흐름** | **정의됨** (`0024`) — 질문 수 제한 없음. 임계는 **문구별 예/아니오**라 은행권 **39개**·저축은행 **45개** (`0027`) · 상태바 숫자 둘 |
| **질문 루프** | **동작** (`0025`·`0027`) — `src/analysis/ask_loop.py`. 하나씩 묻고 답을 받는다. **답은 예/아니오/모름 셋뿐** — 임계도 문구로 묻는다 |
| **화면 계약 검사** | **통과** — `check_screen_contract.py`. 중간 상태 전수에서 **A1~A13** 통과. 위반 셋(A2·A3·A6)을 찾아 고쳤다 (`prereg-09` §8). **A12(세후 라벨)·A13(성격 고지)은 2026-08-31 에 추가**했고, A12 를 만들면서 **질문 루프 화면에 `세후` 가 아예 없던 것**을 찾았다 (`0035`) |
| **후보 집합(스코프)** | **동작** (`0028` · `prereg-11`) — 기관·상품군으로 후보를 좁히면 질문이 **39→7개**. 지표는 카탈로그 고정. 밖의 최고 금리를 항상 표시(A7) |
| 정렬 | **최대 금리 순**이 기본값 (`0017`). `--sort lo` 로 예전 동작 |
| **비교 리포트** | **동작** (`0036` · 이슈 #33) — `--report N` 으로 상위 N개의 *"이 숫자가 어디서 왔나"*. 조건별 %p · **공시 문구 원문** · 배타 그룹 · 상한 · 근거 없는 %p · 정렬 근거. **약속한 칸 14개 중 4개가 없었고** 원천에는 있는데 계산기가 버리고 있었다(`why` 를 더했다) |
| **선호 가중치** | **동작** (`0030` · `prereg-12`) — 설문 5문항 → 고정 표 → **금리 %p 환산** → `--prefs`. 화면에 옮긴 값이 보이고 고칠 수 있다(A10) · **가중치는 정렬만 바꾼다**(A11). `--survey` 로 문항을 본다 |
| **가중치 숫자의 근거** | 다섯 중 **둘뿐이다** — `처음기관=많이 -3.00%p` 는 8/31 실측 하한 2.67%p(**표본 1**) · `확실성=많이 -1.00` 은 `--sort lo` 와 항등식. 나머지 셋은 **"근거 없음 — 판단"** 이라고 표에 적었다 |
| **도피 방지 제약** | **정해짐** (`0018`) — 커버리지 게이트 + 질문 예산 12개. 우선순위·동점도 고정 |
| **표본 검토** | **완료** (`0019`) — 부정 조건 실질 0건 · `기타` 40건 중 13건은 조건이 아니었다 |
| **유형 배정** | **재추출 채택** (`0021`) — 홀드아웃 86% · `기타` 3.8% → **0.7%** · 계단식 부분 해결 |
| 확정 비율 | 12개월 · 질문 예산 12개 시점 — 은행권 **37/67**(55.2%) · 저축은행 **75/106**(70.8%). 저축은행 옛 값 80.2%는 **상품을 틀리게 세던 질문 순서**의 값이다 (`0031` — 후퇴가 아니라 다른 12개를 답한 상태다). 옛 58.2·83.0%는 **버그가 있던 코드의 값**이다 (`prereg-09` §7.1 — 폭이 남은 상품에 `확정`이 붙었다) |
| 폭 평균 | 예산 12개 — 은행권 **0.185**%p · 저축은행 **0.449**%p (세후). 전부 답하면 0.105 · 0.180. 예산 12개는 **유형 질문만 재고 있다**(`prereg-10` §7) |
| **계단식** | **3문구뿐**(`0023`) — 11문구 중 8문구는 짝 정규식이 못 읽은 단일 임계였다. 이제 질문으로 나간다 |
| 커버리지 기준선 | 은행권 **79.7%** · 저축은행 **36.7%** — **유지**(`0018`·`0019`·`0022`). 실측 84.8% · **35.7%(저축은행 불통과 상태)** |
| A 규칙 파서 | 개발(은행권) **77.5%** · 처음 보는 기관 **63.2%** — 두 숫자를 함께 본다 |
| B 제한 스키마 LLM | 처음 보는 기관 **71.4%** (p=0.0987, 아직 유의하지 않다) |
| **A→B 폴백** | **채택** (`0012`) — 은행권 **83.9%** · 저축은행 79.1%. 사전등록 예측 83.8% 통과 |
| 분모 | "우대조건 있는 행" + **"공시 최고금리 > 기본금리"** — 폭 0 행은 `공시미반영` 층으로 |
| 결정 기록 | `0002`·`0005`·`0006`·`0007` 유효 · `0009`(타깃 전환) · `0010`(월 갱신) · `0018`(제약식) · `0019`(조건아님) · `0020`(자기일관성) · `0021`(v2 채택) · `0022`(배타 그룹 밴드) · `0023`(규칙 그만 좁히고 되묻는다) · `0024`(되묻기 흐름) · `0025`(답 처리) · `0026`(질문 수 불가침) · `0027`(문구로 묻는다) · `0028`(후보 집합) · `0029`(성과=내 금리 범위) · `0030`(선호는 금리 %p 환산) · `0031`(행=추출단위·상품=화면단위) · `0032`(세율은 조문에 핀) · `0033`(세후 정렬은 순서를 안 바꾼다) · `0034`(광고 규제는 실재하나 판매업자를 묶는다) · `0035`(화면이 자기가 무엇인지 말하고 판매업자 기준을 지킨다) · `0036`(리포트는 숫자의 출처를 적는다) · `0037`(링크만·수익화 없음) · `0004`·`0008` 일부 폐기 |
| 공시 갱신 | **월 1회** — 매월 20일 전후. 같은 달 안에서는 안 바뀐다(8/24→8/26 실측 0건) |
| **광고 규제 근거** | **확인됨** (`0034` · 2026-08-31) — 법제처 API 로 조문 6개 대조. `0017` 강제 1(범위)은 **감독규정 §17①3가와 문구가 대응**하고, 강제 2(남은 조건 수)는 **조문이 금지하는 결과를 막는 우리 구현**이다. 근거는 `config/ad-sources.json`, 감시는 세율과 같은 `--check`. **적용 대상 여부는 열려 있다**(#31) |
| **세율표** | **확인됨** (`0032` · 2026-08-31) — 법제처 API 로 조문 5개 대조. **틀린 값 하나·빠진 값 하나**를 찾았다(조합등예탁금 1.4% → 2026년 가입분 **5%** · 비과세종합저축 일몰 **2028-12-31**). 근거는 `config/tax-sources.json`, 감시는 `python src/ingest/fetch_law.py --check` |

## 읽는 순서

| 순 | 문서 | 무엇이 있나 |
|---|---|---|
| 1 | **`docs/spec/problem.md`** | **현재 유효한 문제 정의.** 무엇을 만들고 왜 필요한가 |
| 2 | **`docs/spec/design.md`** | 부품별 계약·상태·"없으면 어떻게 되나"·측정 방법 |
| 2-b | **`docs/spec/collection-pipeline.md`** | **"공시 어떻게 자동화했어요?" 에 답하는 문서.** 데이터가 들어오는 경로 · 왜 월 1회인가 · 무엇을 자동화 안 했고 왜인가 |
| 3 | **`docs/spec/roadmap.md`** | **전체 과업 지도** — 무엇이 남았고 무엇이 선행 조건인가. 에픽 이슈(#16~#20)가 이 문서를 가리킨다 |
| 4 | `docs/decisions/0009` | 왜 검증기에서 추천기로 바꿨나. 무엇이 살아남나 |
| 5 | `docs/decisions/` | 정한 것과 폐기한 것. 반증 조건이 세 번 실제로 발동했다 |
| 6 | `docs/spec/prereg-03`·`prereg-05` | 추출기 비교와 규칙 깎기. 방법론의 실제 사례 |
| 7 | `docs/spec/prereg-09` | **중단 정직성 검사의 사전등록** — assert A1~A5 · 표본 · 예측 |
| 7-b | **`docs/spec/prereg-12`** | **"무엇을 주장하지 않는가"를 첫 항목으로 쓴 사전등록.** 정렬 품질에는 정답이 없다 |
| 7-c | `docs/spec/prereg-13` | **기준선을 움직이는 수정을 재등록한 예.** 편향 방향을 "모른다"고 적고 예측을 먼저 커밋했다 |
| 8 | `docs/spec/prereg-06`·`prereg-08` | 열린 결정. `기타` A군과 계단식은 닫혔다(`0021`·`0023`) — 남은 것은 검수 표본 · 민감도 임계 · **저축은행 게이트** |
| — | `docs/spec/problem-v1-verifier.md` | 옛 정의. 보존용, **계획으로 읽지 않는다** |

## 먼저 알아둘 말

| 말 | 뜻 |
|---|---|
| **우대조건** | "급여이체 하면 +0.5%" 처럼 금리를 더 주는 조건. 공시에 자유서술로 적혀 있다 |
| **조건 유형 17종** | 그 조건들을 묶은 목록(자동이체·카드실적·첫거래…). 사용자에게 물을 항목이자 상품을 묶는 축 |
| **닫힘률** | 뽑아낸 우대금리를 다 더한 값이 공시의 (최고금리−기본금리)와 맞는 비율. **맞으면 그 상품은 계산해줄 수 있다** |
| **계산 불가 상품** | 조건을 다 더해도 광고 금리에 안 닿는 상품. 버리지 않고 "근거 없음"으로 표시한다 |
| **홀드아웃** | 규칙을 만들 때 한 번도 안 본 데이터. 처음 보는 데이터에서의 성능을 재려고 따로 떼어 둔다 |
| **사전 등록** | 재기 **전에** 예측·판정 기준을 커밋하는 것. 결과를 보고 기준을 고르는 걸 막는다 |

## AI가 들어가는 자리 — 그리고 안 들어가는 자리

**"AI 프로젝트니까 AI를 쓴다"로 가지 않는다.** 자리마다 근거와 측정이 붙어 있다.

| | 하는 일 | AI가 꼭 필요한가 | 잰 것 |
|---|---|---|---|
| **R1** | 공시 조건문 → 구조 | 규칙과 경쟁 중 | 규칙 62.6% · LLM 71.4% · **폴백 80.2%** |
| **R2** | 사용자 자연어 → 상태 | 그렇다 (폼만 쓸 거면 불필요) | 아직 안 쟀다 |
| **R3** | 분해가 원문과 맞나 판정 | 그렇다 — 의미 판단 | 규칙·LLM이 **9%**에서 갈린다 |
| R4 | 리포트 서술 문단 | 아니다 | **편의 기능.** 잴 지표가 없다 |

나머지(계산·정렬·링크·비교표)는 전부 결정론적 코드다.

## 다음에 할 일

> **2026-08-31 에 이슈 넷을 닫았다** — #22(가입 채널) · #24(선호 가중치·정렬) ·
> #25(상품 신원) · #28(세율표). 0~3번·4-b~4-f·5번도 닫혔다. **작업 중인 브랜치 없고
> `main` 이 최신이다.**
>
> **바로 착수할 수 있는 것 둘.**
>
> ```
> I3   금융광고 규제 조문 확인    0026 이 "조문을 확인하지 않았다" 고 남긴 자리이고
>                             0017 이 강제한 "최대값 단독 사용 금지" 의 법적 근거다
>                             **target=admrul(행정규칙) 이라 fetch_law.py 가 그대로
>                             안 붙는다** — 거기부터 본다
>
> E7   세후 vs 세전 순위 변화    **측정할 것이 없다는 것을 확인했다** (8/31). 한 번의
>                             실행에서 모든 상품이 같은 세율을 받아 순서가 보존된다.
>                             실측으로 권역 2 x 상태 2 전부 `세전순 == 세후순`.
>                             **진짜 선행은 E3 가 아니라 A2(상품설명서)였다** — 상품별
>                             비과세 여부가 API 에 없다. 남은 일은 결정으로 적고 닫기
> ```
>
> **F1·F4-0 이 닫혔고 남은 것은 F4-1~F4-3 이다.** 계획은
> [`docs/spec/ui-plan.md`](./docs/spec/ui-plan.md).
>
> **F4-0 이 2026-09-01 에 끝났다**(`0039` · 이슈 #36) — 화면 계약이 **두 겹**이 됐다.
> `view.py` 의 뷰 모델에 사실이 담겼나(`[모델]`)와 렌더러가 그것을 출력했나를 따로 본다.
> **CLI 출력 회귀 0줄.** 그 과정의 음성 검사가 **A1 의 결함**을 찾았다 — 렌더된 줄이
> 아니라 헬퍼 출력을 보고 있어서 렌더러가 최대값만 출력해도 통과했다.
>
> **다음은 F4-1(서버)** 이다. 서버는 **FastAPI** 로 정했고(`0038`), **의존성 정책의 두
> 번째 예외라 `requirements.txt` 에 사유를 적는 것이 그 단계의 완료 조건**이다.
> **아직 정할 것 둘** — 답을 서버에 저장하나(`0026` 반증 조건이 걸린다) · 배포 형태
> (`0037` 이 "깃 단위에서 끝날 가능성이 높다" 를 전제로 선다). 사람 결정이 남은 것은 [#10](https://github.com/hyos0415/fineprint/issues/10)
> (저축은행 게이트 · 9월 공시 뒤가 유리) · [#3](https://github.com/hyos0415/fineprint/issues/3)
> **둘**이다.
>
> **시간이 정하는 것** — 9월 공시(9/20 전후)에 B6(월 갱신 사람 손 몇 건)이 열린다.
> **AI 정당화의 근거이자 H1(Airflow)의 선행**이고, `fetch_law.py --check` 를 그 DAG 의
> 첫 태스크로 넣기로 해 뒀다(`0032`).
>
> **새 부품을 만드는 작업은 이슈가 없으면 이슈부터 만들고 에픽에 붙인다.**
> `main` 직접 커밋은 문서 정비뿐이다 ("하지 말 것" 참고).
>
> ```
> python src/analysis/ask_loop.py 20260826 --company 우리 --kind 적금   # 질문 7개
> python src/analysis/ask_loop.py 20260826 --survey                    # 선호 설문 5문항
> ```

### 0. 도피를 막는 제약식 — ✅ 완료 (`decisions/0018`)

**커버리지 게이트 + 질문 예산 12개.** 개선을 주장하려면 두 관문을 통과해야 한다.

```
게이트   메인 층(확정+범위) 비율이 기준선 아래로 떨어지면 개선으로 인정하지 않는다
         은행권 12개월 81.0% (64/79) · 저축은행 12개월 36.7% (109/297)

예산     상위 12개 질문까지만 답한 상태에서 확정률·범위 폭을 잰다
         13번째부터는 물어도 되지만 평가 지표에 안 들어간다
```

**원래 후보 셋이 구멍 하나를 통째로 놓치고 있었다.** 질문을 하나씩 늘리며 실측하니
**메인 층 수는 질문에 아예 안 변했고**(은행권 64 고정) **범위 폭 평균은 질문 수의
단조 감소 함수**였다. 즉 후보 1은 질문 부풀리기에 무감각하고, 후보 2는 그것을
정확히 보상한다. 어느 쪽도 "답할 리 없는 질문 25개"를 못 막았다.

**커버리지는 점수에 더하지 않고 게이트로만 쓴다** — 가중 합산하면 커버리지를 조금
내주고 확정률을 크게 올리는 거래가 생긴다. **질문 우선순위와 동점 처리도 같이 고정했다**
(커버리지 내림차순 → 사전순). 사후에 상위 12개를 고르는 우회를 막기 위해서다.
동점은 실제로 예산 경계에 걸린다 — 은행권 11·12위가 둘 다 7상품이다.

앞으로 비교할 기준값은 이것이다.

```
은행권      확정 47/64 (73.4%) · 폭 평균 0.096%p · 커버리지 81.0%
저축은행    확정 91/109 (83.5%) · 폭 평균 0.239%p · 커버리지 36.7%
```

측정 `python src/analysis/ask_budget.py 20260826 --group bank --tolerance` ·
구현 `calculate.py` 의 `ASK_BUDGET`·`rank_questions()`.

### 1. 사람이 표본을 직접 읽었다 — ✅ 완료 (`decisions/0019`)

**부정 조건은 실질 0건이다.** 6건은 상품 하나(KB저축은행 정기예금 × 기간 6개)이고,
그 6건마저 **AI가 방향을 뒤집은 것**이었다 — `"미등록시 해제"`라는 이중 부정을 한 번만
풀어 `required`를 `must_not_have`로 읽었다. 걱정했던 방향의 **정반대**다. 금융위 경보
사례는 우리 상품군의 것이 아니다.

**`기타` 40건은 셋으로 갈렸다.**

```
A  17종 중 하나로 갔어야 한다        20건   재분류는 아직 미정 (아래 2번 옆)
B  애초에 조건이 아니다             13건   0019 에서 처리 완료
C  진짜 기타 — 공시가 안 말한다       7건   그대로 되묻는다 (0016). 손댈 것 없음
```

**규칙으로 걸러낸다. 문구를 박지 않았다** — 숫자와 껍데기 말을 걷어내고 아무것도 안
남으면 조건이 아니다. `기타` 13건을 정확히 집어내고 재추출은 필요 없다($0).

**그 규칙이 `기타` 밖에서 더 나쁜 걸 찾았다.**

```
부산은행 "부산이라 좋다 Big적금" 의 조건문은 이 한 줄이 전부다
   *우대이율 6개월 미만 최대2.00%, 6개월 이상 2.20%

무슨 조건인지 한 글자도 없는데 무조건_특판_이벤트(항상 충족)로 분류돼 있었다
   고치기 전   6개월 상품이 4.80% 로 보인다        사용자 입력 0개
   고친 뒤     2.60~4.80% · 사유 "조건불명"        은행에 전화해 확인하라고 말한다
```

**금액 임계 미탐 36건**도 함께 고쳤다 — `"1천만원 이상"`은 잡는데 `"1천만원이상"`은
못 잡았다(광주 27 · 안양 6 · 경남 3). 방향이 나쁘다: 임계인 줄 모르면 사용자 "예" 답을
그대로 충족으로 세어 **과대 진술**이 된다.

**셋 다 확정률을 낮추는 방향이라 기준선을 재등록했다.** 고치기 **전에** 사유·편향
방향·완화 조치를 `prereg-06` §2.3(1)에 적었고, 예고한 방향 그대로 나왔다.

| | 커버리지 | 예산 12개 확정 | 폭 평균 |
|---|---|---|---|
| 은행권 | 81.0% → **79.7%** | 73.4% → **58.7%** | 0.096 → **0.170**%p |
| 저축은행 | 36.7% → **36.7%** | 83.5% → **81.7%** | 0.239 → **0.241**%p |

**나빠진 게 맞다.** 옛 73.4%에는 *"조건이 뭔지 모르는데 `기타=예`면 확정"*과
*"1천만원 임계를 안 보고 확정"*이 섞여 있었다.

### 2. `기타` A군 재분류 — ✅ 완료 (`decisions/0021` · `prereg-07`)

**예시가 아니라 판정 원리를 프롬프트에 넣고 재추출했다**($2.40). 예시를 나열하면
그게 별칭 사전이고, `CLAUDE.md` 5번이 막은 방향이다.

```
고객군_자격      사용자가 **누구인가**로 갈린다
실천_미션_인증    사용자가 **무엇을 했는가**로 갈린다
쿠폰_코드_추천인   가입 경로에 제3자나 코드가 끼어 있으면
기타            16종 원리를 전부 대 봤는데도 안 맞을 때만
```

**근거는 은행권 11건만 봤다** — 저축은행 9건은 홀드아웃이라 읽지 않았다.

| | 예측 | 실측 |
|---|---|---|
| 주 지표 · 은행권 | ≥ 9/11 | **문구 종 5/5 · 항목 14/14 (100%)** |
| 홀드아웃 · 저축은행 | ≥ 3/9 | **문구 종 6/7 (86%)** |
| `기타` 비율 | ≤ 2.5% | 은행권 **0.7%** · 저축은행 **0.8%** |

**홀드아웃 86%가 결론이다.** 예측을 33%로 낮게 잡은 건 저축은행 A군이 은행권 사례와
안 겹치기 때문이었다. 겹치지 않는데 맞았다 — **원리가 옮겨붙었다.**

**게이트 하나가 걸렸는데 사람이 넘겼다** — 저축은행 커버리지 36.7 → 35.4%.
넘긴 이유는 `0021`에 적었다. **게이트를 없애지는 않았다** — 게이트가 신호를 냈고,
그 신호를 쫓아가서 아래 문제를 찾았다.

### 3. `exclusive_group` — ✅ 밴드로 처리 (`decisions/0022`)

층 판정은 배타 그룹을 무시하고 다 더했는데 `bonus_range()`·공식 닫힘률은 그룹 최댓값을
썼다. **같은 항목이 두 값을 냈다.** 어느 쪽이 맞는지 재보니 **어느 쪽도 아니었다.**

```
배타 그룹이 붙은 행에서 공시 폭에 맞는 쪽
  은행권 93행    최댓값 35 · 합산 32 · 둘 다 틀림 26
```

`iM함께예금`은 `g1`에 `[0.1 × 5]`가 들어 있는데 공시 폭이 0.45다 — 다섯 개가 다
더해져야 맞는다. **중복 적용 불가가 아니라 별개 조건 다섯을 한 그룹으로 묶은 것**이다.
공식이 틀린 게 아니라 라벨을 못 믿는다.

**그래서 고르지 않고 범위로 낸다.** `0015`·`0016`·`0017`·`0019`가 한 것과 같은 답이다.

```
lo = 그룹 최댓값 (보수)   hi = 합산 (낙관, 공시 최고금리 상한에 걸림)
사유 `중복우대불명` — "함께 적용되는지 하나만인지 공시가 분명하지 않습니다"
```

**예측보다 효과가 작았다** — 은행권 84.8%(예측 ≥85%) · 저축은행 35.7%(예측 ≥40%).
유리하게 예측했고 현실은 덜 유리했다. **밴드 전용 행은 1.5%·0%**라 판정이 느슨해지지는
않았다.

### 4. 저축은행 게이트를 어떻게 할지 정한다 — ⚠️ 사람 결정이 남았다

**커버리지 1.0%p 를 규칙으로 쫓는 것은 그만뒀다** (`0023` · 사람 결정). 원인을 셋까지
좁히고 후보 규칙까지 다 쟀지만 만들지 않았다.

```
원인 1  배타 그룹 누락    웰뱅 워킹 적금   걸음수 4단계를 쪼개고 묶지 않았다 (합 13.0 · 폭 7.0)
원인 2  cap 오분류      웰뱅 든든적금   신용평점 항목의 상한 3.0 을 상품 cap 으로 올렸다
원인 3  중복 계상       KB일반정기적금  "미등록시 적용해제" 를 별개 항목 +0.1 로 만들었다
                                  ← 예고에 없던 원인. 3행 중 2행이 이것이다
```

**셋을 다 고치면 37.7%로 기준선 위까지 간다. 그래도 안 만든다.** 규칙을 좁히는 일이
이 프로젝트에서 이미 세 번 같은 모양으로 돌아왔고(`prereg-06` §1.1 · `0019` ·
`prereg-08`), `0007`이 "규칙은 특정 은행의 표기 습관을 배운다"를 실측으로 남겼다.

**그래서 남은 것은 게이트 처리다.** `0018`의 저축은행 게이트 36.7%가 계속 불통과다
(실측 35.7%). `0022`에서 기준선을 유지한 이유가 *"바를 낮추지 않고 넘을 방법을 아는
상태로 둔다"* 였는데 **이제 넘지 않기로 정했으므로 그 이유가 사라졌다.**

```
후보 1  기준선을 실측값 35.7% 로 재등록한다 (`0022` 대안 D)
        못 넘은 바를 낮추는 모양이지만, 이제는 "안 넘기로 정한" 것이라 사정이 다르다
후보 2  게이트를 유지하고 불통과 상태로 둔다
        신호는 계속 나오지만 아무 개선도 게이트를 통과하지 못하는 상태가 이어진다
후보 3  게이트를 권역별로 다르게 본다
        은행권 84.8%(기준선 79.7% 통과) · 저축은행만 문제다
```

**미정.** `0023` §남은 결정 1번 · 이슈 [#10](https://github.com/hyos0415/fineprint/issues/10).

**9월 공시를 먼저 볼 수도 있다** — `0021`의 반증 조건에 *"저축은행 커버리지가 다음
측정에서도 안 돌아오면 v2가 나쁜 것이다"*라고 적어 뒀다. 그러면 후보 2를 9월까지
한시적으로 유지하는 것도 선택지다.

### 4-b. 되묻기 2단계 질문 루프 — ✅ 완료 (`decisions/0025` · 이슈 #11)

`0024`가 흐름을 정의했고 계산기·상태바·`아니오` 경로까지 다 들어갔는데 **질문을 하나씩
던지고 답을 받는 루프가 없었다.** 그 루프가 `src/analysis/ask_loop.py`다. LLM을 안
부르므로 **돈은 안 든다.**

```
python src/analysis/ask_loop.py 20260826 --group bank --term 12     사람이 답한다
python src/analysis/ask_loop.py 20260826 --auto 예                   사람 없이 회귀 확인
```

**세 페르소나가 `0024`의 실측표를 그대로 재현한다** — 시뮬레이션(`ask_budget.py`)과
사람용 루프가 같은 답을 낸다는 확인이다.

| `--auto` | 질문 수 | 확정 | 1위 |
|---|---|---|---|
| 예 | 22 | 64/67 | 5.92% |
| 아니오 | 15 | 65/67 | **4.06%** — 되묻기가 깎은 폭 1.86%p |
| 모름 | 15 | **4**/67 | 5.92% |

**만들자마자 시뮬레이션에서는 안 나오는 버그가 나왔다** (`0025`). 유형에 "예", 수치에
"모르겠다"를 답하면 **같은 질문이 무한히 돌아왔다** — `ask_budget.py`의 페르소나는
수치에 항상 숫자로 답하기 때문에 한 번도 밟은 적이 없는 경로였다. 수치 질문의 "아니오"를
0으로 넣지 않는 것(`"1천만원 이하"`를 오히려 충족시킨다)도 같이 정했다.
**기준선은 안 움직였다** — 은행권 39/67 · 0.176%p · 84.8%, 저축은행 88/106 · 0.277%p · 35.7%.

### 4-c. 중단해도 화면이 정직한가 — 지금 가능 (`decisions/0026` · 이슈 #13)

**처음에는 "사람이 22개를 완주하는가" 를 재려 했는데 그 전제를 기각했다** (사람 결정).
질문을 줄이는 것은 과대광고와 같은 구조다 — 안 물은 조건은 `모름` 이 되어 **`hi` 에
그대로 남으므로**, 질문을 잘라내면 최고 금리는 안 변하고 그 근거만 사라진다.
`0024` 의 반증 조건 1을 폐기하고 `0026` 으로 대체했다.

```
폐기   질문 수를 줄이는 방향 전부 · 완주율·중단률을 지표로 쓰는 것
유지   전부 묻는다. 안 물은 것은 범위 + `미응답` 사유로 낸다
편의   이어하기 · 질문 순서 · 문구 개선으로만 다룬다. 질문 수로는 안 다룬다
```

**중단은 시스템이 지원하는 상태다.** 위험은 사용자가 멈추는 것이 아니라 **멈춘 상태에서
우리 화면이 단정하는 것**이고, 그 실패는 이미 한 번 일어났다 — `0019` 의 부산은행
사례(사용자 입력 0개에서 4.80% 가 확정처럼 보였다). 그래서 재는 것을 바꿨다.

| | 무엇 | 누가 |
|---|---|---|
| M1 | 화면 계약 전수 검사 — 모든 중간 상태에서 assert A1~A5 | **코드** |
| M2 | 남은 질문 수 단조 비증가 — 답 조합을 섞어 전수로 | **코드** |
| M3 | "모르겠다" 비율 — 질문이 답할 수 있는 형태인가(`0024` P1) | 사람 1회 |
| M4 | 상태바 둘째 숫자가 보이는가 — 자기보고 2문항 | 사람, 약한 증거 |

`design.md` 화면 계약이 *"화면 코드에 assert 를 박는다"* 고 이미 적어 둔 것이 M1 이다.
assert 정의·표본(시드 0~199)·예측은 `docs/spec/prereg-09-ask-loop-walkthrough.md` 에
**재기 전에** 커밋했다. 돈은 안 든다.

**M1·M2 는 끝났고 위반 두 건을 찾았다** (`prereg-09` §8). 예측은 둘 다 0건이었다.

```
A2  조건을 전부 답해도 폭이 남는 상품 16종에 `확정` 라벨이 붙었다      ← 예상 밖
    배타 그룹 밴드(0022)를 층 라벨만 안 따랐다. 상태바 "금리가 정해진 상품" 이
    안 정해진 상품을 세고 있었다 (전부 예로 답했을 때 은행권 64개 중 14개)

A3  `미응답`·`수치필요` 사유 문장이 화면에 없었다                   ← 예고했던 위반
    사람이 "문장도 보여준다" 를 골랐다. 두 블록으로 나눠 전부 낸다
```

**고친 뒤 기준선이 예고한 값과 정확히 일치했다** — 확정 은행권 58.2 → **55.2%** ·
저축은행 83.0 → **80.2%**, 폭 평균·커버리지는 안 움직였다(`prereg-09` §7.1에 고치기
전에 적은 예고 그대로). **남은 것은 M3·M4** — 사람이 은행권 1회를 답해 "모르겠다"
비율과 자기보고 2문항을 채우는 일이다.

```
python src/analysis/check_screen_contract.py 20260826 --group bank   회귀 가드 (35초)
python src/analysis/ask_loop.py 20260826 --group bank --term 12      M3·M4 (사람)
```

### 4-d. 임계를 문구로 묻는다 — ✅ 완료 (`decisions/0027` · `prereg-10`)

**사람이 실제로 답해 보고 나온 결정이다.** 수치 질문 두 개에서 답이 왜곡됐다 —
`"예"` 를 세 번 거절당한 뒤 사실이 아닌 `아니오` 로, 다른 하나는 `모름` 으로 답했다.
입력 처리 버그(거절 메시지가 거짓 · `6회` 를 못 읽음 · 힌트 오독)는 따로 고쳤지만,
**고쳐도 남는 문제**가 있었다.

```
타상품_보유동시가입 — 금액이 얼마입니까?
  "②적립식예금 잔액 10만원 이상 보유"        ← 적립식예금 잔액
  "수익증권(펀드)을 3천만원 이상 보유 시"      ← 펀드 보유액
  "정기예금 500만원이상 가입하고 유지"         ← 정기예금 가입액

세 축이 다르다. 답이 존재하지 않는다 — 입력 폼으로 바꿔도 필드 하나로는 못 받는다
```

**그래서 판정 단위를 (유형)에서 (유형 + 문구)로 내렸다.** 공시 문구가 질문 옆 참고
자료가 아니라 **질문 문장 자체**가 된다(`0024` P1이 강해지는 방향이고, 숫자 계산이
사라진다). 사전등록 예측이 **전부 맞았다**.

| | 예측 | 실측 |
|---|---|---|
| 질문 수 (전부 답) | 늘어난다 | 은행권 22 → **39** · 저축은행 27 → **45** |
| 확정 (예산 12) | 내려간다 | **변화 0** — 55.2% · 80.2% |
| 폭 평균 (예산 12) | 올라간다 | 0.176 → **0.185** · 0.277 → **0.386** |
| 전부 답했을 때 확정 | 같다 | 은행권 **50/67** · 저축은행 **93/106** |
| 커버리지 · 계단식 | 안 변한다 | **84.8% · 35.7%** · 3 · 3종 |
| 화면 계약 A1~A5 | 통과 | **전부 통과** |

**예산 12개가 무엇을 재고 있었는지 드러났다** — 문구 질문은 상품 커버리지가 작아
우선순위에서 12위 밖으로 밀린다. 즉 **예산 12개는 유형 질문만 잰다.** 폭 평균 하락은
`0026`("예산을 늘려 지표에서 지우지 않는다") 때문에 그대로 남긴다.

### 4-e. 후보 집합(스코프) — ✅ 구현 완료 · M3 재측정만 남았다 (`0028` · 이슈 #15)

**사람이 두 번째 세션에서 더 큰 문제를 지목했다** — 질문이 카탈로그 17개 기관 전체에서
생성돼, 우리·농협을 보려는 사용자가 제주·광주·경남 상품 때문에 답하고 있었다(답한 문구
질문 8개 중 6개). 0단계 축에 **기관·상품군**이 빠져 있던 것이다.

```
"우리은행을 주거래로 쓰는데 목돈을 모으고 싶어"      ← problem.md §3 기준 시나리오

전체 카탈로그   상품 79개 · 질문 39개
+ 우리은행·적금  상품  4개 · 질문  7개    그리고 7개가 전부 우리은행 문구다
```

**두 층을 분리한 것이 핵심이다** — 화면은 후보 집합, **지표는 카탈로그 고정**.
지표를 후보 기준으로 재면 "쉬운 기관만 골라 확정률 95%" 가 가능해진다(`0018` 이 막은 도피).
사전등록의 주 판정이 그것이었고 **지표가 한 자리도 안 움직였다.**

| | 예측 | 실측 |
|---|---|---|
| 지표(카탈로그) | 한 자리도 안 변한다 | **적중** — 37/67 · 85/106 · 0.185 · 0.386 · 84.8% · 35.7% |
| 스코프별 질문 수 | 전부 20개 이하 | **미달** — 웰컴저축은행 23개 (30곳 중 2건 · 나머지 중앙값 3) |
| 화면 계약 A1~A7 | 전부 통과 | **적중** (스코프 유무 × 두 권역) |
| 스코프 밖 격차 | 1.0%p 이상 | **적중** — 은행권 중앙값 **2.41%p** · 저축은행 8.62%p |

**좁히는 대가를 A7 로 강제한다** — 격차 중앙값이 2.41%p(1천만원 1년이면 24만원)라
안 보여주면 정보 은폐다. 화면에 *"스코프 밖에 5.92% 가 있습니다 (+2.67%p) · 넓히면
질문이 39개로 늘어납니다"* 가 항상 붙는다.

**남은 것 — M3 재측정 (사람 1회).** 질문 7개짜리 세션은 39개짜리와 다른 조건이므로
"모르겠다" 비율을 다시 잰다. 그때 `0028` 반증 조건 1(A7 경고를 보고도 안 넓히는가)도
같이 관측된다.

```
python src/analysis/ask_loop.py 20260826 --company 우리 --kind 적금        질문  7개
python src/analysis/ask_loop.py 20260826 --company 제주                   질문 17개 (은행권 최대)
python src/analysis/ask_loop.py 20260825 --group savingsbank --company 웰컴  질문 23개 (M5 미달 지점)
```

**M3 재측정 완료 (2026-08-31) — 사람이 7개를 완주했다.**

| | 예측 | 실측 |
|---|---|---|
| "모르겠다" 비율 | ≤ 30% (임계 50%) | **0%** (0/7) |
| 질문당 시간 중앙값 | ≤ 20초 | **2.7초** |
| 합계 시간 | ≤ 8분 | **44초** |

전체 카탈로그 세션(8/28)은 39개 중 23개에서 중단 · 모름 17.6% 였다 — **스코프의 효과가
두 세션 대조로 나온다.** 그리고 자기보고에서 **반증 조건 둘이 발동했다.**

```
0028 반증조건 1   스코프 밖 +2.67%p 를 보고도 안 넓혔다 — "주거래도 아니고 연고도 없다"
                → 경고가 소음이 아니라 **선호가 실재한다는 증거**. A7 은 유지한다
                → 발견: A7 에 가입 채널(join_way)이 빠져 있다 (로드맵 E5)

0024 반증조건 4   "진행 바밖에 안 보인다" — 확정 수가 6개 질문 동안 0 고정이었다
                → 성과를 **내 금리 범위**로 바꿨다 (`0029`) + 화면 계약 **A8** 추가
```

### 4-f. 선호 가중치와 정렬 — ✅ 완료 (`decisions/0030` · `prereg-12` · 이슈 #24)

**지금까지 만든 것은 "추천기" 가 아니라 계산기 + 되묻기였다.** `problem.md` §3 이 받겠다고
적어 둔 **두 입력 중 하나(내 선호)** 가 코드에 하나도 없었고, 정렬은 `net_hi` 단순
내림차순이었다. 4-e 의 자기보고가 그 자리를 정확히 가리켰다.

**단위를 "금리 몇 %p 와 맞바꾸겠는가" 로 뒀다.** `problem.md` §3 의 원래 서술(추상 0~1
가중치)을 안 쓴 이유가 둘이다 — 0~1 로 정렬하면 화면에 *"왜 이 순서인지"* 를 못 적고,
정규화 기준(min·max)이 후보 집합에 종속돼 `0029` 와 **같은 실패 모양**이 된다.

```
설문 5문항 → 고정 표 → 조정 %p → 점수 = net_hi + 조정 → 정렬
python src/analysis/ask_loop.py 20260826 --survey                    문항을 본다
python src/analysis/ask_loop.py 20260826 --prefs 확실성=많이,이행=많이   가중치를 건다
```

**다섯 문항 중 숫자에 근거가 붙는 것은 둘뿐이고, 그렇게 표에 적었다.**

```
처음기관=많이   -3.00%p    실측 하한 2.67%p — 사용자가 그 격차를 보고도 안 옮겼다. **표본 1**
확실성=많이     -1.00      `--sort lo` 와 **항등식**으로 같다 (점수 = net_hi - (net_hi-net_lo) = net_lo)
                          계수 0 이면 `--sort hi`. 두 끝이 이미 `0017` 이 정한 값이다
나머지 셋                  **"근거 없음 — 판단"**
```

**사전등록의 첫 항목이 "무엇을 주장하지 않는가" 다** (`prereg-12` §1). *"정렬이
좋아졌다"* 를 잴 정답이 없다 — 가중치를 넣으면 순위가 바뀌지만 그게 나아진 건지
확인할 방법이 없다. **순위 교체 수도 방향이 없는 지표다** — 크면 "작동한다", 작으면
"없어도 된다" 는 뜻일 뿐이다.

| | 예측 | 실측 |
|---|---|---|
| **지표 무변동 (주 판정)** | 한 자리도 안 변한다 | **적중** — 37/67 · 85/106 · 폭 0.185 · 0.386 · 커버리지 84.8% · 35.7% |
| 화면 계약 A1~A11 | 전부 통과 | **전부 통과** (권역 둘 × 선호 있음/없음) |
| 1위가 바뀌는 조합이 있다 | 있다 | **적중** — 은행권 `거래기관+처음기관` · 저축은행 `영업점=못간다` 등 |
| `확실성=많이` == `--sort lo` | 순서가 완전히 같다 | **적중** — 페르소나 셋 × 권역 둘 전부 |
| 선호 없으면 지금과 같다 | 소수점까지 같다 | **적중** |

**화면이 거짓말하는 자리를 하나 더 찾았다.** `0029` 의 성과 줄이 `최고 2.79~2.88%` 라고
썼는데 **같은 화면 3위에 3.26% 가 있었다** — 선호가 걸리면 1위가 최고 금리가 아니다.
`최고` → `1위` 로 고쳤다. `0019` 의 부산은행과 같은 모양을 말 한 단어가 만든다.

**두 가지를 안 했다.** 기본 가중치를 정해 두지 않았고(`--prefs` 없으면 가중치 0개 ·
`0024` P2), 선호로 상품을 목록에서 빼지 않았다(맨 아래로 내리고 이유를 적는다 —
빼면 A7 과 똑같은 은폐 방지 장치를 하나 더 만들어야 한다). 뒤엣것 때문에
`problem.md` §3 의 *"영업점 필수 상품 제외"* 를 정정했다.

**측정 중에 더 큰 것을 하나 찾았고, 고치지 않고 이슈로 냈다** —
[#25](https://github.com/hyos0415/fineprint/issues/25).

```
행의 정체       (기관코드, 상품코드, 단리/복리, 적립방식) — 원천으로 **완전히** 설명된다
               은행권 79 = 79 · 저축은행 297 = 297. 중복 레코드가 아니다

rank_questions  상품을 **상품코드만으로** 센다 -> 저축은행 92개로 보인다
               기관코드까지 보면 **183개**다 (기관명 30종 아래 기관코드 58개 —
               하나저축은행이 세 법인이고 셋 다 상품코드 240001 을 쓴다)
ask_budget      분모를 **행으로** 센다  -> 커버리지 106/297 · 확정률 85/106
```

**두 층이 다른 단위를 센다.** 행 기준으로 다시 세면 질문 우선순위가 **1위부터 달라진다**
(저축은행 `비대면_채널가입` 이 15상품 vs 120행). `0018` 이 고정한 질문 우선순위와
게이트 분모를 동시에 건드리므로 **사전등록이 먼저다.** #24 에서 손댄 것은 검사기
두 곳뿐이고 계산·지표 경로는 안 건드렸다 — 그래서 M9 가 소수점까지 같다.

**`rows` 에 `fin_co_no`·`intr_rate_type_nm`·`rsrv_type_nm` 셋이 안 담겨 있다.**
`kor_co_nm`(#15) · `join_way`(#22) 때와 **같은 자리 세 번째**다 — 원천에 있는데
`load_pairs()` 로 안 넘어와서 부품이 통째로 없었다.

측정 `python src/analysis/prefs_survey.py 20260826 --group bank` ·
`python src/analysis/check_screen_contract.py 20260826 --prefs 확실성=많이`

### 5. 계단식 우대 — 실체는 3문구였다 (`0023`)

`prereg-06` §1.6이 "11건 · 1.0%"라 적은 열린 결정이다. **세어 보니 11문구 중 8문구는
계단이 아니었다** — 같은 파일의 임계 정규식 둘이 안 맞아서 생긴 오분류였다.

```
THRESHOLD_MONEY   "500만원이상" 의 `이상` 을 흡수한다 → "임계가 있다"
MONEY_VALUE       흡수부가 없다. `이` 가 한글이라 막힌다 → "못 읽겠다"
결과              "있는데 못 읽겠다" = 계단식        ← 오분류
```

`"해지원금 기준 500만원이상 : 최고0.2%p"` 에는 단계가 한 개도 없다. **짝을 맞추자
8문구가 `단계불명`(되물어도 못 없앤다)에서 `수치필요`(되물으면 없어진다)로 옮겨갔다.**

| | 전 | 후 |
|---|---|---|
| `단계불명` 문구 | 은행권 7 · 저축은행 4종 | **은행권 3 · 저축은행 3종** |
| 은행권 확정 (예산 12) | 39/67 | 39/67 — **변화 0** |
| 은행권 확정 (예산 40) | 56/67 | **64/67** |
| 은행권 폭 평균 (예산 12) | 0.185%p | **0.176%p** |
| 커버리지 | 84.8% · 35.7% | **안 움직였다** |

**효과가 크지만 13번째 질문 뒤에 있다.** 옮겨간 문구의 수치 후속 질문이 예산 12개
밖으로 밀린다. 사전등록 예측(확정 ≥60%)이 틀렸고, 틀린 이유가 "효과가 없다"가 아니라
**"보는 창이 다르다"** 인 것을 `prereg-08` §6.1에 남겼다.

**진짜 계단 3문구는 그대로 `단계불명`으로 낸다** (후보 A 확정). 스키마에 `tiers`를
넣는 후보 B는 기각했다 — 3문구를 위해 재추출하지 않는다.

```
"요구불평잔 : 0.2% -300만원이상 0.1%, 500만원이상 0.2%"
"신용(체크)카드결제실적 : 0.1% -전월결제금 300만원이상 0.05%, 500만원이상 0.1%"
"당 저축은행 APP 월1회 이상 로그인 기록 횟수에 따라 차등적용+2.0% (12회이상:2.0%)"
```

### 6. 데이터 수집 — 우선순위가 바뀌었다

**상품설명서를 1순위에서 내렸다** (`decisions/0011`). 제주은행 J정기예금 설명서를
실물로 확인했더니 **설명 안 되는 금리가 거기에도 없었다.** 법정 공시 서류에서도
"이벤트시 별도 고시"로 끝낸다. 기본금리는 설명서와 공시가 여섯 구간 전부 일치했다.

```
1순위  정책·정부 상품     청년도약계좌 등. API 에 0건 — "같은 상품, 다른 은행"의 유일한 사례
2순위  지점 위치          편의성 축 2. 금융MAP API 는 기관만 이용 가능해서 다른 경로 필요
3순위  상품설명서         세제 혜택·가입 제한·중도해지 이자율 목적으로만.
                        API 에 없는 정보가 실재한다 (비과세종합저축 가능 여부 등)
```

목록은 `docs/spec/collection-list.md`. **유형별로 한 번씩만 확인하면 된다** —
케이뱅크 "입금할 때마다 랜덤" 처럼 성격이 다른 것만 골라 받는다. 청년도약계좌 같은 상품은 은행마다 금리가 같아서
편의성(비대면 가능 여부·지점 위치)이 결정적인 유일한 경우인데,
**금감원 API에 0건**이다. 이게 없으면 그 문제를 보여줄 수 없다.

### 7. 지금은 손댈 수 없는 것 — 사유를 옆에 적어 둔다

**"다음에 할 일"에 남겨두면 실제로 할 수 있는 1~3번이 안 보인다.** 지우지는 않는다 —
언제 다시 살아나는지가 각각 다르기 때문이다.

| 항목 | 왜 지금 못 하나 | 언제 살아나나 |
|---|---|---|
| **E8 — 월 갱신에 규칙 파서가 깨지나** | **9월 공시가 아직 없다.** 기준 스냅샷 `20260826`은 확보돼 있다 | 매월 20일 전후 갱신 → 9월 공시가 올라오면 바로 |
| **E2~E7** | 선행 조건이 안 채워졌다 (`design.md` §4). RAG·리랭커는 상품설명서 코퍼스가 먼저다 | 코퍼스가 모이면. **선행 조건 없이 먼저 만들지 않는다** — `KAG_LlamaIndex`가 한 실수다 |

E8이 재려는 것은 **월이 바뀔 때 규칙 파서가 몇 건이나 깨지는가**다. "월 5건"이면
AI 없이 가고 "월 60건"이면 필요하다. 재추출 비용 자체는 월 1회 $1 수준이라
판단 근거가 되지 못한다(`0010`).

### ✅ 이미 끝난 것

| 항목 | 결과 | 기록 |
|---|---|---|
| **E1 폴백 재측정** | 은행권 83.9% · 저축은행 79.1%. 사전등록 예측 83.8% 통과. **부 지표가 전부 같은 방향** — 넘침도 줄고 항목 0개도 줄었다 | `0012` · `prereg-04` |
| **도피 방지 제약식** | 커버리지 게이트 + 질문 예산 12개 (위 0번) | `0018` · `prereg-06` §2.3(1) |

E1에서 **예상과 달랐던 것** 하나는 기록해 둘 값어치가 있다 — 실패 방향 차이는 권역이
아니라 **추출기 특성**이었다. A만 권역에 따라 방향이 바뀌고, B는 두 권역 모두 모자람이
주다(제한 스키마는 열거값에 없는 것을 버리므로 과소로 기운다 — 사용자 화면에서는
안전한 방향).

## 2026-08-26에 정한 것 — 셋 다 "모르면 되묻는다"

| 결정 | 내용 | 효과 |
|---|---|---|
| [`0015`](./docs/decisions/0015-ask-back-for-thresholds.md) | 금액·횟수 임계는 **되묻는다**. 재추출 필요 없었다 — 문구에 숫자가 있다(143개 중 132개 파싱) | 확정 은행권 51→64 · 저축은행 86→105 |
| [`0016`](./docs/decisions/0016-ask-back-for-everything.md) | **유형을 가리지 않고** 되묻는다. 공시 문구를 그대로 인용하고 "모르겠다" 선택지를 둔다. 못 채우면 사유 6종을 문장으로 | 전부 되물으면 은행권 범위 **0** |
| [`0017`](./docs/decisions/0017-start-from-the-best-and-narrow.md) | **최고 금리부터 보여주고 물어가며 깎는다.** 최댓값은 단독 표시 금지(항상 범위 + 남은 조건 수) | 최소 순은 사실상 기본금리 순이라 좋은 상품이 묻혔다 |

`prereg-06`의 열린 결정 다섯 개 중 **1번·2번이 닫혔고**, 이튿날 **3번(회피 방지 제약식)도 닫혔다**(`0018`).

## 2026-08-28에 정한 것

| 결정 | 내용 |
|---|---|
| [`0025`](./docs/decisions/0025-answer-handling-in-the-question-loop.md) | **질문 루프가 답을 읽는 방식.** 수치 "아니오"는 유형 자체를 지우고, 수치 "모르겠다"는 다시 묻지 않는다. 시뮬레이션에서는 안 나타나던 무한 되묻기가 사람용 루프 첫 시험에서 나왔다 |
| [`0026`](./docs/decisions/0026-questions-are-not-negotiable.md) | **질문 수는 협상 대상이 아니다.** `0024` 반증 조건 1을 폐기했다 — 질문을 줄이면 최고 금리는 그대로 두고 근거만 없애는 것이다. 완주율·중단률은 지표가 아니다. 재는 것은 **중단 지점의 화면이 단정하지 않는가** 로 바꿨다 |
| [`0027`](./docs/decisions/0027-ask-the-clause-not-the-number.md) | **임계는 숫자로 받지 않고 문구로 묻는다.** 한 유형 아래 적립식예금 잔액·펀드 보유액·정기예금 가입액이 묶여 "얼마입니까" 에 답이 없었다. 사용자가 `예` 를 세 번 거절당한 뒤 **사실이 아닌 `아니오`** 로 답한 것이 근거다 |
| [`0029`](./docs/decisions/0029-progress-shows-my-rate-range.md) | **성과를 "내 금리 범위" 로.** 사람 세션에서 *"진행 바밖에 안 보인다"* 가 나왔다 — 후보 4개 스코프에서 확정 수가 6개 질문 동안 0으로 고정돼 있었다. `0024` 반증 조건 4가 예고한 대안을 그대로 채택 (2026-08-31) |
| [`0028`](./docs/decisions/0028-candidate-scope-is-first-class.md) | **후보 집합을 1급 개념으로.** 질문은 후보에서만 만들고(39→7개), 지표는 카탈로그 고정, **스코프 밖 최고 금리를 항상 표시**(A7). 설문은 하지 않는다 — 모집단 평균으로 개인 스코프를 정하면 `0024` P2 위반이다 |

## 2026-08-27에 정한 것 — 하루에 일곱 개

| 결정 | 내용 |
|---|---|
| [`0018`](./docs/decisions/0018-anti-gaming-constraint.md) | **도피 방지 제약식** — 커버리지 게이트 + 질문 예산 12개. 원래 후보 셋이 구멍 하나를 통째로 놓치고 있었다 |
| [`0019`](./docs/decisions/0019-not-a-condition-and-unclear.md) | **조건 아닌 문구를 규칙으로 걸러낸다.** 부산은행이 아무 조건 없이 4.80%로 보이던 것을 고쳤다 |
| [`0020`](./docs/decisions/0020-self-consistency-not-alias-dict.md) | **같은 문구는 같은 유형으로.** 별칭 사전이 아니다 — 데이터에서 나온다 |
| [`0021`](./docs/decisions/0021-adopt-v2-over-the-gate.md) | **유형 배정 재추출 채택.** 홀드아웃 86%(예측 33%). 게이트 하나를 사람이 넘겼고 그 사실을 남겼다 |
| [`0022`](./docs/decisions/0022-exclusive-group-as-a-band.md) | **배타 그룹을 못 믿으므로 범위로.** 라벨이 반반쯤 틀린다 |
| [`0023`](./docs/decisions/0023-stop-narrowing-rules-ask-instead.md) | **규칙을 그만 좁히고 되묻는다.** 계단식 11문구 중 8문구는 정규식 구멍이었다 |
| [`0024`](./docs/decisions/0024-ask-back-flow.md) | **되묻기 흐름을 정의한다.** 전부 묻고, 남은 질문 수는 줄어들기만 한다 |

**`0015`부터 `0024`까지 열 번 중 일곱 번이 같은 답이었다** — 모르면 추측하지 말고
범위로 내고 이유를 말한다. 이게 이 시스템의 성격이 됐다.

## 이미 닫힌 방향

- **참조 사슬 순회** — 폐기(`0002`). 조건이 가리키는 입출금통장·청약·카드는
  금감원 API에 엔드포인트가 **없다**. 2026-08-26 API 전수 조사로 재확인했다
- **규칙 파서를 더 깎는 것** — 종료(`0007`). 세 라운드 깎아 개발 집합은 +9.1%p,
  처음 보는 기관은 **+0.5%p**였다. 규칙이 배운 건 특정 은행들의 표기 습관이었다
- **그래프 유사도로 비교 쌍 뽑기** — 근거 없음(`0014`). 묶는 축은 기관이지만
  (기관 2.17배 · 상품군 1.25배 · 권역 1.23배), 기관으로 묶고 나면 남는 구조가
  **0.3%**(11/4,180)뿐이고 조건 4개 이상 상품에서는 **0%**(0/248)다
- **회사 계열 연결** — 근거 없음. 금감원 API에 계열 코드가 없고, 카드 상품도 없다
- **대출 상품군 확장** — **추후 과제로 미룸.** 주담대 105·전세 57·신용대출 114건이
  API에 있지만 우대조건 문구 필드가 없고 금리 구조가 다르다(최저·평균·최고 / 신용등급별).
  조건이 금리를 만드는 구조가 아니라 닫힘률이 성립하지 않는다. 하려면 별도 상품군 +
  별도 규칙으로 분리해야 한다 (`design.md` §4.2)

## 하지 말 것

- ❌ **측정한 뒤에 기준을 정하지 않는다.** 정의·임계·표본을 재기 전에 커밋한다
- ❌ **홀드아웃을 보고 규칙을 고치지 않는다.** 고치면 홀드아웃 자격을 잃는다
- ❌ **규칙 파서 v2를 수정하지 않는다.** 파일럿 30문항과 gold가 v2로 만들어졌다
- ❌ **예측 라우터를 앞에 세우지 않는다.** 검사 게이트로 되는지 먼저 본다
- ❌ **LLM이 숫자를 만지게 하지 않는다.** 출력 스키마에서 분리한다
- ❌ **잴 수 없는 것을 성과로 주장하지 않는다.** R4는 편의 기능이라고 적는다
- ❌ **열린 이슈를 참조하는 작업을 `main`에 직접 커밋하지 않는다.** `CLAUDE.md`의 기준은
  하나다 — **이슈가 열려 있으면 브랜치·PR**. `decision-needed` 같은 장기 추적 이슈도
  포함된다. 2026-08-27에 `prereg-08`·`0023`·`0024`를 전부 #2를 참조하며 만들고도 `main`에
  직접 커밋했다(사람이 발견). 직접 커밋은 **이슈가 없는 작업과 문서 정비**뿐이다.
  **세션 첫 단계로 `git branch -vv` + `gh issue list --state open`을 같이 확인한다**
- ❌ **원본 데이터를 커밋하지 않는다.** `data/`는 `.gitignore`에 있다
- ❌ **미정 항목을 Agent가 임의로 확정하지 않는다.** 후보와 trade-off를 정리해 사람에게 묻는다

## 계보

```
KAG_LlamaIndex     그래프부터 만들고 쓸 곳은 나중에 → 한계 발견
        ↓
finance_verifier   평가부터 만들기 → 검증기가 조건을 놓치는 실패를 실제로 관측
        ↓
reranker_FT        리랭커 PEFT → 평가 결함 4건으로 수치 철회 (EVAL_AUDIT.md)
        ↓
FINeprint          앞선 셋을 하나로. 부품마다 "없으면 어떻게 되나"를 적고 측정한다
```

참고 저장소 (읽기만 하고 수정하지 않는다):

- https://github.com/hyos0415/finance_verifier — 검증기·클레임 분해 파이프라인
- https://github.com/hyos0415/KAG_LlamaIndex — 그래프 시도
- https://github.com/hyos0415/reranker_FT — 리랭커 PEFT (수치 철회 상태)

## 저장소

https://github.com/hyos0415/fineprint (public, `main`) ·
사람이 정해야 할 결정은 [이슈](https://github.com/hyos0415/fineprint/issues)에 있다.
