# START-HERE — FINeprint

> 새 세션은 이 문서부터 읽는다. 마지막 갱신 2026-08-24.

## 이 프로젝트를 한 문장으로

AI가 금융 상품을 설명할 때 **빼먹는 필수조건**을, 문장 뜻을 비교하는 방식이 아니라
**조건을 구조로 적어두고 계산해서** 잡아낸다 — 단, **구조 없이도 되는지부터 먼저
확인한다.**

겨냥하는 실수는 두 가지다. 둘 다 선행 프로젝트 finance_verifier에서 실제로 관측됐고,
**프롬프트를 고쳐서는 안 됐던** 것들이다.

| 실수 | 무슨 일이 벌어지나 |
|---|---|
| `condition_omission`(필수조건 누락) | 조건 A·B를 다 채워야 하는데 답변이 A만 말했고, 검사기가 통과시킨다 |
| `INSUFFICIENT` ↔ `UNSUPPORTED` 혼동 | "판단할 정보가 없음"과 "명시적으로 틀림"을 구분하지 못한다 |

문제 자체가 처음이면 [`README.md`](./README.md)에 실제 상품 예시가 있다.

## 먼저 알아둘 말 몇 개

이 문서와 `docs/`에서 반복해서 나오는 말들이다.

| 말 | 뜻 |
|---|---|
| **검증기 / Verifier** | AI 답변이 공시 근거로 뒷받침되는지 판정하는 프로그램. finance_verifier 코드를 그대로 재사용한다 |
| **evidence** | 판정의 근거로 넣어주는 금융감독원 공시 원문 |
| **claim** | 검사 대상이 되는 AI 답변 문장 하나 |
| **baseline(비교 기준선)** | "구조를 안 쓰고도 되는지" 재보는 값싼 방법. 여기에 지면 구조는 필요 없다 |
| **반증 게이트** | 그래프·스키마를 만들기 전에 통과해야 하는 확인 절차(`docs/handoff/v2.md` §3). G0·G1·G2·G4·G5 |
| **1단계 / 2단계** | 1단계는 "상품 하나 안에서" 조건 누락을 잡는 문제. 2단계(조건이 다른 상품을 가리킬 때)는 **측정 후 폐기됐다** — 아래 §5 |
| **사전 등록** | 재기 **전에** 예상치와 판정 임계를 커밋하는 것. 결과를 보고 기준을 고르는 걸 막는다 (`docs/spec/prereg-*.md`) |
| **평가 slice** | 성능을 재는 데 쓰는 사례 묶음. 정답 라벨이 붙은 claim 세트 |
| **ablation(부품 제거 실험)** | 만든 부품을 하나씩 빼며 재서, 성과가 실제로 어느 부품에서 나왔는지 가리는 측정 |

## 현재 상태

```
뼈대 + 계획(handoff v2) 확정.   구현 0줄.   실험 0건.
```

결정 기록 2건(`0001` 폐기 → `0002` 사슬 순회 폐기)과 사전등록 1건(측정 완료)이 있다.
적금 59건 · 예금 38건 스냅샷은 로컬에만 둔다(`data/`는 커밋하지 않는다).

착수 전에 **사람이 정해야 할 것이 남아 있다** (아래 §다음에 할 일 0번).

## 읽는 순서

아래 순서대로 읽으면 "왜 이 계획인가"까지 따라올 수 있다.

| 순 | 문서 | 무엇이 있나 |
|---|---|---|
| 1 | **`docs/handoff/v2.md`** | **현재 유효한 계획.** 특히 §3 반증 게이트 |
| 2 | `docs/reference/finance-verifier-findings.md` | 출발점이 된 실제 측정값. 무엇이 확인됐고 무엇이 아직 안 됐는지 |
| 3 | `docs/decisions/README.md` | 이미 정한 것 · 아직 사람이 정해야 할 것(D1~D5) |
| 4 | `docs/audit/00-comparison.md` | 계획을 v1에서 v2로 고친 근거 (필요할 때만) |
| — | `docs/handoff/v1-original.md` | 옛 계획. 보존용이며 **계획으로 읽지 않는다** |

## 다음에 할 일 (순서 고정)

### 0. 사람이 정할 것 — 이것부터

이 세 개가 정해지기 전에는 실험을 시작해도 의미 있는 숫자가 안 나온다.
**Agent가 임의로 정하지 않고, 후보와 장단점을 정리해서 사람에게 묻는다.**

- **D1 — 사례를 몇 건까지 늘릴 것인가.** 지금 `condition_omission` 사례가
  **전체 3건**이고, 모델을 바꿔가며 한 확인은 사례 2건으로 했다. 이 크기에서는
  어떤 개선도 "우연이 아니다"라고 말할 수 없다
- **D4 — 게이트 통과 기준.** 값싼 방법이 얼마나 잡아내면 "구조 필요 없음"으로
  판정할 것인가. **재기 전에** 정해야 한다 (재고 나서 기준을 정하면 결과에 맞춰
  기준을 고르게 된다)
- **D3 — 조건을 누가 어떻게 판정하는가.** 공시 원문에 "모두 충족" 같은 표현이
  분명히 적힌 상품이 38개 중 **6개**뿐이다. 나머지를 사람이 어떤 규칙으로 판정할지,
  판정이 불가능한 사례는 어떻게 제외할지 정해야 한다

### 1. 적금 스냅샷 수집 (v2 §5.1)

조건이 명시된 상품이 6개뿐이라는 상한을 풀기 위해 은행 **적금**까지 대상을 넓힌다.
적금은 필드 구성이 정기예금과 거의 같아서 finance_verifier의 정리 코드를 대부분
그대로 쓸 수 있다.

### 2. G0 — 평가 slice 만들기

성능을 잴 사례 묶음을 만든다. 이때 **라벨을 처음부터 붙인다**(v2 §8.5: 상품군 /
조건 구조 / 실패 유형 / 정답 출처). 나중에 붙이려 하면 결과를 쪼개서 볼 수 없다.

"틀리지 않은 답변을 잘못 걸러내지는 않는지" 재는 데 쓸 사례는 finance_verifier에서
오류가 없다고 확인된 claim **71건**을 재사용할 수 있다.

**이미 알려진 예금 3건(`p002_c04_3` · `p020_c02` · `p034_c02`)은 반드시 유지한다** —
선행 프로젝트 결과와 직접 맞대볼 수 있는 유일한 지점이다.

### 3. G4 → G5 ← 가장 먼저 돌릴 두 실험

조건을 구조로 정리한 다음, **누가 비교할지**만 다르게 해서 두 번 잰다.

- **G4**: 정리된 조건을 **코드**가 비교한다 (혜택 단위로 묶기 + 한 단계 중첩까지)
- **G5**: **같은 정리 결과를 LLM에게 자료구조 형태로 주고** "조건 전부가 답변에
  언급됐는지 확인하라"고 지시한다

이 둘을 비교해야 성과가 어디서 나온 건지 갈린다 — **조건을 정리한 것 자체**인가,
**코드로 엄격하게 검사한 것**인가.

> **G5가 통과하면 거기서 끝낸다.** 그래프도, 코드 검사기도 필요 없다는 뜻이고,
> "그래프 안 써도 됐다"를 측정으로 보인 것이므로 실패가 아니라 결론이다.

### 4. G1 → G2 → 게이트 판정

- **G1**: 프롬프트만 고쳐서 되는지 ("답변에 언급되지 않은 필수조건이 있는지 공시
  전체와 대조하라"를 명시)
- **G2**: 답변을 쪼개는 단위를 바꿔서 되는지 (쪼개는 단계에서 "이 답변이 인용한
  조건 집합"을 직접 뽑아내게 한다)

판정 표는 `docs/handoff/v2.md` §3 그대로 쓴다. 결과는 `docs/decisions/`에 5항목
형식으로 남긴다. **어느 게이트에서 끝나든 리포트는 쓴다.**

### 5. 이미 닫힌 질문 하나 — 사슬 순회(2단계)는 폐기됐다

조건이 **다른 상품을 가리키는** 경우가 있다. iM함께예금의 우대조건은 "iM함께적금에
동시 가입하고 만기까지 보유"를 요구하고, 그 적금에도 자기 조건이 있다. 판정에 필요한
정보가 예금 하나의 레코드 안에 없으니, 프롬프트로는 해결되지 않는다.

그래서 "사슬을 따라가야 잡히는 누락이 있는가"를 2단계 질문으로 세우고, **적금을
넣으면 사슬이 몇 건까지 늘어나는지 예측을 먼저 커밋했다**
(`docs/spec/prereg-01-savings-scope.md`, 커밋 `96788ec`). 임계는 "6건 미만이면 폐기"였다.

그다음 적금 전수 59건을 수집해 세어 보니 **4건**이었다. 임계 미달 → **폐기**
(`docs/decisions/0002-drop-chain-traversal.md`).

**왜 4건뿐이었나.** 참조 자체는 오히려 예측보다 많았다(적금 조건의 51.8%가 외부 상품·
서비스를 요구한다). 그런데 그 대상이 대부분 **입출금통장 · 주택청약 · 카드**이고,
금융감독원 API에는 그 상품군 엔드포인트가 **아예 없다**(404 실측). 즉 더 모아서
해결되는 부족이 아니다.

**얻은 것은 남는다.** 폐기는 "이 방향이 끝"이라는 뜻이고, 측정이 준 것은 세 가지다.

- **조건이 외부 상품 가입을 요구하는 상품이 41건**(적금 29 + 예금 12). "AI가 그 요구를
  숨겼는지"는 사슬 없이 한 홉에서 물을 수 있고, 표본이 41건이라 측정이 성립한다
- **적금 조건의 53.6%가 횟수·기간을 담는다**("매월 자동이체", "12회 이상 납입").
  있음/없음 비교로는 표현이 안 되므로 조건 표현 형식(D2)에 수량 비교가 필요하다
- **공시 데이터가 4일 만에 바뀐다.** e-그린세이브예금의 우대이율이 0.1%→0.2%로 변했고
  NH고향사랑기부예금에는 조건이 하나 추가됐다. **정답은 스냅샷 ID와 함께 고정해야 한다**

## 하지 말 것

- ❌ **게이트를 통과하기 전에 스키마·그래프 코드를 쓰지 않는다.**
  `docs/spec/schema.md`가 비어 있는 건 실수가 아니라 결정이다
- ❌ **검증기를 새로 만들지 않는다.** finance_verifier 코드를 그대로 재사용한다
  (비교의 기준이 되는 통제군 A)
- ❌ **아직 안 정해진 것(D1~D5)을 Agent가 임의로 확정하지 않는다.** 후보와 장단점을
  정리해서 사람에게 묻는다
- ❌ **표기가 다른 말을 같은 말로 묶어주는 사전(별칭 정규화)을 만드는 쪽으로 가지
  않는다.** 선행 저장소가 시도해서 기각했다 (다시 등장하는 항목이 4.5%뿐이라 효과가
  없었다). 해법은 뽑아낼 때 형식을 제한하는 것이다
- ❌ **재고 나서 지표를 정의하지 않는다.** 정의 · 기준값 · 표본 크기를 재기 전에
  적어서 커밋한다

## 계보

```
KAG_LlamaIndex (News-Arena)   그래프부터 만들기 → 한계 발견
        ↓
finance_verifier              평가부터 만들기 → 필수조건 누락 발견 (사례 3건)
        ↓
FINeprint                     "구조가 필요한가" 확인 먼저 → 필요한 만큼만 → 효과 측정
```

참고 저장소 (읽기만 한다, 수정하지 않는다):

- https://github.com/hyos0415/finance_verifier (선행, 완료)
- https://github.com/hyos0415/KAG_LlamaIndex (선행, 보존)

## 저장소

로컬 git만 초기화돼 있다. GitHub 원격은 **아직 없다** — 필요할 때 만든다.
