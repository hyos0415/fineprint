# START-HERE — FINeprint

> 새 세션은 이 문서부터 읽는다. 마지막 갱신 2026-08-26.

## 한 문장

**내 상황과 내가 중요하게 여기는 것에 맞는 예금·적금을 골라주는 시스템을 만든다.**

시스템은 선호를 추측하지 않는다. 사용자가 알려주고, 시스템은 계산만 책임진다.

정식 정의는 [`docs/spec/problem.md`](./docs/spec/problem.md), 부품별 설계는
[`docs/spec/design.md`](./docs/spec/design.md)에 있다.

> **2026-08-26에 타깃을 바꿨다.** 원래는 "AI 답변이 공시로 뒷받침되는지 검사하는
> 검증기"였다. 그건 선행 프로젝트 `finance_verifier`가 이미 한 일이라, 사용자가 실제로
> 겪는 문제(내게 맞는 상품 고르기)로 옮겼다. 사유와 무엇이 살아남는지는
> [`docs/decisions/0009-pivot-to-recommender.md`](./docs/decisions/0009-pivot-to-recommender.md).
> 옛 정의는 [`docs/spec/problem-v1-verifier.md`](./docs/spec/problem-v1-verifier.md)에 보존했다.

## 현재 상태

```
문제 정의·설계 완료.   다음은 데이터 수집과 E1 실험.
```

| 항목 | 상태 |
|---|---|
| 데이터 | 은행 97상품(조건 253행) · 저축은행 668상품(조건 1,090행) · 조건문 177종 |
| 조건 추출 | 동작 — 규칙 파서 v3 + 제한 스키마 LLM |
| A 규칙 파서 | 개발(은행권) **77.5%** · 처음 보는 기관 **63.2%** — 두 숫자를 함께 본다 |
| B 제한 스키마 LLM | 처음 보는 기관 **71.4%** (p=0.0987, 아직 유의하지 않다) |
| **A→B 폴백** | **채택** (`0012`) — 은행권 **83.9%** · 저축은행 79.1%. 사전등록 예측 83.8% 통과 |
| 분모 | "우대조건 있는 행" + **"공시 최고금리 > 기본금리"** — 폭 0 행은 `공시미반영` 층으로 |
| 결정 기록 | `0002`·`0005`·`0006`·`0007` 유효 · `0009`(타깃 전환) · `0010`(월 갱신) · `0004`·`0008` 일부 폐기 |
| 공시 갱신 | **월 1회** — 매월 20일 전후. 같은 달 안에서는 안 바뀐다(8/24→8/26 실측 0건) |

## 읽는 순서

| 순 | 문서 | 무엇이 있나 |
|---|---|---|
| 1 | **`docs/spec/problem.md`** | **현재 유효한 문제 정의.** 무엇을 만들고 왜 필요한가 |
| 2 | **`docs/spec/design.md`** | 부품별 계약·상태·"없으면 어떻게 되나"·측정 방법 |
| 3 | `docs/decisions/0009` | 왜 검증기에서 추천기로 바꿨나. 무엇이 살아남나 |
| 4 | `docs/decisions/` | 정한 것과 폐기한 것. 반증 조건이 세 번 실제로 발동했다 |
| 5 | `docs/spec/prereg-03`·`prereg-05` | 추출기 비교와 규칙 깎기. 방법론의 실제 사례 |
| — | `docs/spec/problem-v1-verifier.md` | 옛 정의. 보존용, **계획으로 읽지 않는다** |

## 먼저 알아둘 말

| 말 | 뜻 |
|---|---|
| **우대조건** | "급여이체 하면 +0.5%" 처럼 금리를 더 주는 조건. 공시에 자유서술로 적혀 있다 |
| **조건 유형 17종** | 그 조건들을 묶은 목록(자동이체·카드실적·첫거래…). 사용자에게 물을 항목이자 상품을 묶는 축 |
| **닫힘률** | 뽑아낸 우대금리를 다 더한 값이 공시의 (최고금리−기본금리)와 맞는 비율. **맞으면 그 상품은 계산해줄 수 있다** |
| **계산 불가 상품** | 조건을 다 더해도 광고 금리에 안 닿는 상품. 버리지 않고 "근거 없음"으로 표시한다 |
| **홀드아웃** | 규칙을 만들 때 한 번도 안 본 데이터. 처음 보는 데이터에서의 성능을 재려고 따로 떼어 둔다 |
| **사전 등록** | 재기 **전에** 예측·판정 기준을 커밋하는 것. 결과를 보고 기준을 고르는 걸 막는다 |

## AI가 들어가는 자리 — 그리고 안 들어가는 자리

**"AI 프로젝트니까 AI를 쓴다"로 가지 않는다.** 자리마다 근거와 측정이 붙어 있다.

| | 하는 일 | AI가 꼭 필요한가 | 잰 것 |
|---|---|---|---|
| **R1** | 공시 조건문 → 구조 | 규칙과 경쟁 중 | 규칙 62.6% · LLM 71.4% · **폴백 80.2%** |
| **R2** | 사용자 자연어 → 상태 | 그렇다 (폼만 쓸 거면 불필요) | 아직 안 쟀다 |
| **R3** | 분해가 원문과 맞나 판정 | 그렇다 — 의미 판단 | 규칙·LLM이 **9%**에서 갈린다 |
| R4 | 리포트 서술 문단 | 아니다 | **편의 기능.** 잴 지표가 없다 |

나머지(계산·정렬·링크·비교표)는 전부 결정론적 코드다.

## 다음에 할 일

### 1. 데이터 수집 — 우선순위가 바뀌었다

**상품설명서를 1순위에서 내렸다** (`decisions/0011`). 제주은행 J정기예금 설명서를
실물로 확인했더니 **설명 안 되는 금리가 거기에도 없었다.** 법정 공시 서류에서도
"이벤트시 별도 고시"로 끝낸다. 기본금리는 설명서와 공시가 여섯 구간 전부 일치했다.

```
1순위  정책·정부 상품     청년도약계좌 등. API 에 0건 — "같은 상품, 다른 은행"의 유일한 사례
2순위  지점 위치          편의성 축 2. 금융MAP API 는 기관만 이용 가능해서 다른 경로 필요
3순위  상품설명서         세제 혜택·가입 제한·중도해지 이자율 목적으로만.
                        API 에 없는 정보가 실재한다 (비과세종합저축 가능 여부 등)
```

목록은 `docs/spec/collection-list.md`. **유형별로 한 번씩만 확인하면 된다** —
케이뱅크 "입금할 때마다 랜덤" 처럼 성격이 다른 것만 골라 받는다. 청년도약계좌 같은 상품은 은행마다 금리가 같아서
편의성(비대면 가능 여부·지점 위치)이 결정적인 유일한 경우인데,
**금감원 API에 0건**이다. 이게 없으면 그 문제를 보여줄 수 없다.

### 2. 9월 공시가 올라오면 E8 측정

**매월 20일 전후에 공시가 갱신된다.** 기준 스냅샷 `20260826`(은행 예적금 97상품)을
확보해 뒀으니, 9월 공시를 받아 비교하면 된다.

```
재야 할 것   월이 바뀌면 몇 건이 바뀌나
            그중 규칙 파서가 깨지는 건 몇 건인가   ← 이게 진짜 E8
```

이 숫자가 "월 5건"이면 AI 없이 가고, "월 60건"이면 필요하다.
다만 재추출 비용 자체는 월 1회 $1 수준이라 크지 않다(`0010`).

### 3. E1 폴백 재측정 — ✅ 완료 (`decisions/0012`)

사전등록(`prereg-04`)을 실행 전에 커밋하고 은행권에 B를 돌렸다.

```
            A 규칙    B 스키마    폴백
은행권       75.8%     71.2%     83.9%    ← 예측 83.8% 통과
저축은행      63.2%     71.4%     79.1%

LLM 호출 24.2% · 진짜 과다 추출 16→12 · 항목 0개 6.4%→1.7%
```

**부 지표가 전부 같은 방향이다.** 주 지표만 오른 게 아니라 넘침도 줄고 항목 0개도 줄었다.

**예상과 달랐던 것** — 실패 방향 차이는 권역이 아니라 추출기 특성이었다.
A만 권역에 따라 방향이 바뀌고, B는 두 권역 모두 모자람이 주다(제한 스키마는
열거값에 없는 것을 버리므로 과소로 기운다 — 사용자 화면에서는 안전한 방향).

### 4. 나머지 실험은 선행 조건 대기

`design.md` §4에 E1~E8이 선행 조건과 함께 정리돼 있다.
**선행 조건이 안 채워진 부품을 먼저 만들지 않는다** — 그게 `KAG_LlamaIndex`가 한 실수다.

## 이미 닫힌 방향

- **참조 사슬 순회** — 폐기(`0002`). 조건이 가리키는 입출금통장·청약·카드는
  금감원 API에 엔드포인트가 **없다**. 2026-08-26 API 전수 조사로 재확인했다
- **규칙 파서를 더 깎는 것** — 종료(`0007`). 세 라운드 깎아 개발 집합은 +9.1%p,
  처음 보는 기관은 **+0.5%p**였다. 규칙이 배운 건 특정 은행들의 표기 습관이었다
- **그래프 유사도로 비교 쌍 뽑기** — 근거 없음(`0014`). 묶는 축은 기관이지만
  (기관 2.17배 · 상품군 1.25배 · 권역 1.23배), 기관으로 묶고 나면 남는 구조가
  **0.3%**(11/4,180)뿐이고 조건 4개 이상 상품에서는 **0%**(0/248)다
- **회사 계열 연결** — 근거 없음. 금감원 API에 계열 코드가 없고, 카드 상품도 없다
- **대출 상품군 확장** — **추후 과제로 미룸.** 주담대 105·전세 57·신용대출 114건이
  API에 있지만 우대조건 문구 필드가 없고 금리 구조가 다르다(최저·평균·최고 / 신용등급별).
  조건이 금리를 만드는 구조가 아니라 닫힘률이 성립하지 않는다. 하려면 별도 상품군 +
  별도 규칙으로 분리해야 한다 (`design.md` §4.2)

## 하지 말 것

- ❌ **측정한 뒤에 기준을 정하지 않는다.** 정의·임계·표본을 재기 전에 커밋한다
- ❌ **홀드아웃을 보고 규칙을 고치지 않는다.** 고치면 홀드아웃 자격을 잃는다
- ❌ **규칙 파서 v2를 수정하지 않는다.** 파일럿 30문항과 gold가 v2로 만들어졌다
- ❌ **예측 라우터를 앞에 세우지 않는다.** 검사 게이트로 되는지 먼저 본다
- ❌ **LLM이 숫자를 만지게 하지 않는다.** 출력 스키마에서 분리한다
- ❌ **잴 수 없는 것을 성과로 주장하지 않는다.** R4는 편의 기능이라고 적는다
- ❌ **원본 데이터를 커밋하지 않는다.** `data/`는 `.gitignore`에 있다
- ❌ **미정 항목을 Agent가 임의로 확정하지 않는다.** 후보와 trade-off를 정리해 사람에게 묻는다

## 계보

```
KAG_LlamaIndex     그래프부터 만들고 쓸 곳은 나중에 → 한계 발견
        ↓
finance_verifier   평가부터 만들기 → 검증기가 조건을 놓치는 실패를 실제로 관측
        ↓
reranker_FT        리랭커 PEFT → 평가 결함 4건으로 수치 철회 (EVAL_AUDIT.md)
        ↓
FINeprint          앞선 셋을 하나로. 부품마다 "없으면 어떻게 되나"를 적고 측정한다
```

참고 저장소 (읽기만 하고 수정하지 않는다):

- https://github.com/hyos0415/finance_verifier — 검증기·클레임 분해 파이프라인
- https://github.com/hyos0415/KAG_LlamaIndex — 그래프 시도
- https://github.com/hyos0415/reranker_FT — 리랭커 PEFT (수치 철회 상태)

## 저장소

https://github.com/hyos0415/fineprint (public, `main`) ·
사람이 정해야 할 결정은 [이슈](https://github.com/hyos0415/fineprint/issues)에 있다.
