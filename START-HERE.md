# START-HERE — FINeprint

> 새 세션은 이 문서부터 읽는다. 마지막 갱신 2026-08-25.

## 한 문장

**AI가 예금·적금 금리를 설명할 때, 사용자가 실제로 받을 금리를 과대하게 말하는 것을
잡아낸다.** 상품을 추천하는 AI가 아니라, AI 답변이 공시 원문으로 뒷받침되는지 검사하는
**검증기**를 만든다.

문제가 처음이면 [`README.md`](./README.md)에 실제 상품 두 개로 만든 예시가 있다.
정식 정의는 [`docs/spec/problem.md`](./docs/spec/problem.md)다.

## 현재 상태

```
추출기 비교 완료 · 판정 보류.   홀드아웃이 지표를 깨뜨렸다 — 다음은 prereg-04.
```

| 항목 | 상태 |
|---|---|
| 데이터 | 개발: 은행권 상품 97 · 조건 있는 행 253 · 스냅샷 `20260824`<br>홀드아웃: 저축은행 상품 668 · 조건 있는 행 1,090 · 스냅샷 `20260825` |
| 파일럿 | **완료** — 30문항 × 네 모델. 결과·판정은 `docs/spec/prereg-02-pilot.md` §7 |
| 판정 | 태스크 성립 · 주 타깃은 **"확인 불가 판정"** · 병목은 **추출** (`decisions/0004`) |
| 추출 채점기 | 동작 — `src/analysis/{score_extraction,compare_extractors}.py`. A 기준선 **62.8%**(은행권) · **62.6%**(저축은행, 지표 성립 행) |
| 추출기 비교 | **완료** — 결과·판정은 `docs/spec/prereg-03-extraction.md` §7 |
| 다음 실험 | `prereg-04` 미작성 — 분모를 고쳐 다시 잰다 (`prereg-03` §10) |
| 결정 기록 | `0001`(폐기) · `0002`(사슬 순회 폐기) · `0003`(선행 결론 정정) · `0004`(파일럿 판정) · `0005`(유형 16종·분석 단위) · `0006`(홀드아웃이 지표를 깨뜨림) |

**파일럿이 무엇을 보였나** — 네 모델(Qwen 4B · Kanana 3B · Haiku 4.5 · Sonnet 5) 전부
`조건없음` 층은 5/5로 맞히고, 조건이 붙으면 절반, 공시로 계산이 안 되는 상품에서는
0~2/10이다. **체급으로 해결되지 않는다.** 다만 "구조화하면 나아진다"는 아직 측정하지
않았다 — 그건 다음 실험이다.

**병목은 판정이 아니라 추출이다.** 주 실패(확인 불가인데 단정)는 결정론적 산수로 잡히며,
우리 규칙 코드가 이미 잡는다. 어려운 것은 자유서술 공시에서 조건을 뽑는 것이고, 규칙을
여덟 라운드 고치는 동안 매번 새 표기 변형이 나왔다.

**그래프는 여전히 정당화되지 않았다.** 지금까지 어떤 관측도 그래프를 요구하지 않는다.

원본 데이터는 커밋하지 않는다(`data/`는 `.gitignore`). 스냅샷·표본·응답은 로컬에만 있고
스크립트로 재생성된다.

## 읽는 순서

| 순 | 문서 | 무엇이 있나 |
|---|---|---|
| 1 | **`docs/spec/problem.md`** | **현재 유효한 문제 정의.** 실패 4종 · 외부 근거 · 고정한 시나리오 |
| 2 | **`docs/spec/prereg-02-pilot.md`** | 파일럿 설계. 표본·정답 규칙·실패 분류·**닫는 기준** |
| 3 | `docs/handoff/v2.md` §3 | 반증 게이트 — 비싼 것을 만들기 전에 싼 것으로 되는지 재는 순서 |
| 4 | `docs/decisions/` | 정한 것과 폐기한 것. `0002`가 왜 한 방향을 닫았는지 |
| 5 | `docs/spec/prereg-01-savings-scope.md` | 사전 등록이 실제로 작동한 사례(예측 7개 중 6개 적중, 틀린 하나로 방향 폐기) |
| — | `docs/handoff/v1-original.md` | 옛 계획. 보존용, **계획으로 읽지 않는다** |

`docs/handoff/v2.md`는 **문제 정의 이전에 쓰인 계획**이다. §3(게이트)과 §11(이관 분류)은
지금도 유효하지만, 타깃 서술은 `problem.md`가 우선한다.

## 먼저 알아둘 말

| 말 | 뜻 |
|---|---|
| **검증기 / Verifier** | AI 답변이 공시 근거로 뒷받침되는지 판정하는 프로그램 |
| **닫힘 / 안닫힘 / 조건없음** | 상품을 나누는 세 층. "공시된 최고금리를 명시된 우대조건으로 계산할 수 있는가"로 가른다(`prereg-02` §2) |
| **사전 등록** | 재기 **전에** 예측·판정 기준을 커밋하는 것. 결과를 보고 기준을 고르는 걸 막는다 |
| **반증 게이트** | 구조·그래프를 만들기 전에 값싼 방법으로 되는지 재는 절차(`v2` §3) |
| **ablation** | 부품을 하나씩 빼며 재서 성과가 어느 부품에서 나왔는지 가리는 측정 |

## 다음에 할 일 — 이슈 #5

`docs/spec/prereg-03-extraction.md`에 설계가 고정돼 있다. 순서대로.

### 1. 저축은행 홀드아웃 스냅샷 수집 — ✅ 완료 (2026-08-25)

```
저축은행 정기예금 391 · 적금 277 = 상품 668 · 기관 79 · 조건 있는 행 1,090
```

`python src/ingest/fetch_finlife.py {deposit|saving} --group savingsbank`로 받는다.
스냅샷 ID는 `20260825`로 고정했고, 계수와 함께 `prereg-03` §2.1에 적었다.

**여기서 나온 것 하나 — 분석 단위가 미정이다.** 조건 있는 1,090행이 서로 다른 조건문
75종에서만 나온다(한 조건문이 평균 14.5행). McNemar를 행 단위로 돌리면 같은 텍스트를
열네 번 세는 것이고 p값이 실제보다 작게 나온다. 후보 4개와 trade-off는 `prereg-03`
§2.1(나)에 있다. **정하기 전에 LLM 추출기를 돌리지 않는다.**

**홀드아웃을 보고 규칙을 고치지 않는다.** 고치면 홀드아웃 자격을 잃는다 — 결함을 찾으면
기록만 하고 이슈 #6으로 넘긴다. 조건 유형 열거값도 **개발 집합 빈도만 보고** 정한다
(그래서 지금까지 홀드아웃 조건문 본문은 읽지 않았다).

### 2. 조건 유형 16종 확정 — ✅ 완료 (2026-08-25, `decisions/0005`)

**축을 빈도순이 아니라 "사용자 상태로 O/X를 답할 수 있는가"로 잡았다.**

```
층 1 판정 가능 12종   타상품보유동시가입 · 자동이체 · 첫거래 · 마케팅동의 · 카드실적
                     비대면가입 · 목표달성 · 급여연금이체 · 주거래장기거래 · 잔액평잔
                     고객군자격 · 오픈뱅킹              (은행권 빈도순)
층 2 판정 불가 4종    실천미션인증 · 쿠폰코드추천인 · 무조건특판이벤트 · 판정불가불특정
+ 기타               17번째 탈출구. 기타 비율을 부 지표로 함께 보고한다
```

주 타깃이 "확인 불가 판정"이므로, 공시 문구만으로 누구도 판정할 수 없는 조건을 **유형
단계에서** 갈라두면 추출 결과가 그대로 판정 신호가 된다. 열거값 전문·빈도·경계 규칙표
(헷갈리는 문구를 어디로 보낼지)는 `prereg-03` §1.1에 있다.

**분석 단위도 같이 정했다.** 닫힘률은 행(1,090) · McNemar는 (조건문,기간) 쌍(228) ·
신뢰구간은 조건문 75종 클러스터 부트스트랩. 한 조건문이 홀드아웃 행의 15.8%를 혼자
차지하므로 **가중 닫힘률**(조건문마다 동일 가중)을 부 지표로 함께 본다 (`prereg-03` §2.1).

### 3~4. 추출기 구현 · 홀드아웃 비교 — ✅ 완료 (2026-08-25, `decisions/0006`)

**결론부터 — 판정을 채택하지 않았다.** 사전등록 기준대로는 "제한 스키마 채택"이 나왔지만,
같은 문서에 미리 적어둔 반증 조건이 발동해서 그쪽을 따랐다.

**무엇이 깨졌나.** 채점 방식은 "뽑아낸 우대금리를 다 더한 값이 공시의 (최고금리 −
기본금리)와 맞는가"다. 그런데 저축은행은 **우대금리를 최고금리 필드에 넣지 않는다.**

```
조건문이 있는 홀드아웃 행 1,090
  최고금리 == 기본금리   908행 (83.3%)   ← 차이가 0이라 맞출 방법이 없다
  최고금리 >  기본금리   182행 (16.7%)   ← 채점이 성립하는 행
```

원본 필드로 확인했다 — KB저축은행 정기예금 3개월은 `intr_rate`와 `intr_rate2`가 둘 다
2.0인데 조건문에는 "우대금리 연 0.1%p 적용"이 적혀 있다. 은행권은 이런 행이 없다.

**따라온 정정 하나 — A는 과적합되지 않았다.** 채점이 성립하는 182행에서 A는 62.6%이고,
개발 집합 62.8%와 사실상 같다. 홀드아웃 전체 숫자 10.5%는 분모에 닫을 수 없는 행 908개가
섞인 결과였다.

**판정과 별개로 확정된 것 두 개.** ① 제한 스키마는 형식으로서 작동한다 — 스키마 위반
0/228, `기타` 4.7%, 항목 0개 비율이 A보다 오히려 낮다. ② 비용은 문제가 아니다 —
228호출 $1.00 · 8분.

숫자 전부는 `prereg-03` §7, 사후 분석은 §7.2(**판정에 쓰지 않는다**)에 있다.

### 5. 다음 — `prereg-04` 작성

`prereg-03` §10에 넘긴 것이 네 개 있다. 요지는 두 개다.

- **분모를 고친다** — "우대조건이 있는 행" + "최고금리 > 기본금리". 조건문은 있는데 폭이
  0인 행은 지표가 말할 수 없는 행이다
- **표본이 부족하다** — 폭 > 0 행은 저축은행에 182개(조건문 46종)뿐이다. 그 규모에서는
  8.8%p 차이도 유의해지지 않는다(p=0.0987). 권역을 넓히거나 지표를 바꿔야 한다

**버리지 않을 것** — 폭 = 0인 908행은 다른 질문의 재료다. "조건문에 우대금리가 적혀
있는데 공시 최고금리에는 없다"는 것 자체가 `problem.md`의 실패 유형에 가깝다.

### (참고) 3단계 구현 명세

`prereg-03` §1.1의 16종 열거값과 §6의 실행 조건(Haiku 4.5 · temperature 0 · 재시도 없음)
으로 만든다. 출력은 structured outputs로 강제하고, **금리(기본·최고)를 입력에 주지
않는다** — 채점 신호를 보면 거기에 맞춰 값을 만들어낸다.

프롬프트·스키마 전문을 `prereg-03`에 붙인 뒤 실행하고, **실행 후 수정하지 않는다**(§6).

### 4. 홀드아웃에서 비교 → 판정

`prereg-03` §5 판정표를 그대로 적용한다. 닫힘률 차이 5%p + McNemar p<0.05 + 항목 0개
비율 제약. **규칙 파서가 이겨도 유효한 결과다** — 그때는 `0004`의 첫 번째 반증 조건이
발동한다.

## 이미 닫힌 방향

**조건이 다른 상품을 가리킬 때 사슬을 따라가는 것(2단계)** — 폐기됐다. 적금을 넣으면
사슬이 몇 건까지 늘어날지 예측을 먼저 커밋하고(임계 6건) 적금 전수 59건을 수집해
세어보니 **4건**이었다. 참조 자체는 예측보다 많았지만(적금 조건의 51.8%) 대상이 대부분
입출금통장·주택청약·카드이고, 금융감독원 API에는 그 상품군 엔드포인트가 **없다**.

기록: `docs/decisions/0002-drop-chain-traversal.md` · `docs/spec/prereg-01-savings-scope.md`

## 하지 말 것

- ❌ **측정한 뒤에 기준을 정하지 않는다.** 정의·임계·표본을 재기 전에 커밋한다.
  기준을 고쳐야 한다면 **모델을 돌리기 전에** 고치고, 사유·편향 방향·완화 조치를 함께
  적는다(`prereg-02` §2.1이 그 예다)
- ❌ **게이트를 통과하기 전에 스키마·그래프 코드를 쓰지 않는다.** `docs/spec/schema.md`가
  비어 있는 건 실수가 아니라 결정이다
- ❌ **검증기를 새로 만들지 않는다.** finance_verifier 코드를 재사용한다
- ❌ **표기가 다른 말을 묶어주는 사전(별칭 정규화)을 만들지 않는다.** 선행 저장소가
  시도해서 기각했다. 해법은 추출할 때 유형을 제한된 목록에서 고르게 하는 것이다
- ❌ **원본 데이터를 커밋하지 않는다.** API로 공개되는 것을 GitHub에 재배포하지 않는다
- ❌ **미정 항목을 Agent가 임의로 확정하지 않는다.** 후보와 trade-off를 정리해 사람에게 묻는다

## 계보

```
KAG_LlamaIndex     그래프부터 만들고 쓸 곳은 나중에 → 한계 발견
        ↓
finance_verifier   평가부터 만들기 → 검증기가 조건을 놓치는 실패를 실제로 관측
        ↓
FINeprint          문제를 데이터로 정의 → 구조는 필요한 만큼만 → 효과를 측정
```

참고 저장소 (읽기만 하고 수정하지 않는다):

- https://github.com/hyos0415/finance_verifier (선행, 완료)
- https://github.com/hyos0415/KAG_LlamaIndex (선행, 보존)

## 저장소

https://github.com/hyos0415/fineprint (public, `main`) ·
사람이 정해야 할 결정은 [이슈](https://github.com/hyos0415/fineprint/issues)에 있다.
