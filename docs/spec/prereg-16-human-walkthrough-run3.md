# prereg-16 · 사람이 바뀐 화면을 완주한다 (3런) — 목록 질문과 바뀐 낱말을 본다

> **#61 이전 (2026-09-04 표시).** 이 문서의 **저축은행** 값은 추출 행 절반이 다른 기관의 이름·조건을 달고 있던 때의 값이다(`decisions/0052` · `prereg-22`). 저축은행 숫자는 비교가 아니라 교체 대상이고 새 기준선은 `prereg-22` §6 이다. 은행권 값과 결정의 논리는 그대로 유효하다.

> 작성 2026-09-03 (**재기 전**) · 선행 `prereg-14`(1런·2런) · 화면을 바꾼 결정 `0045`(F6 ·
> 조건은 기관별로 갈린다) · `0046`(F5 · 화면 낱말을 사람 말로) · 로드맵 F7 의 선행

**이 문서는 재기 전에 쓴다.** `prereg-14` 의 예측 P1~P8 은 **옛 화면**에 대한 것이다.
그 사이에 화면이 두 번 크게 바뀌었다 — 질문이 기관별로 갈리고 맨 앞에 **목록 질문**이
생겼고(F6), 내부 이름·층·사유 코드가 전부 **사람 말**로 바뀌었다(F5). 옛 예측을 그대로
쓰거나, 돌리고 나서 기준을 고르면 이 저장소의 규율이 깨진다(`CLAUDE.md` 3번).

## 왜 재나 — 두 결정이 남긴 물음이 셋이다

```
1. 목록 질문이 답할 수 있는 형태인가        `0045` 가 맨 앞에 "거래해 본 은행을 골라 주세요"
                                        를 두었다. 은행권 17곳 · 저축은행 30곳을 이름으로
                                        줄 세운다. 사람이 자기 은행을 찾는가
2. 목록에 "모름" 을 고르는가               `0045` 반증 조건 2. 그 경로는 유도가 안 걸려
                                        예산 12개 시점 폭 평균 0.432(기준선 0.185) ·
                                        확정률 20.9% 다. **얼마나 흔한지를 모른다** —
                                        이 답이 로드맵 F7 의 착수 조건이다
3. 바뀐 낱말이 조건의 뜻을 바꿨나           `0046` 반증 조건 3. 가장 위험한 칸은
                                        `기타`(→ "그 밖의 조건") 와
                                        `판정불가_불특정`(→ "누가 받는지 미리 알 수 없는 우대")
```

2런에서 남은 것도 같이 본다 — 공시에 뜻이 없는 낱말(탑스 · 소중한 날)이 기관별 판정
뒤에도 질문으로 나오는가.

## §1 무엇을 세나 — 정의 (재기 전 확정)

`prereg-14` §6 의 정의를 그대로 잇는다. 새로 더한 것은 **L(목록)** 과 **W(낱말)** 다.

```
M1a  맥락이 없어서 고를 수 없었다 — 어느 은행/상품의 문구인지 몰라서
M1b  용어를 몰라서 고를 수 없었다 — 은행은 알지만 그 은행 고유 용어를 모른다
M1c  짐작으로 눌렀다 — 고를 수는 있었지만 근거가 없었다
     안 센다 — 의도적으로 "모름" 을 고른 것. 시스템이 지원하는 답이다 (`0024` P5)

M3   같은 걸 두 번 묻는 것처럼 느낀 자리 수
M4   완주했나 (예/아니오) · 걸린 시간 (초). **완주율은 지표가 아니다**(`0026` D2)

M2   뜻을 모르는 낱말 수 — **부 지표. 근거로 쓰지 않는다.**
     `prereg-14` §7 이 확인했다: 만든 사람이 표본이라 자기가 지은 낱말을 안 센다.
     세더라도 기록만 한다

L1   목록 질문에서 자기 은행을 **이름으로 찾았나** (예/아니오 · 못 찾은 은행 이름)
L2   목록 질문의 답 — 고른 은행 수 / "거래 없음"(하나도 안 고르고 제출) / "모름"
     (웹의 **모르겠습니다** 버튼 · CLI 의 `[모름]`). 웹은 두 답이 다른 동작이라
     (`screen.html` 안내문) 사람이 그 차이를 읽었는지도 적는다
L3   목록 질문에 걸린 시간 (초) — 나머지 질문 평균과 따로 적는다

W1   라벨만 보고 답한 뒤 **문구를 읽고 답을 바꾼** 자리 수
     라벨이 뜻을 바꿨는지 보는 유일한 관측 방법이다. 답을 바꿨다 = 라벨과 문구가
     다른 말을 했다
W2   `기타` · `판정불가_불특정` 라벨이 화면에 나온 횟수 (W1 과 대조한다)
```

## §2 표본 — 어떻게 도나

```
사람      1명 (만든 사람). `prereg-14` §4 의 약점이 그대로다 — M1·M2 는 **하한**으로 읽는다
세션      2회. 둘 다 12개월 · 찾는 범위 없음 · 선호 없음 · 정렬 최대순
  (가) 은행권      ← prereg-14 의 (가)와 같은 경로. 2런(39개 · 완주)과 **앞뒤 비교**가 된다
  (나) 저축은행    ← 목록이 30곳이다. 이름으로 아는 곳이 적어 "모름" 이 나올 가능성이
                    가장 큰 경로. 3런에서 처음 도는 권역이다
기록      질문마다 (순서, 조건 유형 라벨, 은행, 고른 답, M1 해당 여부, W1 해당 여부)
         목록 질문은 L1~L3 을 따로
어디서    웹 — python -m uvicorn src.web.app:app --host 127.0.0.1 --port 8000
         세션 로그 data/pilot/ask_session_*.json
```

**(나)를 넣는 이유** — F7 의 착수 조건은 *"모름 경로가 얼마나 흔한가"* 다. 은행권만
돌면 사람이 자기 은행을 아니까 "모름" 이 나올 리가 없고, 그러면 F7 물음에 답이 안 된다.
저축은행은 그 반대 극단이다. 두 극단을 다 봐야 하한과 상한이 나온다.

**6개월 경로는 안 돈다** — `prereg-14` (나)가 계단식(M3)을 보려고 6개월을 골랐는데,
`0023` 뒤로 계단식은 3문구뿐이고 이번 물음(목록·낱말)과 무관하다.

## §3 예측 — 재기 전에 적는다

```
P1  (가) L1 = 예 · L2 = 은행 1곳 이상 · "모름" 아님
    근거 — 자기가 거래하는 은행은 이름으로 안다. 0045 반증 조건 1(공시 원문 이름)은
    F5 가 법인 형태를 떼서 닫았다 (농협은행주식회사 → 농협은행)

P2  (나) L2 = **"거래 없음"** 이다 — "모름" 이 아니다
    근거 — 저축은행 계좌가 없는 사람은 없다는 것을 안다. "모름" 은 거래 여부 자체를
    모를 때의 답이고, 그 상태는 드물다고 본다. **이게 틀리면 F7 이 바로 열린다**
    위험 — 웹에서 "거래 없음" 은 **아무것도 안 고르고 제출**하는 것이라, 사람이 그걸
    답으로 인식하지 못하고 **모르겠습니다** 를 누를 수 있다. 그러면 P2 는 틀리지만
    원인은 F7(질문 순서)이 아니라 **화면 형태**다. L2 기록에 "왜 그 버튼을 눌렀나"
    한 줄을 같이 받아 둘을 가른다

P3  (가) 질문 수가 **10~20개** 안이다 (목록 질문 포함)
    근거 — prereg-15 P5 실측. 거래 0·1·2·3곳이 10·15·16·20개였다. 어느 은행이냐에
    따라 ±2 흔들린다

P4  (가) M1a = 0 — 2런에서 0이 됐고 F6 이 질문 문장에 은행 이름을 넣었다
    (`view.check_model` 이 그것을 검사한다 · prereg-15 §6)

P5  (가) M1b ≤ 1 — 2런의 2건(탑스=제주은행 · 소중한 날=국민은행)은 이제 **그 은행을
    골랐을 때만** 질문으로 나온다. 안 고르면 유도로 "아니오" 가 되어 묻지 않는다.
    사람의 거래 은행에 제주은행이 없다고 보고 1 이하로 쓴다. 국민은행이 있으면 1 이다

P6  M3 = 0 — 12개월에는 계단식 문구가 없고, 첫거래·주거래가 배타라 한쪽만 묻는다(0045)

P7  W1 = 0 — 라벨 아래 공시 문구가 그대로 붙어 있어 대조가 된다.
    **1건이라도 나오면** 0046 반증 조건 3 이 발동한다 → 라벨을 빼고 문구만 보여주는
    쪽으로 돌린다

P8  M4 = 완주 · (가) 180초 이하
    근거 — 8/31 재측정이 7개에 44초였다. 20개면 130초 안쪽이고 목록 질문에 시간이
    더 든다 (L3 로 따로 잰다)

P9  (나) 질문 수가 (가)보다 **적다** — "거래 없음" 이면 은행 상대 조건이 전부 유도된다.
    prereg-15 §6 이 저축은행 9~15개라 했다
```

## §4 이 측정이 못 하는 것 — 미리 적는다

```
표본 1명 · 만든 사람      M1·M2 과소 추정 (prereg-14 §4). 하한으로 읽는다
"모름" 의 빈도는 n=1      P2 가 맞아도 "모름 경로는 드물다" 라고 **말할 수 없다.**
                       말할 수 있는 것은 "저축은행 계좌가 없는 사람 하나는 모름을
                       안 골랐다" 까지다. F7 을 ⏸ 에 두는 근거는 되지만 닫는 근거는
                       못 된다
W1 은 자기보고           라벨만 보고 답했는지를 스스로 판정한다. 그래서 "답을 바꿨나" 라는
                       행동으로 정의했다
학습 효과               (가)를 먼저 돌면 (나)에서 낱말을 안다. M2 는 (가)에서만 센다
```

## §5 이 결과로 무엇을 하나

```
P2 가 틀리면 (모름을 골랐다)   이유를 먼저 가른다.
                             · 거래 여부를 정말 몰라서 → F7 을 ▶ 로 올리고 이슈를 판다.
                               에픽 #18. 질문 순서(0018 커버리지 순)를 기관별 질문에
                               맞게 다시 보는 것이 후보다 (prereg-15 §5 P6 조항)
                             · "거래 없음" 이 답인 줄 몰라서 → 화면 형태 문제다. 목록
                               질문에 "거래해 본 은행이 없습니다" 버튼을 따로 낼지
                               사람이 정한다. F7 은 ⏸ 그대로
P2 가 맞으면                  F7 은 ⏸ 그대로. 로드맵에 "3런 n=1 에서 모름 0건" 을 적는다.
                             닫지 않는다 (§4)
P1 이 틀리면 (은행을 못 찾음)  0045 반증 조건 1. 못 찾은 이름을 적고 F5 표(ORG_ALIAS)에
                             넣을지 사람이 정한다 — 브랜드 개칭은 공시에 없는 이름이라
                             0046 이 비워 둔 자리다
P5 가 틀리면 (M1b ≥ 2)        공시에 뜻이 없는 낱말이 더 있다. G(검색)의 수요 사례에
                             더한다. 선행은 코퍼스 조사다 (A2 가 아니다 · 2026-09-02)
P7 이 틀리면 (W1 ≥ 1)         0046 반증 조건 3 발동. 그 라벨은 빼고 문구만 낸다
P3·P9 가 틀리면              measure_per_institution.py 의 페르소나와 실제 사람의 차이를
                             적는다. 도구가 잰 숫자가 사람 경로를 대표하지 않는 것이다
```

**관측만 하고 손대지 않는 것** — 같은 상품이 두 줄로 나오는 자리(WON적금 2·3위 · 단리·복리
행). 눈에 걸리면 어느 화면 몇 위였는지만 적는다. 정렬·집계가 걸려 지표가 움직이므로
고치려면 사전등록이 따로 필요하다.

**무엇을 고칠지는 위에 있고, 얼마나 고칠지는 재고 나서 정한다.**

---

# §6 결과 (재고 나서 채운다)

```
세션 (가) 은행권 12개월 · 2026-09-03 · 웹 · 정렬 최대순
  L1 · L2 · L3 : 예(바로 찾았다) · 3곳(농협·토스·하나) · "금방"(초 단위 기록 없음)
                 **3곳은 안내문을 잘못 읽은 값이다** — "셋을 따로 적는다" 를 "셋을
                 고른다" 로 읽었다. 실제 거래 은행 수는 더 많을 수 있고, 모른다
  질문 수      : 18개 (목록 1 + 조건 17) · 예측 P3 10~20 안 · prereg-15 P5 의 3곳=20 과 -2
  M1a · M1b · M1c : 0 · 1(소중한 날 · 국민은행 · 14번 `기타`) · 0
  M3 · M4      : 0 · 완주 · 60~120초 (시계로 안 잼 · 자기보고)
  W1 · W2      : 0 · 1 (`기타` → "그 밖의 조건" 이 14번에 나왔다)
  M2 (부 지표)  : 1 (소중한 날 — M1b 와 같은 것)
  최종 화면     : 확정 63/67 · 1위 토스뱅크 아이 적금 4.23% 확정 (세후)
  재현          : state_json 의 키 순서로 다시 돌려 18개 질문의 키가 전부 일치했다.
                 웹은 로그를 안 쓰므로(0040) 사람이 마지막 화면의 hidden 값을 붙여 줬다

세션 (나) 저축은행 12개월 · 2026-09-03 · 웹 · 정렬 최대순 · 스냅샷 20260825(손으로 고침)
  L1 · L2 · L3 : 해당 없음(거래 은행 없음) · **"모름"** · 시간 기록 없음
                 이유(자기보고) — "해당 은행의 계좌나 상품 가입을 안 해서 모름을 두었다"
                 → 거래가 없다는 것을 **알면서** 모름을 눌렀다. 빈 제출이 "거래 없음"
                 답이라는 것을 답으로 인식하지 못했다 (§3 P2 가 미리 적은 위험)
  질문 수      : **48개** (목록 1 + 조건 47) · 그중 원문 재질문(#) 13개
  M1a · M1b · M1c : 0 · 0 · (자기보고 없음)
                 대신 다른 실패가 났다 — 아래 관측 2·5
  M3 · M4      : **2 이상** ("같은 질문 두 번" · 4→5번 하나저축은행 카드 · 6→7번 오픈뱅킹)
                 · 완주 · **5분 이상** (자기보고)
  W1 · W2      : 0 · 1 (`기타` → "그 밖의 조건" · 31번 · 문구 "올해의 띠")
  최종 화면     : 확정 97/106 · 1위 WELCOME 아이사랑 정기적금 4.23~6.77% (범위)
  예산 12개 시점 : 13번째 질문에서 확정 76/106 (71.7%) — 전역 유형 질문이 앞에 와서
                 도구의 "모름" 페르소나(0045 · 은행권 20.9%)와 모양이 다르다.
                 페르소나와 사람이 갈린 자리다 — §5 P3·P9 조항
  재현          : state_json 키 순서로 48개 전부 일치
```

**예측 판정**

```
(가)  P1 맞음 · P3 맞음 · P4 맞음 · P5 맞음(1) · P6 맞음 · P7 맞음 · P8 맞음
(나)  P2 **틀림** — "모름" 을 골랐다. 원인은 §3 이 미리 가른 둘 중 **화면 형태**다.
          거래가 없다는 것을 알면서도 빈 제출을 답으로 인식하지 못했다
      P9 **틀림** — 48개 > (가) 18개. "모름" 경로라 유도가 하나도 안 걸렸다.
          P9 의 전제("거래 없음이면 유도된다")가 P2 와 함께 무너졌다
      P6 **틀림** — M3 ≥ 2. 12개월에도 원문 재질문이 같은 질문으로 느껴진다
      P7 맞음 (W1 = 0)
```

**9개 중 (가) 7 맞음 · (나) P2·P6·P9 틀림.** 틀린 셋이 전부 저축은행에서 났다.

## (나-2) — 같은 사람이 목록 답만 바꿔 다시 돌았다 (재기 전 예측 셋 · 같은 날)

(나)가 끝난 뒤 사람이 *"없으면 안 누르고 고르는 거구나"* 라고 했고 다시 돌기를 원했다.
**(나)의 기록은 지우지 않는다** — P2 의 위험으로 미리 적어 둔 것이 그대로 일어난 것이고,
그 자체가 화면 형태의 관측이다. (나)와 (나-2)는 반복이 아니라 **목록 답만 다른 두 경로**다.

```
재기 전 예측   Q1 질문 수 9~15개 (prereg-15 §6 저축은행 실측) · Q2 2분 안쪽 ·
             Q3 은행 상대 질문(@ 붙은 것 중 # 없는 것)이 0개 — 전부 유도된다

세션 (나-2) 저축은행 12개월 · 목록 = **빈 제출("거래 없음")**
  질문 수      : **15개** (목록 1 + 유형 8 + 원문 재질문 6) · Q1 맞음(경계값)
  Q3           : **맞음** — @ 만 붙은 질문 0개. 나온 것은 전부 원문 재질문(#)이다
  M4           : 완주 · **3분 언저리**(자기보고) · Q2(2분 안쪽) **틀림** — 질문이 15개로
                 줄었는데도 (가) 18개(1~2분)보다 오래 걸렸다. 저축은행 문구가 더 길고
                 낯설다는 뜻으로 읽되, 시계로 잰 값이 아니라 근거로는 안 쓴다
  M3           : **1 이상** — "KB저축은행 질문이 똑같이 두 번" (4→5번)
  W2           : 1 (`기타` · "올해의 띠")
  최종 화면     : 확정 96/106 · 1위 애큐온 청년플랜적금 5.92% 확정
  재현          : state_json 키 순서로 15개 전부 일치

(나) 대 (나-2) — 같은 사람 · 같은 권역 · 목록 답만 다르다
  질문 수   48 → 15        시간   5분 이상 → 3분 언저리        확정  97 → 96 / 106
  **목록 질문의 답 하나가 질문 33개를 가른다.** 확정 수는 거의 같다 — "모름" 경로에서
  더 물은 33개가 확정을 1개 더 얻었다
```

**목록 질문에 대한 사람의 말** — *"이지선다라 모르겠습니다 · 없습니다 같은 선택지 같이
느껴졌어."* 버튼이 둘("고른 대로 계속" · "모르겠습니다")이고 "없습니다" 가 버튼이 아니라
**안내문**이라, 거래가 없는 사람에게는 고를 수 있는 것이 "모르겠습니다" 하나로 보인다.
P2 가 틀린 원인이 이 문장으로 확정된다.

**(나-2)에서 새로 걸린 것 — 문구가 하나뿐인 유형 질문은 정보가 없다.** 4번 유형 질문
"오픈뱅킹에 다른 은행 계좌 등록"의 근거 문구가 KB저축은행 것 **하나**였고, 5번 원문
재질문이 **같은 문구 하나**를 다시 물었다. 두 화면이 글자까지 같다. 유형 아래 문구가
하나이고 그것이 임계 문구이면 유형 질문은 건너뛰고 원문 재질문만 내야 한다. 이건 규칙
하나로 고칠 수 있고 질문 수가 줄어드는 방향이라 `0026`(질문 수 불가침)과 충돌하지 않는다
— 없어지는 것이 **정보 없는 질문**이기 때문이다. 얼마나 흔한지는 셀 수 있다(유형 질문 중
문구 1개 · 그 문구가 임계인 것의 수). 사전등록 뒤 고친다.

**사람이 고쳐야 한다고 느낀 것** — "나이·지역 같은 자격"의 토글. 나이·지역 말고 전기차·
수소차 같은 문구가 같이 있다. 관측 2 와 같은 자리다.

## §5 판정 조항 적용

```
P2 틀림 · 원인 = 화면 형태   → §5 두 번째 가지. 목록 질문에 "거래해 본 은행이 없습니다"
                            버튼을 따로 낼지 **사람이 정한다.**
                            F7 은 규칙대로면 ⏸ 그대로다. 다만 착수 조건("흔한가")에
                            새 정보가 있다 — 화면 형태를 고쳐도 **정말 모르는 사람은
                            이 경로를 걷고**, 그 경로가 48개·5분이라는 것을 사람이
                            실제로 확인했다. ▶ 로 올릴지는 사람이 정한다
P6 틀림                     원문 재질문(#)이 유형 질문의 반복으로 느껴진다. prereg-14
                            §3 P3 이 계단식에서 예측한 모양이 **임계 문구 전반**에서
                            난다. 안내 문구("위는 공시 문구 원문입니다")는 안 읽혔다
                            → 후보를 관측 3 에 적었다
P9 틀림                     measure_per_institution.py 의 페르소나가 사람 경로를 대표하지
                            않는다. 도구의 "모름" 페르소나는 전역 질문도 모름으로 답하는데
                            사람은 전역 질문에는 예/아니오로 답했다
```

## (나)에서 사전등록에 없던 관측 — 여섯

**1. 시작 폼이 저축은행에서 깨진다.** 스냅샷 기본값이 권역과 무관하게 `20260826` 인데
저축은행 추출은 `20260825` 하나뿐이다. *"추출 결과가 없다"* 로 첫 화면에서 막혔고, 사람이
날짜를 알아서 고쳐 써야 돌아갔다. 스냅샷은 사용자가 고를 값이 아니다 — **권역별로 서버가
최신 것을 고르고** 화면에는 "공시 기준 2026-08-25" 로 날짜만 보이면 된다.

**2. 유형 하나에 답할 수 없는 조건이 묶여 있다.** 3번 "나이·지역 같은 자격" 아래 동반가입
인원 · 60세 이상 · KB스타클럽 등급 · 결혼·임신·출산 · 자녀 수 · 장애인 등록 · 신용평점이
함께 있다. 사람은 *"하나로 묶어도 되느냐"* 고 물었고, 유형에 **예**라고 답한 뒤 원문 재질문
6개(25~30번)에 **전부 아니오**라고 답했다 — 유형 답이 사실이 아니었다는 뜻이다.
`0027`("유형을 먼저 묻고 문구는 뒤에")의 대가가 저축은행에서 은행권보다 훨씬 크다. 유형
17종은 은행권 표본으로 만들어졌다(`0021`). 하나저축은행·KB저축은행 문구도 같은 문제라고
사람이 보고했다.

**3. 토글의 문구가 뒤에 올 질문을 미리 보여준다 → 두 번 묻는 것으로 읽힌다.** 유형 질문의
근거 문구가 그대로 원문 재질문(#)으로 온다. 이번 세션에서 13번. 후보 — (ㄱ) 유형 질문의
토글에서 임계 문구를 빼거나 따로 표시한다, (ㄴ) 재질문 화면이 *"아까 예라고 하셨는데, 이
문구는 금액·횟수 기준이 있어 따로 묻습니다"* 로 시작한다. 질문 수는 안 바뀌고 문구만
바뀌므로 화면 계약 안에서 고칠 수 있다.

**4. 질문의 시제가 틀린 조건이 많다.** 7번 *"신규가입일 포함 14일 이내 타기관 공인인증서
미등록시 우대금리 적용해제"* 는 지금 상태가 아니라 **가입 뒤에 할 일**이다. 화면은 모든
조건을 "충족하십니까"(현재)로 묻는다. 조건에는 두 종류가 있다 — 지금 사실인 것(나이 ·
거래 이력)과 앞으로 하겠다고 정하는 것(자동이체 · 인증서 등록 · 미션 · 잔돈적립). 후자는
"하실 건가요"가 맞다. 화면 문구를 넘어 되묻기 설계(`0016`·`0024`)의 전제를 건드린다.
**사람이 정할 결정**이다 — 두 종류를 가르는가, 갈면 어디서 가르는가(추출 스키마에 시제를
넣나 · 유형별로 정하나).

**5. 거래가 없는 은행의 실적을 묻는다.** "모름" 경로라 은행 상대 질문 47개가 왔고, 사람은
*"계좌도 없는 은행 상품을 다시 묻는 환경이 이상했다"* 고 했다. 답도 흔들렸다 —
민국저축은행 주거래에 **예**(21번), 애큐온 첫거래에 **아니오**(16번). 거래가 없는 은행이면
첫거래는 예 · 주거래는 아니오가 사실이다. **관측 5 와 P2 는 하나의 원인이다** — 목록
질문의 "거래 없음" 이 답으로 보이지 않았다. 그 하나가 47개 질문과 흔들린 답을 낳았다.

**6. 같은 상품이 넷까지 나온다.** 최종 화면 4~7위가 디비저축은행 DB행복씨앗적금 ·
M-DB행복씨앗적금 × 복리·단리, 금리는 넷 다 5.076% 다. (가)의 수협 두 줄과 같은 자리이고
저축은행에서 더 크다. 관측만 적는다.

## (가)에서 사전등록에 없던 관측 — 셋

**1. 안 고른 은행의 문구가 질문으로 나왔다 — `기타` 가 유도를 빠져나간다.**
14번 질문 "그 밖의 조건" 의 문구는 *"소중한 날 · 국민은행"* 하나뿐이었고, 사람은
국민은행을 목록에서 고르지 않았다. `기타` 는 은행 상대 유형이 아니라 **전역**으로 묻기
때문에 목록 답이 유도하지 못한다. P5 의 논리(*"안 고른 은행 문구는 안 나온다"*)는 은행
상대 유형 15종에서만 성립하고 `기타` 에서는 깨진다. **F6(`0045`)의 빈틈**이고, 이번 M1b
1건이 정확히 그 자리에서 났다. 고치는 방향은 재고 나서 정한다 — `기타` 문구에 기관이
붙어 있으므로 목록 답으로 유도할 수 있는지, 아니면 `기타` 는 조건 유형이 아니어서 유도가
맞지 않는지 따져야 한다. 손대면 질문 수·확정률이 움직이므로 **사전등록이 필요하다.**

**2. 그 라벨이 `0046` 이 가장 위험하다고 지목한 칸이었다** (W2 = 1). 사람은 "모름" 을
답했고 라벨 때문이 아니라 **문구 자체에 뜻이 없어서**였다. 라벨이 뜻을 바꾼 것(W1)은 아니다.

**3. 같은 이름의 상품이 두 줄이다.** 최종 화면 5위·7위가 수협은행 *"Sh해양플라스틱Zero!적금"*
자유적립식·정액적립식이다(3.51% · 3.38%). START-HERE 가 적어 둔 WON적금 자리와 같은
모양이다. 이번엔 관측만 적는다 — 정렬·집계가 걸린다.

**측정 방법에서 걸린 것 하나.** 웹은 답을 서버에 남기지 않으므로(`0040` 무상태) 3런 기록은
사람이 마지막 화면의 `state_json` 을 붙여 준 것이다. 화면을 닫으면 회수가 안 된다.
(나)도 같은 방식으로 간다. 웹에 로그를 남기게 고치려면 `0040` 에 예외를 적어야 한다.

**시작 폼에서 걸린 것 하나** (이번 경로가 지나가지 않는 칸 · 사람이 보고했다). 선호
목록형 축(`처음기관`)이 **가운뎃점**을 구분자로 받는다(예: 우리·농협). 바로 위 "은행
좁히기" 칸은 쉼표다. 원인은 CLI `--prefs` 가 쉼표를 축 구분자로 쓰는 제약이 웹 폼까지
올라온 것(`prefs.LIST_SEP`). 모바일·일반 키보드에서 가운뎃점은 치기 어렵다. 후보는
웹 칸에서 쉼표·공백도 받아 가운뎃점으로 바꿔 넘기는 것(표는 한 곳 그대로 · `0030`
위반 없음)과 체크박스로 바꾸는 것이다. 3런 뒤에 정한다.
