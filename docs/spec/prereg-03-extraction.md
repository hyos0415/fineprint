# 사전 등록 03 — 추출기 비교: 규칙 파서 vs 제한 스키마 LLM

> 등록일 2026-08-25 · 상태: **설계만 있음 (측정 전)** · 이슈 #4
>
> 파일럿에서 병목이 **추출**로 확정됐다(`../decisions/0004-pilot-passed-extraction-is-the-bottleneck.md`).
> 이 문서는 두 추출기를 비교하기 **전에** 지표·임계·홀드아웃을 고정한다.

## 0. 답하는 질문 하나

> **자유서술 공시에서 조건을 뽑을 때, 출력 형식을 닫힌 열거값으로 제한하면
> 규칙 파서보다 나은가?**

"나은가"의 기준을 아래에서 숫자로 고정한다.

## 1. 비교 대상

| | 추출기 | 무엇 |
|---|---|---|
| **A** | 규칙 파서 | `src/analysis/finlife_rules.py`. 현재 은행권 닫힘률 **62.8%**(159/253) |
| **B** | 제한 스키마 LLM | 조건 유형을 16종 열거값에서 고르게 하고 스키마로 출력 강제 (이슈 #5) |

**둘 다 금리를 보지 못한다.** 입력은 `spcl_cnd` 텍스트와 가입기간뿐이다. 기본금리·최고금리는
채점기만 갖는다 — 추출기가 목표값을 보면 거기에 맞춰 값을 만들어낼 수 있다.

### 1.1 조건 유형 열거값 16종 — **확정** (2026-08-25)

**무엇을 정한 것인가.** LLM 추출기가 조건 하나를 뽑을 때 `condition_type`에 넣을 수 있는
값을 아래 16개 + `기타`로 못 박는다. 자유 서술을 금지하는 게 핵심이다 —
`자동이체실적` / `②공과금자동이체` / `자동이체우대금리연` 24종 문구가 한 값으로 수렴한다.

**왜 유형 축이 두 층인가.** 이 프로젝트의 주 타깃은 "확인 불가 판정"이다
(`../decisions/0004-...md`). 사용자 상태로 O/X를 답할 수 있는 조건과, 공시 문구만으로는
누구도 판정할 수 없는 조건(랜덤 지급·특판·미션)을 **유형 단계에서 갈라두면** 추출 결과가
그대로 판정 신호가 된다. 별도 규칙을 덧붙이지 않아도 된다.

빈도는 은행권(개발 집합) **조건 있는 상품 81건** 중 그 유형이 등장한 상품 수다.
한 상품에 여러 유형이 있으므로 합은 81을 넘는다.

**이 숫자는 키워드 정규식으로 센 근사값이다.** gold 라벨이 아니고, 열거값을 몇 개로
자를지 가늠하는 데만 썼다. 추출 성능 지표로 쓰지 않는다.

**층 1 — 사용자 상태로 O/X 판정 가능 (12종)**

| # | 열거값 | 무엇이 여기 오나 | 빈도 |
|---:|---|---|---:|
| 1 | `타상품_보유동시가입` | 주택청약·입출금통장·펀드·수익증권 보유, 짝 상품 동시가입, 교차거래 | 25 (30.9%) |
| 2 | `자동이체` | 이 상품으로의 자동이체 납입, 공과금·지로·아파트관리비·통신비 자동이체 | 23 (28.4%) |
| 3 | `첫거래_신규고객` | 최초거래, 최근 N개월 예적금 미보유·신규해지 이력 없음, 첫예금거래 | 21 (25.9%) |
| 4 | `카드실적` | 신용·체크카드 결제·이용실적, 카드 신규 발급 | 20 (24.7%) |
| 5 | `마케팅_정보동의` | 마케팅 동의, 개인(신용)정보 수집·이용 동의, 수신동의, 혜택알림 동의 | 21 (25.9%) |
| 6 | `비대면_채널가입` | 이 상품을 인터넷·모바일·비대면·디지털 채널로 가입 | 16 (19.8%) |
| 7 | `목표달성_납입실적` | 목표금액 달성, 입금 횟수·연속 성공, 저금 성공 일수, 별 모으기 | 13 (16.0%) |
| 8 | `급여_연금이체` | 급여이체·소득이체·연금 입금·가맹점대금 입금 | 12 (14.8%) |
| 9 | `주거래_장기거래_재예치` | 주거래 우대, 거래기간 N년 이상, 장기거래·재예치·자동재예치, 회전 | 12 (14.8%) |
| 10 | `잔액_평잔_가입금액` | 총수신·요구불 평잔, 가입(재예치)금액 N원 이상, 월부금 N원 이상 | 12 (14.8%) |
| 11 | `고객군_자격` | 연령(MZ·만65세 이상), 다자녀, 중소기업 근로자, VIP, 단체가입, 비과세종합저축 대상 | 10 (12.3%) |
| 12 | `오픈뱅킹_타행계좌등록` | 오픈뱅킹 서비스 가입, 타행 계좌 등록 | 6 (7.4%) |

**층 2 — 공시 문구만으로 판정 불가 (4종)**

| # | 열거값 | 무엇이 여기 오나 | 빈도 |
|---:|---|---|---:|
| 13 | `실천_미션_인증` | ESG 서약, 대중교통 이용, 퀴즈·방문 인증, 봉사활동·상품홍보, 인증 서비스 등록, 종이통장·통장미발급 미션 | 13 (16.0%) |
| 14 | `쿠폰_코드_추천인` | 금리우대쿠폰 등록, 금리쿠폰·우대 코드 입력, 추천인 | 8 (9.9%) |
| 15 | `무조건_특판_이벤트` | 특판·이벤트 우대이율, "가입고객 모두에게 적용" 특별금리, 만기해지 요건만 있는 항목 | 10 (12.3%) |
| 16 | `판정불가_불특정` | 랜덤 지급, 조건 서술 없이 "우대금리 적용"만, "상품설명서 참조" | 3 (3.7%) |

**17번째 값 `기타`.** 위 16개에 넣기 어려운 조건은 `기타`로 보낸다. 탈출구를 없애면
모델이 분류할 수 없는 항목을 **버리게** 되고, 그건 §4가 막으려는 "항목 0개" 방향이다.
대신 **`기타` 비율을 부 지표로 함께 보고한다**(§4).

#### 경계 규칙 — 헷갈리는 자리를 미리 정한다

두 유형에 걸치는 문구가 실제로 있다. 규칙을 측정 전에 고정하지 않으면 프롬프트를
고칠 때마다 기준이 흔들린다.

| 헷갈리는 문구 | 어디로 | 왜 |
|---|---|---|
| "자동이체로 6회 이상 납입" | `자동이체` | 수단(자동이체 등록)을 요구하면 1번. 횟수는 `threshold`에 담는다 |
| "누적 10회 입금 시" (수단 무관) | `목표달성_납입실적` | 등록해야 할 수단이 없다 |
| "적립식예금 잔액 10만원 이상 보유" | `타상품_보유동시가입` | **다른 상품을 갖는 것**이 핵심. 금액은 `threshold` |
| "이 상품 가입금액 2천만원 이상" | `잔액_평잔_가입금액` | **이 상품의 금액 수준**이 핵심 |
| "급여/연금 이체" | `급여_연금이체` | 자동이체의 하위가 아니라 별도 유형. 실측에서 12건이 독립적으로 쓰였다 |
| "재예치고객 우대" | `주거래_장기거래_재예치` | 첫거래의 반대 방향이다. 3번과 섞지 않는다 |
| "이벤트금리(비대면금리)" | `비대면_채널가입` | 채널 조건이 **명시되면** 6번. 조건 없는 특판이면 15번 |
| "만기 해지 시 연 2.50% 제공" (토스뱅크 굴비 적금) | `무조건_특판_이벤트` | 사용자 상태와 무관하다. 만기까지 보유하면 누구나 받는다 |
| "스마트폰뱅킹의 상품알리기 0.80%" | `실천_미션_인증` | 채널 가입이 아니라 **홍보 행위**를 요구한다 |
| "최근 1년간 예적금 미보유" | `첫거래_신규고객`, `polarity: required` | 유형 자체가 "이력 없음"을 뜻한다 |
| "가입 이전 6개월 카드 사용실적이 있으면 제외" | `카드실적`, `polarity: must_not_have` | `must_not_have`는 이런 **부정 조건**에만 쓴다 |

**절차 요건은 유형이 아니다.** "만기시 제공" · "신규시 제공" · "만기일 전일까지 유지"는
19건에 나오지만, 조건의 **종류**가 아니라 지급 시점·유지 요건이다. 유형 값으로 두지 않고
항목의 수식어로 취급한다. 단 항목 내용이 그것뿐이면 위 표대로 15번으로 보낸다.

#### 이 목록의 알려진 흠 — 감추지 않고 적는다

- **13번은 순수하지 않다.** `통장미발급`(3건)은 사용자가 O/X로 답할 수 있는 조건인데
  종이 절감 미션과 함께 13번에 넣었다. 층 구분은 **대략적인 축**이고, 판정 단계에서
  13번을 일괄 "판정 불가"로 처리하면 이 3건을 놓친다. 이슈 #6에 남긴다
- **`기타`가 얼마나 나올지는 모른다.** 개발 집합에서는 22개 후보를 16개로 접었으므로
  낮게 나오겠지만, 홀드아웃(저축은행)에서 높을 수 있다
- **열거값은 개발 집합만 보고 만들었다.** 홀드아웃 조건문 본문은 읽지 않았다(§2.1).
  홀드아웃에서 `기타`가 많이 나오면 **그건 결과로 보고하고 열거값을 고치지 않는다** —
  고치면 규칙 파서에 금지한 것과 같은 이유로 홀드아웃 자격을 잃는다

## 2. 홀드아웃 ★ 가장 중요한 설계

**규칙 파서는 은행권 253행을 여덟 라운드 보면서 만들어졌다.** 같은 집합에서 비교하면
과적합된 baseline과 겨루는 것이고, 그 숫자는 쓸 수 없다.

```
개발 집합 (규칙이 이미 본 데이터)   은행권      예금 38 + 적금 59  = 상품 97 · 옵션 253행
홀드아웃 (아무도 안 본 데이터)      저축은행    예금 391 + 적금 277 = 상품 668
```

저축은행은 **같은 공시 체계·같은 상품 유형인데 기관이 전혀 다르다** — 표기 습관이 다를 것이
기대되고, 그게 홀드아웃의 요건이다. 규모도 은행권의 7배라 검정력에 유리하다.

**판정은 홀드아웃에서만 한다.** 개발 집합 숫자는 참고로만 보고한다.

- 홀드아웃 스냅샷은 비교 **직전에 한 번** 수집하고 그 ID를 고정한다
- 규칙 파서를 홀드아웃을 보고 고치지 않는다. 고치면 홀드아웃 자격을 잃는다
  (고쳐야 할 결함을 발견하면 기록만 하고 이슈 #6으로 넘긴다)

### 2.1 수집한 스냅샷 — `20260825` (고정)

수집 완료: `python src/ingest/fetch_finlife.py {deposit|saving} --group savingsbank`.
원본은 커밋하지 않는다(`data/raw/{deposit,saving}_savingsbank_20260825.json`).

| | 은행권(개발, `20260824`) | 저축은행(홀드아웃, `20260825`) |
|---|---:|---:|
| 상품 | 97 | **668** (예금 391 + 적금 277) |
| 취급 기관 | 18 | **79** |
| 옵션 행(기본·최고금리 양쪽 존재) | 333 | 3,998 |
| 조건없음 행 | 80 (24.0%) | 2,908 (72.7%) |
| **우대조건 있는 행 = 주 지표 분모** | **253** | **1,090** |
| 서로 다른 조건문 | 79 | **75** |
| (조건문, 가입기간) 쌍 | 234 | **228** |
| 조건문 길이 중위 | 116자 | 51자 |

§2에서 예측한 상품 668건은 그대로 맞았다. 그런데 **행 수를 상품 수의 대리로 쓴 것이
틀렸다** — 저축은행은 상품당 옵션(가입기간) 행이 훨씬 많아 조건 있는 행이 1,090이다.
아래 두 가지가 여기서 따라온다.

**(가) 검정력은 §5 표보다 좋다.** 1,090행이면 닫힘률 차이의 95% 신뢰구간 폭이
±5.2%p가 아니라 **약 ±4.2%p**다. 5%p 임계는 그 경계보다 넓으므로 **그대로 둔다**
(임계를 좁히면 측정 전 기준 변경이 되고, 넓은 쪽이 보수적이다).

**(나) 행은 서로 독립이 아니다 — 이건 결정이 필요하다.** 조건 있는 1,090행이
서로 다른 조건문 **75종**에서 나온다. 한 조건문이 평균 14.5행에 걸쳐 있으므로,
McNemar를 1,090행에 그대로 돌리면 같은 텍스트를 열네 번 세는 것이고 **p값이 실제보다
작게 나온다**(우위를 과대 판정하는 방향). 개발 집합의 62.8%도 같은 성질을 갖는다
(253행 / 79 조건문).

분석 단위를 측정 **전에** 고정해야 한다. 후보:

| 후보 | 분모 | 장점 | 단점 |
|---|---:|---|---|
| ① 행 단위 | 1,090 | 개발 집합 62.8%와 같은 단위 | p값이 낙관적. 기관 편중에 취약 |
| ② (조건문, 기간) 쌍 단위 | 228 | LLM 호출 단위와 일치. 중복 계산 없음 | 표본이 작아 CI ±6.5%p |
| ③ 조건문 단위 (기간은 12개월 고정) | 75 | 완전 독립 | 표본이 너무 작다 |
| **④ 세 숫자를 각각 다른 단위로** | — | **채택** | 구현이 늘어난다 |

**④로 확정한다 (2026-08-25).** 한 단위로 통일하지 않는 이유는, 세 숫자가 답하는 질문이
서로 다르기 때문이다.

| 무엇 | 단위 | 왜 이 단위인가 |
|---|---|---|
| **닫힘률** (주 지표) | **행** n=1,090 | 개발 집합 62.8%(253행)와 **같은 단위**여야 비교가 된다 |
| **McNemar 유의검정** | **(조건문, 기간) 쌍** n=228 | LLM 호출 단위와 일치하고 같은 텍스트를 중복해 세지 않는다 |
| **95% 신뢰구간** | **조건문 75종 클러스터 부트스트랩** | 실질 표본은 1,090이 아니라 75다. 그걸 숨기지 않는다 |
| 호출 비용·시간 | (조건문, 기간) 쌍 n=228 | 실제로 호출하는 횟수 |

부트스트랩은 **조건문 75종을 복원추출**해 그 조건문에 딸린 행 전체를 함께 뽑는 방식이다
(1,000회 · `random.seed(20260825)` 고정 · 표준 라이브러리만). 행을 개별로 뽑으면 클러스터
구조를 무시해 CI가 다시 좁아진다.

**편중을 실측했다 — 그래서 부 지표를 하나 더 넣는다.** 조건문 75종의 행 수 분포가 고르지
않다.

```
행 수 상위 5종        172 · 121 · 94 · 68 · 58  →  합 513행 = 전체의 47.1%
중위 조건문의 행 수    4행
```

**한 조건문이 홀드아웃 행의 15.8%(172/1,090)를 혼자 차지한다.** 그 하나를 A는 닫고 B는
놓치면(또는 반대면) 행 단위 닫힘률이 15%p 흔들리고, 그건 추출 품질이 아니라 그 상품군의
행 수를 재는 것이다. 클러스터 부트스트랩이 CI 폭으로는 이걸 반영하지만 **점추정값은
그대로 흔들린다.**

따라서 **가중 닫힘률**을 부 지표로 함께 보고한다(§4). 조건문 75종에 각각 동일 가중을 주고
(각 조건문의 닫힘 비율을 평균) 낸 값이다. 두 값이 크게 다르면 결과가 큰 조건문 몇 개에
끌려간 것이다.

**측정 전에 정한 것이므로 이건 사후 조정이 아니다.** 편중은 추출기를 돌리지 않고 행 수만
세어 알 수 있는 값이다 — 어떤 추출 결과도 보지 않았다.

**예상되는 결과를 미리 적는다.** 클러스터 CI는 ±4.2%p보다 넓을 것이다. 넓어진 CI 때문에
5%p 임계에 못 닿으면 §5 판정표의 두 번째 줄("규칙 파서 유지")이 적용된다. **그것도 정직한
결과다** — 저축은행 공시 668상품이 서로 다른 조건문 75종뿐이라는 것 자체가 이 홀드아웃의
실질 크기이고, 그 사실을 검정력으로 위장하지 않는다.

**(다) 홀드아웃 조건문이 개발 집합보다 짧다**(중위 51자 vs 116자). §8 세 번째 반증
조건("홀드아웃이 더 정형적이라 난이도가 낮다")이 발동할 가능성을 미리 적어 둔다.
판정할 때 A의 홀드아웃 닫힘률이 개발 집합보다 +15%p 이상이면 그 조항을 적용한다.

**홀드아웃 본문은 아직 읽지 않았다.** 위 숫자는 행 수·길이·중복 같은 계수뿐이고,
조건 유형 열거값(§1 B)은 **개발 집합(은행권)의 실측 빈도만 보고** 정한다. 홀드아웃
텍스트를 보고 열거값을 설계하면 B가 홀드아웃에 맞춰지고, 규칙 파서에 적용한 것과
같은 이유로 홀드아웃 자격을 잃는다.

## 3. 주 지표 — 닫힘률

```
닫힘률 = (항목 합계(상한 적용)가 |최고금리 − 기본금리| 와 0.06%p 이내로 일치하는 행)
         ─────────────────────────────────────────────────────────────────
                          (우대조건이 있는 행)
```

**한계를 먼저 적는다.** 한 행만 보면 "추출 실패"와 "진짜 설명 불가"를 구분할 수 없다.
따라서 이것은 **절대 정확도가 아니라 두 추출기의 상대 비교 지표**다. 진짜 설명 불가
상품의 비율은 두 추출기에 동일하게 걸리므로 차이는 추출 품질의 차이로 읽는다.

## 4. 부 지표 — 회피를 막는다

닫힘률만 보면 **"아무것도 뽑지 않으면 불일치가 안 생긴다"** 방향이 유리해질 수 있다.
파일럿의 F7(과잉 회피)과 같은 구멍이고, 선행 프로젝트에서 `"UNSUPPORTED"` 상수 스텁이
1·2·3순위를 전부 이긴 것과 같은 구조다(`../reference/finance-verifier-findings.md` §0.4).

**항상 함께 보고한다.**

| 부 지표 | 왜 |
|---|---|
| **항목 0개 비율** | 추출을 포기한 행. 이게 상대보다 높으면 닫힘률 우위를 인정하지 않는다 |
| **`기타` 비율** (B만) | 열거값 16종이 실제 공시를 덮는지. 높으면 §1.1 목록이 홀드아웃에 맞지 않는다는 결과다 |
| 불일치 폭 분포 | 0.06~0.30 / 0.30~1.00 / 1.00~ 구간별. 실패의 성질을 보여준다 |
| 상한 인식률 | 은행권에서 cap 인식 여부가 닫힘률 15%p 차이를 만들었다 |
| **호출 비용·시간** | LLM 추출은 비용이 있다. 닫힘률 3%p 우위에 100배 비용이면 채택 근거가 약하다 |

**제약**: B의 **항목 0개 비율이 A보다 5%p 이상 높으면**, 닫힘률이 높아도 **우위로 인정하지
않는다.** 뽑기를 포기해서 얻은 점수이기 때문이다.

## 5. 판정 임계 (측정 전에 고정)

같은 행에 두 추출기를 돌리므로 **쌍체 비교**다. 불일치쌍(한쪽만 닫힌 행)으로 McNemar
정확검정을 한다.

**검정력 참고 — 불일치쌍 n_d 중 한쪽이 b건일 때 유의(p<0.05)해지는 최소 b**

| n_d | 최소 b | 비율 |
|---:|---:|---|
| 10 | 9 | 90% |
| 15 | 12 | 80% |
| 20 | 15 | 75% |
| 30 | 21 | 70% |
| 60 | 39 | 65% |

**닫힘률 차이의 95% 신뢰구간 폭** (홀드아웃 규모별)

| 행 수 | ± 폭 |
|---:|---|
| 253 | ±8.4%p |
| 400 | ±6.7%p |
| 668 | ±5.2%p |
| 1,090 (실제 홀드아웃 행 수 — §2.1) | ±4.2%p |

위 표는 행이 서로 독립일 때의 값이다. 실제로는 조건문 75종에 묶여 있으므로 **보고하는
CI는 §2.1의 클러스터 부트스트랩 값**이고, 이보다 넓다.

**판정표**

| 관측 | 판정 |
|---|---|
| B가 A보다 닫힘률 **+5%p 이상** 높고 McNemar **p < 0.05**, 그리고 §4 제약 통과 | **제한 스키마 채택.** "추출 시점 형식 제한"이 답이다 |
| 차이가 **±5%p 이내**거나 p ≥ 0.05 | **규칙 파서 유지.** 비용이 낮은 쪽을 택한다. 이슈 #6으로 규칙을 깎는다 |
| A가 B보다 **+5%p 이상** 높고 p < 0.05 | **제한 스키마 기각.** 규칙 파서를 본선으로 쓴다 |
| B의 항목 0개 비율이 A보다 5%p 이상 높음 | 닫힘률과 무관하게 **우위 불인정** |

5%p를 임계로 잡은 근거: 홀드아웃 668행에서 신뢰구간 폭이 ±5.2%p이므로, 그보다 작은 차이는
구별할 수 없다. 임계를 그 경계에 맞춘다.

**실측 후에도 5%p를 유지한다.** 실제 홀드아웃은 668행이 아니라 1,090행이고 폭이
±4.2%p로 좁아졌다(§2.1). 임계를 4%p로 좁히면 측정 전 기준을 유리한 쪽으로 고치는 것이
되므로 하지 않는다 — 넓은 임계는 B에게 불리하고, 그게 보수적인 방향이다.

## 6. 실행 조건 (고정)

- **LLM 추출기 모델**: `claude-haiku-4-5-20251001` (비용 대비. 상위 모델은 부수 실험)
- `temperature 0` · `max_tokens 4096` · 재시도 없음 · 문항당 1회
- 출력은 **structured outputs로 강제**한다. 자유 텍스트 파싱에 의존하지 않는다
- 스키마 위반은 **실패로 집계**한다(항목 0개와 구분해 별도 보고)
- 프롬프트·스키마 전문을 이 문서에 붙인 뒤 실행한다. 실행 후 수정하지 않는다
- 홀드아웃 스냅샷 ID를 모든 결과에 함께 기록한다 (공시는 4일 만에도 바뀐다)

### 6.1 프롬프트·스키마 전문 (실행 전 고정)

**정본은 `src/analysis/extract_llm.py`의 `SYSTEM_PROMPT` · `USER_TEMPLATE` · `SCHEMA`
상수다.** 아래는 그 사본이며, **B를 홀드아웃에 돌린 커밋 이전에 고정**했다. 실행 후
수정하지 않는다.

**호출 구조** — 규칙·열거값은 호출마다 같으므로 `system`에, 변하는 것(가입기간·조건문)만
`user`에 둔다. 금리(기본·최고)는 어느 쪽에도 들어가지 않는다.

**호출 단위는 (조건문, 가입기간) 쌍**이다. 같은 쌍이 여러 행에 걸쳐 있으면 결과를
재사용한다 — 홀드아웃 1,090행이 228쌍이므로 호출은 228번이다. 사전등록 §6의
"문항당 1회"는 이 쌍 단위를 뜻한다.

#### 출력 스키마 (structured outputs로 강제)

```json
{
  "type": "object",
  "properties": {
    "cap": {"type": ["number", "null"]},
    "items": {"type": "array", "items": {"type": "object", "properties": {
      "condition_type": {"type": "string", "enum": [
        "타상품_보유동시가입", "자동이체", "첫거래_신규고객", "카드실적",
        "마케팅_정보동의", "비대면_채널가입", "목표달성_납입실적", "급여_연금이체",
        "주거래_장기거래_재예치", "잔액_평잔_가입금액", "고객군_자격",
        "오픈뱅킹_타행계좌등록", "실천_미션_인증", "쿠폰_코드_추천인",
        "무조건_특판_이벤트", "판정불가_불특정", "기타"]},
      "rate": {"type": "number"},
      "polarity": {"type": "string", "enum": ["required", "must_not_have"]},
      "applies_to_term": {"type": "boolean"},
      "exclusive_group": {"type": ["string", "null"]},
      "evidence": {"type": "string"}
    }, "required": ["condition_type", "rate", "polarity", "applies_to_term",
                    "exclusive_group", "evidence"], "additionalProperties": false}}
  },
  "required": ["cap", "items"], "additionalProperties": false
}
```

**`threshold`(금액·횟수 임계)는 넣지 않았다.** 이슈 #5 초안에는 있었지만 닫힘률 산수에
쓰이지 않고, 필드를 늘리면 출력 토큰과 실패 면적만 커진다. 사용자 상태 판정에 필요해지는
것은 다음 실험(G4/G5)이므로 그때 넣는다.

#### 합계를 내는 방식 (A와 같은 순서)

```
① applies_to_term 이 false 인 항목을 버린다
② exclusive_group 으로 묶인 항목은 합하지 않고 그 그룹의 최댓값만 센다 ("중복 적용 불가")
③ 나머지를 더한다
④ cap 이 있으면 min(합계, cap)
```

②는 **B가 표현할 수 있고 A는 못 하는 것**이다. A는 `중복 적용 불가`를 감지만 하고
사람이 만든 override에 의존한다(`finlife_rules.py` 규칙 E).

#### 프롬프트 전문

````
[system]
한국 예금·적금 공시의 우대조건 문구에서 **우대금리 항목**을 뽑는다.

## 뽑는 규칙

1. **금리가 명시된 조건만** items에 넣는다. 금리를 알 수 없는 조건은 넣지 않는다.
2. **상한 줄은 항목이 아니다.** "최고우대금리 0.5%" · "우대이율(최대 0.90%p)" ·
   "최고 연 1.5%p" 같은 표기는 `cap`에 넣고 items에는 넣지 않는다.
3. **한 줄에 금리가 여럿이면 하나만 고른다.** 금액·기간 구간별로 값이 나뉘어 있으면
   (예: "300만원이상 0.1%, 500만원이상 0.2%") 그 줄의 **최댓값**을 쓴다.
4. **"각 연0.10%p" 형태**는 앞에 나온 금리 없는 조건들 각각에 그 금리를 준다.
   조건이 세 개면 항목 세 개가 된다.
5. **가입기간 차등**이 붙은 항목은 위에 주어진 가입기간에 해당하는지 판단해
   `applies_to_term`에 담는다. 기간 표기가 없으면 true다.
   ("6개월제 0.5%, 12개월제 0.9%"에서 가입기간이 12개월이면 앞은 false, 뒤는 true)
6. **"중복 적용 불가"**로 묶인 항목들에는 같은 `exclusive_group` id를 준다("g1", "g2"...).
   묶이지 않은 항목은 null이다.
7. `polarity`는 조건을 **갖춰야** 하면 "required", **없어야** 하면 "must_not_have"다.
   "카드 사용실적이 있으면 제외" 같은 부정 조건에만 must_not_have를 쓴다.
   "최근 1년간 예적금 미보유"는 유형 자체가 이력 없음을 뜻하므로 required다.
8. 조건문에 **없는 값을 만들지 않는다.** 금리는 적힌 숫자만 쓴다.

## condition_type — 아래 17개 중 하나만 고른다

  (§1.1의 층 1 12종 · 층 2 4종 · 기타 목록을 한 줄 설명과 함께 그대로 나열)

## 헷갈리는 자리

  (§1.1의 경계 규칙표 9행을 그대로 나열)

절차 요건("만기시 제공" · "신규시 제공" · "만기일 전일까지 유지")은 **유형이 아니다.**
지급 시점일 뿐이므로 유형으로 두지 않는다. 단 항목 내용이 그것뿐이면
`무조건_특판_이벤트`로 보낸다.

[user]
가입기간: {term}개월

조건문:
```
{text}
```
````

열거값 목록과 경계 규칙표는 §1.1과 **같은 내용**이라 여기서는 줄여 적었다. 두 곳이
어긋나면 `extract_llm.py`의 상수가 정본이다.

### 6.2 실행 전에 정한 것 두 가지 (측정 전 기록)

**(가) 쌍 단위 McNemar의 이진화 규칙.** §2.1에서 McNemar를 (조건문,기간) 쌍 단위로
하기로 했는데, **한 쌍이 여러 행에 걸치고 그 행들의 실제 금리폭이 다를 수 있다**는 것을
구현하면서 발견했다. 세어 보니

```
은행권(개발)      쌍 234 전부 gap 한 개        (이진화 규칙이 결과에 영향 없음)
저축은행(홀드아웃) 쌍 228 중 197(86.4%)은 gap 한 개
                 나머지 31쌍이 행 402개(36.9%)를 차지한다  ← 규칙이 결과를 바꾼다
```

**규칙: 쌍의 행 중 과반이 닫히면 그 쌍을 닫힘으로 본다. 동수는 닫히지 않음.**
A와 B에 똑같이 적용하므로 두 추출기 사이에 편향을 만들지 않는다.

**이진화를 피하는 검정을 하나 더 함께 보고한다** — 쌍별 "닫힌 행 비율"의 차에 대한
**Wilcoxon 부호순위검정**이다. 31개 이질 쌍을 0/1로 접지 않고 다룬다.
**두 검정의 판정이 갈리면 Wilcoxon을 쓴다** (이진화가 개입하지 않는 쪽이다).

**(나) prompt caching은 쓰지 않는다.** `system` 블록에 `cache_control`을 걸고 재봤지만
`cache_creation_input_tokens`가 계속 0이었다(beta 헤더를 붙이고 접두를 2.8k 토큰까지
늘려도 동일). 이 키·모델 경로에서 캐시가 걸리지 않는다. 228쌍 전체 비용이 ~$1.2 수준이라
파고들 값이 없다고 판단했고, **§4의 비용 지표는 캐시 없는 값으로 보고한다.**

### 6.3 검증한 것 (측정 전)

- **McNemar 정확검정 구현이 §5 검정력 표를 정확히 재현한다** — n_d = 10·15·20·30·60에서
  유의해지는 최소 b가 표의 9·12·15·20 → 21·39와 모두 일치했다
- **A 재현** — 새 코드 경로(`compare_extractors.py`)로 개발 집합을 다시 재서 닫힘률
  159/253 = **62.8%**, 항목 0개 **13.4%**를 얻었다. §7 표의 값과 같다
- **스키마 연기 테스트** — 개발 집합 4쌍으로 API가 스키마를 받아들이고 출력이 파싱되는지만
  확인했다(4/4 통과). **닫힘률은 보지 않았다** — 품질을 보고 프롬프트를 고치면 사전등록이
  무의미해진다

## 7. 결과 (2026-08-25 측정)

| | 개발(은행권 253행) | 홀드아웃(저축은행 1,090행) |
|---|---|---|
| 스냅샷 ID | `20260824` | `20260825` (668상품 · 79기관) |
| A 규칙 파서 닫힘률 | 62.8% (159/253) | **10.5%** (114/1090) |
| B 제한 스키마 닫힘률 | — (연기 테스트만) | **13.6%** (148/1090) |
| 차이 (B−A) | — | **+3.1%p** · CI [−0.5, +8.3] |
| **가중 닫힘률** A / B | — | **31.0% / 40.6%** · 차이 **+9.6%p** · CI [+0.2, +18.9] |
| 불일치쌍 (B만 / A만) | — | 27 / 4 (쌍 228 단위) |
| McNemar p | — | **< 0.0001** |
| Wilcoxon p (이진화 없음) | — | 0.0001 (0이 아닌 차 39) |
| 항목 0개 비율 A / B | 13.4% / — | 21.4% / **19.6%** (B가 1.7%p 낮다) |
| `기타` 비율 (B) | — | **4.7%** (16/340 항목) |
| 스키마 위반 | — | **0/228** |
| 상한 인식률 A / B | — | 20.6% / 22.9% |
| 비용·시간 A / B | ~0 / — | ~0·1초 미만 / **$1.00·487초** |

**사전등록 판정**: 주 지표(행 +3.1%p)와 가중 닫힘률(+9.6%p)이 갈렸으므로 `decisions/0005`에
따라 가중 닫힘률을 판정에 쓴다 → +9.6%p ≥ 5%p, McNemar p < 0.05, §4 제약 통과(B의 항목
0개 비율이 오히려 1.7%p 낮다) → **제한 스키마 채택**.

### 7.1 그런데 §8 두 번째 반증 조건이 발동했다 — 위 판정보다 이것이 앞선다

A와 B **둘 다 닫힘률이 40% 미만**이다(10.5% · 13.6%). §8에 미리 적어둔 대로 "이 지표
자체가 저축은행 공시에는 맞지 않을 가능성"을 검토했고, **원인이 확정됐다.**

```
조건문이 있는 홀드아웃 행 1,090
  그중 최고금리 == 기본금리 인 행     908  (83.3%)   ← 어떤 추출로도 닫히지 않는다
  최고금리 >  기본금리 인 행          182  (16.7%)
```

닫힘률은 **"항목 합계가 (최고금리 − 기본금리)와 맞는가"**로 정의된다. 그 차이가 0이면
맞출 방법이 없다 — 항목을 뽑으면 넘치고, 안 뽑으면 항목 0개로 분류된다.

**원본 필드로 확인했다.** 코드 버그가 아니다.

```
KB저축은행 · 정기예금 · 3개월
  intr_rate = 2.0     intr_rate2 = 2.0        ← 최고금리에 우대분이 반영돼 있지 않다
  spcl_cnd  = "인터넷뱅킹 신규가입하여 출금계좌를 등록한 후 당일 창구 신규시
               우대금리 연 0.1%p 적용 …"       ← 그런데 조건문에는 0.1%p가 적혀 있다
```

즉 **저축은행은 우대금리를 최고금리 필드에 넣지 않는다.** 은행권은 넣는다(조건 있는 상품
81건 전부 폭 > 0). 같은 API·같은 스키마인데 기관군의 기입 관행이 다르다. 홀드아웃을 고를 때
"같은 공시 체계"라고 본 §2의 전제가 이 지점에서 틀렸다.

### 7.2 사후 분석 — **판정에 쓰지 않는다** (측정 후에 자른 부분집합이다)

지표의 전제가 성립하는 행(최고금리 > 기본금리)만 남기면 이렇게 된다.

| | 행 | A | B | 차이 | McNemar p |
|---|---:|---:|---:|---:|---:|
| 전체 (사전등록) | 1,090 | 10.5% | 13.6% | +3.1%p | <0.0001 |
| **폭 > 0** | 182 | **62.6%** | **71.4%** | +8.8%p · CI [−9.8, +27.6] | 0.0987 |
| 폭 = 0 | 908 | 0.0% | 2.0% | +2.0%p | 0.0039 |

두 가지를 읽을 수 있다. **둘 다 사후 분석이므로 주장이 아니라 다음 실험의 가설이다.**

**(가) A는 과적합되지 않았다 — 앞선 해석을 정정한다.** 지표가 성립하는 182행에서 A는
**62.6%**이고, 개발 집합 62.8%와 사실상 같다. 홀드아웃 전체 숫자 10.5%는 과적합이 아니라
**분모에 닫을 수 없는 행 908개가 섞인 결과**다. "여덟 라운드 깎은 정규식이 다른 기관에서
무너졌다"는 읽기는 틀렸다.

**(나) B가 앞서지만 유의하지 않다.** +8.8%p는 임계 5%p를 넘지만 p = 0.0987이고 신뢰구간이
[−9.8, +27.6]으로 0을 포함한다. 조건문 46종·쌍 109개로는 이 크기의 차이를 가릴 수 없다.

### 7.3 판정 — 채택하지 않는다

**사전등록 판정("제한 스키마 채택")을 채택하지 않는다.** 근거가 된 가중 닫힘률 +9.6%p가
대부분 폭 = 0 행에서 나오고(그 부분집합에서 A 0.0% / B 8.1%), 그 행들에서는 "이긴다"는 말이
의미를 갖지 않는다. 반증 조건은 이런 상황을 위해 측정 전에 적어둔 것이고, 발동했으므로
따른다.

**대신 확정된 것 세 가지가 있다.**

1. **제한 스키마는 형식으로서 작동한다** — 스키마 위반 0/228, `기타` 4.7%, 항목 0개 비율이
   A보다 낮다. 열거값 16종이 다른 기관군의 문장을 4.7%만 흘렸다. 이건 사후 분석이 아니라
   사전등록한 부 지표의 결과다
2. **비용은 문제가 아니다** — 228호출 $1.00 · 8분. "닫힘률 3%p 우위에 100배 비용"(§4)이
   걱정거리가 아님이 확인됐다
3. **저축은행은 이 지표의 홀드아웃으로 쓸 수 없다** — 83.3%가 분모에서 빠져야 한다

`prereg-04`로 분모를 고쳐 다시 잰다(§10).

## 10. 다음 실험에 넘기는 것 (prereg-04)

측정 **전에** 정해야 할 것을 여기 적어 둔다. 확정은 다음 사전등록에서 한다.

- **분모 정의를 고친다** — "우대조건이 있는 행"에 "그리고 최고금리 > 기본금리"를 더한다.
  조건문은 있는데 폭이 0인 행은 **지표가 말할 수 없는 행**이다
- **표본이 부족하다** — 폭 > 0 행은 저축은행에 182개(조건문 46종)뿐이다. §5 판정력 기준으로
  이 규모에서는 8.8%p 차이도 유의해지지 않는다. 권역을 넓히거나(신용협동조합 등) 지표를
  바꿔야 한다
- **폭 = 0 행을 버리지 않는다** — 그 908행은 다른 질문의 재료다. "조건문에 우대금리가
  적혀 있는데 공시 최고금리에는 없다"는 것 자체가 `problem.md`의 실패 유형에 가깝다.
  AI가 그 0.1%p를 받을 수 있다고 말하면 근거가 어디에도 없다
- **A의 실제 성능은 62.6%다** — 홀드아웃에서도 개발 집합과 같다. 이슈 #6(규칙 깎기)의
  출발점을 10.5%가 아니라 62.6%로 잡는다

### 10.1 폭 = 0인 908행을 들여다본 결과 (탐색 · 2026-08-25)

**결론부터 — 이걸 다음 실험으로 삼지 않는다. 규모가 작고 성격이 다르다.**
§7.3에서 "다른 질문의 재료"라고 적었는데, 세어 보니 재료가 되기에는 부족하다.

908행을 걸러 내려가면 이렇게 줄어든다.

```
최고금리 = 기본금리인 행                            908
  ├─ 일부 기간만 폭 0 (다른 기간에는 폭이 있다)       589   제외 — 정상적인 기간별 차등이다
  └─ 모든 기간에서 폭 0                             319
       ├─ 조건문에 숫자 우대금리가 없음               173   제외 — 중도해지·회전 설명뿐이다
       └─ 숫자 우대금리가 적혀 있음                   146
            ├─ 금리 산식 서술 (B의 오독)               22   제외
            └─ 진짜 후보                            124
```

**진짜 후보는 조건문 11종 · 상품 12개다.** 124행 중 68행이 조건문 하나(한 상품의 여러
기간)에서 나온다. 이 프로젝트가 이미 벗어난 병목과 같은 규모다 — 조건 누락 3건,
사슬 4건(`../decisions/0002`로 폐기). 12건으로 실험을 세우면 같은 자리로 돌아간다.

걸러낸 22행이 왜 오독인가:

```
"회전시점의 정기예금 12개월 금리 + 우대금리 0.1% 적용"
   → B는 "우대금리 0.1%"를 조건부 혜택으로 읽었다
   → 실제로는 갱신 시점에 기본금리를 계산하는 산식이다. 고시된 3.6%에 이미 반영돼 있다
```

**성격도 §7.3에서 적은 것과 다르다 — 정정한다.** "AI가 그 금리를 받을 수 있다고 말하면
근거가 어디에도 없다"고 썼는데, **근거가 있다. 조건문도 공시다.**

```
하나저축은행 세바퀴정기예금 · 1개월
  기본금리 1.3%  최고금리 1.3%   ← 이 칸은 우대분이 0이라고 말한다
  조건문 "가입시 0.1% / 인터넷뱅킹·모바일APP으로 가입시 0.1%"
                                ← 이 칸은 0.2%를 받을 수 있다고 말한다
```

**공시 두 곳이 서로 어긋난다.** 어느 쪽을 근거로도 말할 수 있고, 공시만으로는 어느 쪽이
맞는지 알 수 없다. 그래서 이건 주 타깃(과대 진술)이 아니라 **두 번째 타깃인 "확인 불가
판정"**에 해당한다(`problem.md` §5 · v2 §4.1).

**은행권 통제군.** 폭 0인 행이 은행권에도 17개(상품 4개) 있다. 그런데 그중 하나가
카카오뱅크 정기예금이고 조건문이 `"복잡한 우대조건 없이 가입가능한 정기예금"`이다 —
조건문처럼 생겼지만 조건이 아니다. 은행권 사례는 대체로 이 현상이 아니다.

**기관 편중.** 폭 0 행 908개 중 하나저축은행 382 · KB저축은행 274 = 656행(72%)이다.
"저축은행 전체의 관행"이 아니라 **몇 기관의 기입 방식**에 가깝다. §7.1에서 "기관군의
기입 관행이 다르다"고 쓴 것은 이 정도로 좁혀 읽어야 한다.

### 10.2 그래서 `prereg-04`가 먼저 다뤄야 할 것

**분모를 `최고금리 > 기본금리`로 좁히는 것은 어긋나는 행을 조용히 버리는 일이다.**
버려도 되는지에 근거가 필요하다. 지금 채점 방식은 최고금리 칸에 전부를 걸고 있는데,
위 12건은 **그 칸이 조건문과 어긋날 수 있다**는 실측 사례다.

`prereg-04`는 분모를 고치기 전에 이것을 정해야 한다.

- 최고금리 칸을 정답으로 쓸 수 있는 조건은 무엇인가
- 어긋나는 행을 버릴 것인가, 별도 층("공시 자체가 모순")으로 둘 것인가
- 층으로 둔다면 그 층의 정답은 무엇인가 (아마 "확인 불가"다)

## 8. 반증 조건

- **B가 A를 넘지 못하면** → "추출 시점 형식 제한이 답이다"가 틀린 것이다. 규칙 파서를
  유지하고 표기 변형을 계속 흡수하는 쪽이 옳았다는 뜻이며, `0004`의 첫 번째 반증 조건이
  발동한다
- **A와 B 모두 홀드아웃에서 닫힘률이 40% 미만이면** → 두 접근 다 실패다. 이 지표 자체가
  저축은행 공시에는 맞지 않을 가능성을 검토한다(공시 체계가 다를 수 있다)
- **홀드아웃 닫힘률이 개발 집합보다 크게 높으면**(예: +15%p) → 저축은행 공시가 더 정형적이라는
  뜻이고, 홀드아웃으로서의 난이도가 낮다. 이 경우 대출 상품군 등 더 먼 홀드아웃을 검토한다

## 9. 이 문서가 주장하지 않는 것

- **추출이 좋아지면 판정도 좋아진다고 주장하지 않는다.** 닫힘률은 추출 품질의 대리 지표이고,
  최종 판정 성능은 별개 실험(G4/G5)에서 잰다
- **그래프와 무관하다.** 추출은 문법·스키마 문제이며, 지금까지 어떤 관측도 그래프를
  요구하지 않았다(`0004`)
