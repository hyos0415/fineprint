# NNNN — <결정 한 줄 요약>

> 날짜: YYYY-MM-DD · 상태: 유효 | 폐기(NNNN로 대체) | 보류

## Decision

무엇을 정했나. 한 문장.

## Evidence

왜 그렇게 정했나. 측정값이나 `파일:줄` 인용.
근거가 없으면 **"근거 없음 — 판단"** 이라고 쓴다. 없는 근거를 만들지 않는다.

## Alternative

검토한 다른 선택지. 하나씩.

## Why rejected

각 대안을 왜 버렸나.

## 반증 조건

**무엇이 관측되면 이 결정이 틀린 것인가.** 결정 시점에 미리 적는다.

이 칸이 이 템플릿의 핵심이다. 기준을 날짜와 함께 미리 박아두면, 틀렸을 때
조용히 합리화하지 않고 "무엇이 언제 왜 틀렸는지"를 지목할 수 있다.
