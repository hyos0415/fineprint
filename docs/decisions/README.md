# 결정 기록 (Decision Log)

> **문제 정의는 `../spec/problem.md`가 현재 유효하다** (2026-08-24 갱신).
> 아래 열린 결정 D1·D3·D4는 그 이전(조건 누락 중심) 기준으로 작성됐고, 각 이슈 본문
> 맨 위에 무엇이 바뀌었는지 적어두었다. D1·D4는 파일럿 결과 뒤에 정한다(`after-pilot`),
> D3은 파일럿 진행에 필요해 지금 열려 있다(`blocking`).

## 형식

모든 결정은 `0000-template.md`의 5항목으로 남긴다.

```
Decision · Evidence · Alternative · Why rejected · 반증 조건
```

파일명은 `NNNN-<주제-슬러그>.md` (`0001-`부터 순번). **결정 직후 커밋한다** —
결정이 언제 근거로 승격됐는지 추적할 수 없으면, 선행 프로젝트가 두 번 겪은
재현성 실패와 같은 모양이 된다.

## 이미 정해진 것 (handoff v1 → v2 전환 시 확정)

| # | 결정 | 기록 |
|---|---|---|
| — | 프로젝트명 `FINeprint`, 레포/디렉토리는 소문자 `fineprint` | 아래 §초기 결정 |
| — | 신규 저장소로 시작, KAG_LlamaIndex의 git history를 잇지 않음 | handoff v1 §13 |
| — | 진단 산출물(감사 3종)은 seed artifact로 명시 이식 | `docs/audit/`, 감사 권고 #6 |
| — | Verifier 코드는 finance_verifier를 재사용, 새로 만들지 않음 | `docs/reference/finance-verifier-findings.md` §3 |
| — | **은행 적금까지 확대**, 대출·카드·보험은 제외 | v2 §5.1 — 명시적 AND 상품이 38개 중 6개뿐 |
| — | **선행 프로젝트를 적금으로 재검증하지 않는다** | v2 §5.1 주석 — 주장이 정기예금으로 한정돼 있고, 적금 성능은 FINeprint 통제군 A가 직접 측정 |
| — | **MVP 최소 요건은 혜택 스코핑 + 1단계 중첩** (평면 ALL_OF 아님) | v2 §7.1 — 실제 3건 중 2건이 중첩 |
| — | **INSUFFICIENT 경계를 두 번째 타깃으로 승격** | v2 §4.1 — 표본 6건, 같은 구조로 거의 공짜 |
| — | 게이트에 **G5**(구조화 evidence + LLM 판정) 추가 | v2 §3 — Layer 1/2를 갈라야 기여 지점이 정해진다 |
| — | ~~참조 사슬을 2단계 질문으로 유지~~ → **폐기** | `0001` (폐기, `0002`로 대체) |
| — | **2단계(사슬 순회) 폐기** — 적금 전수 수집 결과 사슬 4건, 사전등록 임계 6건 미달 | `0002-drop-chain-traversal.md` · `../spec/prereg-01-savings-scope.md` §4 |

## 열린 결정 — 사람이 정해야 할 것

착수 전에 정해야 하는 것부터 순서대로. **미정 항목을 Agent가 임의로 확정하지 않는다.**

D1 · D3 · D4는 **GitHub 이슈로 옮겨 놓았다.** 후보안과 trade-off, 판정에 필요한 숫자가
이슈 본문에 있고, 처음 읽는 사람도 이해되게 자기 완결적으로 썼다.

| # | 항목 | 상태 |
|---|---|---|
| ~~**D1**~~ | **본 실험 표본 크기 — 확정.** B안(층별 20~25문항 + 정상 claim 71건 전부) → `0008` | **확정** (2026-08-25) · 층별 배분은 `prereg-04`/`06`에 남김 |
| **D3** | **정답(사용자 상태별 금리)을 누가 어떤 규칙으로 확정하는가** — 사전등록: `../spec/prereg-06-matching-and-judgment.md` 1부. **임계값은 확정** — 되묻고 비교한다(`0015`, 143건 · 13.6% · 확정 +25%). **되물음 방식도 확정**(`0016` — 유형을 가리지 않고 묻고, 못 채우면 사유 표시). **표본 검토 완료**(`0019` — 부정 조건은 실질 0건, `기타` 40건 중 13건은 조건이 아니었다). **`기타` A군도 확정**(`0021` — 원리 기반 재추출, 홀드아웃 86%). **계단식도 확정**(`0023` — 11문구 중 8문구는 정규식 구멍이었고, 진짜 계단 3문구는 `단계불명`으로 낸다). 남은 것: 없음 → [#2](https://github.com/hyos0415/fineprint/issues/2) | **확정** (2026-08-27) |
| **D9** | **저축은행 커버리지 게이트를 어떻게 할 것인가** — 실측 35.7% · 게이트 36.7% 불통과. 원인은 3행·상품 3개로 특정됐고 규칙으로 쫓지 않기로 정했다(`0023`). `0022`에서 대안 D를 기각한 이유(*"넘을 방법을 아는 상태로 둔다"*)가 이제 성립하지 않는다. 후보 셋 → [#10](https://github.com/hyos0415/fineprint/issues/10) | 미정 · **9월 공시 뒤에 보는 것도 선택지** |
| **D4** | **부품별 판정 기준** — 사전등록: `../spec/prereg-06-matching-and-judgment.md` 2부. **여섯 부품은 확정**(R1 `0012` · 추출개선 `0013` · R4 편의기능 · RAG 대기 · 라우터 · 묶는 축 `0014`). **회피 방지 제약식도 확정**(`0018` — 커버리지 게이트 + 질문 예산 12개). 남은 둘: 사람 검수 표본 크기 · 민감도 임계 → [#3](https://github.com/hyos0415/fineprint/issues/3) | **부분 확정** (2026-08-27) · 잔여 미정 |
| **D2** | 조건 구조의 표현 형식 — 노드/리터럴 속성/논리식 값 구분표. 적금 조건의 53.6%가 횟수·기간을 담으므로 **수량 비교**가 필수 항목이 된다 | 미정 · **파일럿·게이트 통과 후** 최우선 |
| **D5** | 그래프 백엔드 (in-memory / SQLite / Neo4j) — **파일럿·게이트 통과 후에만 논의** | 보류 |
| ~~**D6**~~ | ~~2단계(참조 사슬) 설계~~ | **폐기** (`0002`) — 사슬 순회는 측정 불가 |
| **D7** | **확인 불가능한 외부 조건을 판정에 어떻게 쓸 것인가** — 조건이 외부 상품 가입을 요구하는 상품 41건(적금 29 + 예금 12). 한 홉 안에서 끝나고 `INSUFFICIENT` 경계와 직결 | 미정 · `0002` §다음 질문 (가) |
| **D8** | **claim 생성 규칙 사전 등록** — 파일럿분은 `../spec/prereg-02-pilot.md`에 등록 완료. 본 실험분은 미정 — 유형 비율 · 생성 모델·프롬프트 고정 · 사람 검수. claim을 우리가 만들므로 규칙 없이는 측정이 성립하지 않는다 | 미정 · **after-pilot** |

### D1 / D4 참고 자료 (결정 시 근거)

**코퍼스 상한 — 실측**

```
은행 정기예금 전수                        38개  (total_count=38)
  우대조건(spcl_cnd) 있음                 32개
  조건 항목 2개 이상                      22개
  조건 항목 3개 이상                      18개
  "모두 충족"·"및"·"함께" 명시적 AND        6개   ← D1의 병목, D3가 필요한 이유
negative(오류 없는 claim) 재사용 가능       71건  (finance_verifier Pilot 46 + Test 25)
```

**D1 검정력 참고**: 구조 검사가 잡고 Verifier가 놓치는 건수가 **6건이면 유의**(p≈0.03,
쌍체 이항검정). 놓치는 비율을 감안하면 정답 20~25건에서 그 6건이 안정적으로 확보된다.
12건이면 효과가 거의 완벽할 때만 유의해진다.

**D4는 두 숫자를 쌍으로 정한다** — 재현율만 정하면 "정상 claim 과차단"을 놓친다.
3구간 판정 예시:

| 구간 | 조건 | 결정 |
|---|---|---|
| 충분 | 누락 잡음 ≥80% · 정상 오차단 ≤5% | 구조 불필요. 리포트 쓰고 종료 |
| 애매 | 잡음 50~80% 또는 오차단 5~15% | 놓친 **유형**을 분석해 결정 |
| 불충분 | 잡음 <50% 또는 오차단 >15% | 구조 필요. 다음 단계로 |

**세 번째 필수 조건**: 재현율과 함께 **조건 추출 성공률**을 반드시 같이 보고한다.
추출이 40% 실패하면 재현율 숫자 자체가 무의미하고, 그 실패가 오히려 "형식을 제한한
스키마가 필요하다"는 근거가 된다.

## 선행 저장소에서 넘어온 미결 (참고용, 이 프로젝트에서 재판단)

KAG_LlamaIndex `docs/audit/00-comparison.md` §6의 Q1~Q4. 이관 시 다음과 같이 처리했다.

| 원 항목 | 처리 |
|---|---|
| Q1 `verify_metrics.py`/serialization 이관 여부 | **조정안 채택** — 코드가 아니라 "산출물과 계산 코드를 함께 커밋하고 지표 재현을 검증하는 패턴"을 자산으로 승격. 코드는 새 도메인에 맞게 새로 씀. Codex의 수용 기준("전 지표가 baseline과 완전 일치")을 이 프로젝트 eval harness의 완료 조건으로 사용 |
| Q2 handoff §2.1(정규화) 정정 여부 | v2 §2에 반영 — 아래 §초기 결정 참고 |
| Q3 KAG `docs/CONTEXT.md` 발견 1 정정 여부 | **원 저장소를 수정하지 않는다.** 그 문서는 선행 저장소의 진실 원본이므로 보존하고, 코드 사실과 충돌한다는 점만 인용 시 명시 |
| Q4 감사 브랜치 처리 | **이 프로젝트 범위 밖.** 선행 저장소의 정리 과제로 남김 (감사 문서는 이미 seed로 이식 완료) |

## 초기 결정

- `0001-two-stage-scope.md` — 참조 사슬을 2단계 질문으로 유지 (2026-08-24) · **폐기, `0002`로 대체**
- `0002-drop-chain-traversal.md` — 2단계(사슬 순회) 폐기. 사전등록 임계 발동 (2026-08-24)
- `0003-upstream-conclusions-corrected.md` — 선행 프로젝트가 결론 2건을 정정. 게이트
  질문을 "프롬프트로 잡히는가"에서 **"과잉거부 없이 잡히는가"**로 재정의 (2026-08-25)
- `0004-pilot-passed-extraction-is-the-bottleneck.md` — 파일럿 통과. 주 타깃은 "확인 불가
  판정", 병목은 판정이 아니라 **추출** (2026-08-25)
- `0005-condition-type-enum-and-analysis-unit.md` — 조건 유형 열거값 **16종 + 기타** 확정
  (축은 빈도순이 아니라 "사용자 상태로 O/X 판정 가능한가"). 분석 단위는 지표마다 다르게 —
  닫힘률은 행, McNemar는 (조건문,기간) 쌍, CI는 조건문 클러스터 부트스트랩 (2026-08-25)
- `0006-holdout-broke-the-metric.md` — 저축은행 홀드아웃 1,090행 중 908행(83.3%)이
  최고금리 = 기본금리라 닫힘률로 채점할 수 없다. **사전등록 판정을 채택하지 않고**
  반증 조건을 따른다. A의 성능은 10.5%가 아니라 **62.6%**로 기록 (2026-08-25)
- `0008-main-experiment-sample-size.md` — 본 실험 표본은 **B안**(층별 20~25문항 +
  정상 claim 71건 전부). A는 12건이라 효과가 완벽할 때만 유의해지고, C는 하위집단
  결론을 위해 비용을 두 배 쓰는데 그건 지금 질문이 아니다. **층별 배분은 미정**
  (2026-08-25) · 이슈 #1
- `0007-rules-refined-then-closed.md` — 채점 정의를 사전등록 글자대로 고치고(기준선
  62.8% → 68.4%), 규칙 H·I·J를 채택해 개발 집합 **77.5%**. 그런데 홀드아웃은 **+0.5%p**
  뿐이라 **이슈 #6을 닫는다** — 규칙이 고친 것은 은행권의 표기 습관이었다 (2026-08-25)

- `0009-pivot-to-recommender.md` — 타깃을 **검증기에서 추천기로** 바꾼다. 선호는 추론하지
  않고, 정답은 실수령 예상금리다. 조건 유형 17종·닫힘률·추출기는 그대로 살아남는다 (2026-08-26)
- `0010-disclosure-updates-monthly.md` — 공시는 **월 단위로 갱신된다.** "4일 만에 바뀐다"를
  정정하고(8/24→8/26 실측 변화 0건) 수집 주기를 월 1회로, E8을 월 경계 측정으로
  다시 정의한다 (2026-08-26)

- `0011-disclosure-docs-do-not-explain-the-gap.md` — 상품설명서에도 설명 안 되는 금리가
  **없다**(제주은행 J정기예금 실물 확인). 수집 1순위를 내리고 "근거 없음" 표시를 정식
  기능으로 확정한다. PDF 도구는 `pdfplumber` 로 교체 (2026-08-26)

- `0012-adopt-fallback.md` — **폴백 구조 채택.** 규칙 먼저, 산수 안 맞는 것만 AI.
  사전등록 예측 83.8% → 실측 **83.9%**(은행권). 부 지표도 전부 같은 방향 —
  넘침 16→12 · 항목 0개 6.4%→1.7% · LLM 호출 24.2%. 분모도 "폭>0" 으로 고쳤다 (2026-08-26)

- `0013-stop-improving-extraction.md` — **추출 개선을 멈춘다.** 폴백 이후 남은 실패
  51행 중 **71%가 공시 문제**다("최고 0.5%p"라는데 폭이 2.0 / 조건문에 금리가 없음).
  고칠 수 있는 것은 10행(20%)뿐이고 은행권에 편중돼 있다. 남은 실패는 `설명부족`·
  `계산불가` 층으로 사용자에게 표시한다 (2026-08-26)

- `0014-institution-is-the-grouping-axis.md` — 묶는 축은 **기관**이다(2.17배).
  상품군 1.25배·권역 1.23배로 신호가 거의 없다. 다만 기관으로 묶고 나면 남는 구조가
  0.3%뿐이라 **유사도·그래프는 여전히 근거가 없다.** 이전에 적은 "3.24배"는 표본이
  얇은 결과였고 2.17배로 정정한다 (2026-08-26)

- `0015-ask-back-for-thresholds.md` — 금액·횟수 임계는 **되묻는다.** "급여이체 실적
  (월 50만원 이상)"에 사용자가 "O"라고만 답하면 판정 불가였던 것을, 수치를 물어 비교한다.
  **재추출이 필요 없었다** — 근거 문구에 숫자가 이미 있어 143개 중 132개(92.3%)를 읽는다.
  확정이 +25%(은행권 51→64, 저축은행 86→105) (2026-08-26)

- `0016-ask-back-for-everything.md` — **애매하면 유형을 가리지 않고 되묻는다.** 공시
  문구를 그대로 인용하고 "모르겠다" 선택지를 둔다. 못 채우면 조용히 빼지 않고 사유를
  문장으로 보여준다(사유 6종). 전부 되물으면 **은행권은 범위가 0**이 되고 남는 것은
  "약속을 지켜야 받는다"는 주의뿐이다 (2026-08-26)

- `0017-start-from-the-best-and-narrow.md` — **최고 금리부터 보여주고 물어가며 깎는다.**
  정렬 기본값을 최소값에서 최대값으로 바꿨다. 최소 순은 사실상 기본금리 순이라 조건이
  좋은 상품이 묻힌다(두 정렬의 첫 화면 상위 3위 겹침 0/3, 전부 답하면 3/3). 광고와
  구분되게 **범위 표시와 남은 조건 수를 강제**한다 (2026-08-26)

- `0018-anti-gaming-constraint.md` — **도피 방지 제약식.** 개선을 주장하려면 커버리지
  게이트(메인 층 비율 하한 — 은행권 81.0% · 저축은행 36.7%)를 통과하고, **상위 12개
  질문까지만 답한 상태**에서 확정률·범위 폭을 재야 한다. 실측 결과 메인 층 수는 질문에
  아예 안 변하고(64 고정) 범위 폭은 질문 수의 단조 감소 함수라, 원래 후보 둘 중
  **하나는 무감각하고 하나는 질문 부풀리기를 보상**했다. 질문 우선순위와 동점 처리
  (커버리지 내림차순 → 사전순)도 사후 우회를 막으려고 같이 고정했다 (2026-08-27)

- `0019-not-a-condition-and-unclear.md` — **조건이 아닌 문구를 규칙으로 걸러낸다.**
  숫자와 껍데기 말을 걷어내고 아무것도 안 남으면 조건이 아니다(`기타` 40건 중 13건 정확 적중,
  재추출 $0). 금리 표기가 있으면 빼지 않고 **판정 불가 + `조건불명` 사유**로 두고
  **은행에 전화해 확인하라고 안내한다.** 규칙이 `기타` 밖에서 4건을 더 찾았는데 그게 더
  나빴다 — 부산은행 `"우대이율 6개월 이상 2.20%"`가 `무조건_특판_이벤트`(항상 충족)로
  분류돼 **아무것도 안 물어도 2.20%p를 준 것으로 계산**하고 있었다. 금액 임계 미탐
  36건(띄어쓰기)도 함께 고쳤고, 셋 다 확정률을 낮추는 방향이라 기준선을 재등록했다
  (은행권 81.0% → **79.7%**) (2026-08-27)

- `0021-adopt-v2-over-the-gate.md` — **유형 배정 재추출을 채택했다. 게이트를 사람이
  넘겼다.** 프롬프트에 예시가 아니라 **판정 원리**를 넣고 근거는 은행권 11건만 봤다.
  주 지표 100%, **홀드아웃 86%**(예측 33%) — 겹치지 않는 유형이 맞았으니 예시를 외운
  게 아니라 원리가 작동한 것이다. `기타`가 4.5배 줄고(3.8%→0.7%) **계단식 우대가
  부수적으로 풀렸다.** 다만 저축은행 커버리지가 36.7→35.4%로 **회귀 가드 불통과**였고,
  그 사실과 넘긴 이유를 기록에 남겼다. 진단 중 `exclusive_group` 신호를 **절반쯤 못
  믿는다**는 것이 드러났다(최댓값 35 : 합산 32) — 열린 문제 (2026-08-27)

- `0022-exclusive-group-as-a-band.md` — **배타 그룹을 못 믿으므로 범위로 낸다.**
  층 판정은 그룹을 무시하고 다 더했고 `bonus_range()`·공식 닫힘률은 그룹 최댓값을 썼다 —
  **같은 항목이 두 값을 냈다.** 어느 쪽이 맞는지 재보니 **어느 쪽도 아니었다**
  (최댓값 35 : 합산 32 : 둘 다 틀림 26). `iM함께예금`은 `g1`에 `[0.1×5]`가 들어 있는데
  공시 폭이 0.45라 다섯 개가 다 더해져야 맞는다 — **라벨을 못 믿는 것**이다. 그래서
  `lo=그룹 최댓값 · hi=합산` 밴드로 판정하고 `중복우대불명` 사유를 보여준다.
  **밴드 전용 행이 1.5%·0%**라 판정이 느슨해지지 않았다. 기준선은 유지했다.
  앞선 진단(`0021`의 "커버리지 하락은 내부 모순 때문")이 **절반 틀렸음도 정정했다**
  (2026-08-27)

- `0023-stop-narrowing-rules-ask-instead.md` — **규칙을 그만 좁히고 되묻는다.**
  계단식으로 분류된 11문구 중 **8문구는 계단이 아니었다** — 같은 파일의 임계 정규식
  둘이 짝이 안 맞아 `"500만원이상"`을 "임계가 있는데 못 읽겠다"로 읽은 오분류였다.
  짝을 맞추자 8문구가 `단계불명`(되물어도 못 없앤다)에서 `수치필요`(되물으면 없어진다)로
  옮겨갔고, 예산 40개 확정이 56/67 → **64/67**이 됐다. 진짜 계단 3문구는 그대로
  `단계불명`으로 낸다. 저축은행 커버리지 1.0%p는 원인 셋까지 좁히고도 **규칙으로 쫓지
  않기로 정했다**(사람 결정) — 같은 모양의 실패가 세 번 돌아왔다 (2026-08-27)

- `0024-ask-back-flow.md` — **되묻기 흐름을 정의한다. 전부 묻고, 남은 수는 줄어들기만
  한다.** 질문 수를 제한하지 않는다(행원은 전부 확인한다) — `ASK_BUDGET=12`는 평가
  지표로만 쓴다. 대신 상태바에 **숫자 둘**을 나란히 놓는다: 진행(남은 질문) · 성과(확정
  상품). 남은 질문 수만 보여주면 "모르겠다"가 "아니오"와 똑같이 진전으로 보이는데,
  끝까지 갔을 때 확정이 **예 64 · 아니오 65 · 모르겠다 4**다. 분모는 "모두 예" 최대치라
  구조적으로 줄어들기만 한다(은행권 22 · 저축은행 27). "모르겠다"를 "아니오"로 취급하는
  안은 기각 — 확정이 4 → 65로 뛰어 **방어가 아니라 단정**이 된다 (2026-08-27)

- `0025-answer-handling-in-the-question-loop.md` — **질문 루프가 답을 읽는 방식.**
  수치 질문의 "아니오"는 0이 아니라 **유형 자체를 `False`**로 되돌린다(0을 넣으면
  `"1천만원 이하"` 같은 최대 방향 임계를 오히려 충족시킨다). 수치 질문에 "모르겠다"를
  답하면 **다시 묻지 않고** `모름` 사유로 낸다 — 고치기 전에는 같은 질문이 무한히
  돌아왔다. **시뮬레이션에서는 안 나타나는 버그였다**(페르소나는 수치에 항상 숫자로
  답한다). 기준선은 안 움직였고, 루프가 `0024`의 실측표를 그대로 재현한다 (2026-08-28)

- `0026-questions-are-not-negotiable.md` — **질문 수는 협상 대상이 아니다.**
  `0024` 의 반증 조건 1("중간에 그만두면 상위 N개만 묻는다")을 **폐기**했다 — 안 물은
  조건은 `모름` 이 되어 `hi` 에 그대로 남으므로, 질문을 줄이는 것은 **최고 금리를 유지한
  채 근거만 없애는 것**이다. `0024` 대안 D 를 "방어가 아니라 단정" 이라며 기각한 것과
  같은 자리에서 반대편으로 넘어가는 셈이었다. **완주율·중단률은 지표가 아니다**(기록만
  한다). 피로는 이어하기·질문 순서·문구로만 다룬다. 대체 반증 조건은 *"중단 지점의
  화면이 범위를 단일 숫자로 줄이거나 미확인 조건 위에 `확정` 라벨을 붙이면 그때가 틀린
  것"* 이고, 그 실패는 이미 한 번 일어났다(`0019` 부산은행 — 입력 0개에서 4.80%).
  `prereg-09` 가 assert 다섯 개로 전수 검사한다 (2026-08-28)

- `0027-ask-the-clause-not-the-number.md` — **임계는 숫자로 받지 않고 문구로 묻는다.**
  사람이 실제 루프를 답해 보니 수치 질문에 **답이 존재하지 않았다** — 한 유형 아래
  적립식예금 잔액 · 펀드 보유액 · 정기예금 가입액이 묶여 있어 "금액이 얼마입니까"에
  답할 수가 없다. 실측에서 `예` 를 세 번 거절당한 뒤 **사실이 아닌 `아니오`** 로 답했다.
  판정 단위를 (유형)에서 (유형 + 문구)로 내리고 공시 문구를 **질문 문장 자체**로 쓴다
  (키는 문구 해시 — `0018` 의 "내용 무관 정렬" 을 지키려고 텍스트를 키로 쓰지 않았다).
  사전등록 예측이 전부 적중 — 질문 수 22 → **39** · 예산 12개 확정 **변화 0** · 폭 평균
  0.176 → 0.185 · 전부 답하면 50/67 그대로 · 화면 계약 A1~A5 통과. **예산 12개가 유형
  질문만 재고 있었다는 사실도 드러났다** (2026-08-28)

- `0028-candidate-scope-is-first-class.md` — **후보 집합(스코프)을 1급 개념으로.**
  질문이 카탈로그 17개 기관 전체에서 생성돼, 우리·농협을 보려는 사용자가 제주·광주 상품
  때문에 답하고 있었다(실측 세션에서 답한 문구 질문 8개 중 6개). 0단계 축에 **기관·상품군**을
  넣고 질문을 후보에서만 만든다 — "우리은행 주거래인데 목돈" 시나리오가 **39개 → 7개**다.
  **화면 층(후보)과 지표 층(카탈로그 고정)을 분리**한 것이 핵심이다(후보 기준으로 재면
  쉬운 기관만 골라 확정률을 올리는 도피가 생긴다 — 실측으로 지표 무변동 확인). 좁히는
  대가는 **A7**(스코프 밖 최고 금리 표시)로 강제한다 — 기관별 격차 중앙값 **2.41%p**.
  **설문은 하지 않는다** — 모집단 평균으로 개인 스코프를 정하면 `0024` P2 위반이다
  (2026-08-28)

- `0029-progress-shows-my-rate-range.md` — **상태바 성과를 "내 금리 범위" 로.**
  사람 세션에서 *"진행 바밖에 안 보인다"* 가 나왔고, 원인이 데이터에 있었다 — 후보 4개
  스코프에서 **확정 수가 6개 질문 동안 0 으로 고정**이었다(폭은 1.18 → 0.51 로 움직였다).
  확정 수는 후보 집합 크기에 종속이고 범위 폭은 아니다. `0024` 가 반증 조건 4에 미리
  적어 둔 대안(*"폭 평균이나 1위 금리 변화로 바꿔 본다"*)을 그대로 채택했으므로 **새
  사전등록 없이** 결정 하나로 처리했다. 확정 수는 "모르겠다" 의 대가를 가르는 유일한
  숫자라 **부 지표로 남긴다.** 화면 계약 **A8**(성과 줄이 1위 상품과 일치한다)을 같이
  넣었다 — 화면에 숫자를 하나 더 올리면 거짓말할 자리가 하나 더 생긴다 (2026-08-31)

- `0030-preferences-are-rate-equivalents.md` — **선호 가중치는 "금리 몇 %p 와
  맞바꾸겠는가" 다.** `problem.md` §3 의 추상 0~1 가중치를 **안 쓴다** — 0~1 로 정렬하면
  화면에 *"왜 이 순서인지"* 를 못 적고(§3 이 약속한 것이다), 정규화 기준이 후보 집합에
  종속돼 `0029` 와 같은 실패 모양이 된다. 시작 근거는 8/31 세션의 **관측**이다 — 사용자가
  스코프 밖 **+2.67%p** 를 보고도 안 넓혔고("연고도 없다"), 그게 그대로 `처음기관=많이`
  의 **하한**이 됐다(표본 1). 다섯 문항 중 숫자에 근거가 붙는 것은 그 축과 `확실성`
  (`--sort lo` 와 **항등식**) **둘뿐**이고 나머지 셋은 표에 "근거 없음 — 판단" 이라고
  적었다. **기본값을 안 만든다**(`--prefs` 없으면 가중치 0개 · `0024` P2) · **선호로
  상품을 빼지 않는다**(맨 아래로 · 빼면 A7 과 같은 은폐 방지 장치가 하나 더 필요하다 —
  `problem.md` §3 을 정정했다) · **점수는 금리 칸에 안 들어간다**(A11). 사전등록
  `prereg-12` 의 첫 항목이 **"무엇을 주장하지 않는가"** 다 — 정렬 품질에는 정답이 없고
  순위 교체 수도 방향이 없는 지표다. 주 판정(지표 무변동) 통과 · 예측 다섯 전부 적중 ·
  화면 계약 A1~A11 통과. **화면이 거짓말하는 자리를 하나 더 찾았다** — 선호가 걸리면
  1위가 최고 금리가 아닌데 성과 줄이 `최고` 라고 썼다(A8 을 낱말까지 보게 강화했다).
  **검사기 버그를 쫓아가다 더 큰 것도 나왔다** — `rank_questions` 는 상품을 code 로,
  `ask_budget` 은 행으로 센다(저축은행 297행 = 92 code). `0018` 의 우선순위·게이트
  분모를 건드리므로 **고치지 않고 이슈 #25 로 냈다** (2026-08-31)

- `0031-row-is-extraction-product-is-screen.md` — **행은 추출 품질의 단위, 상품은
  화면의 단위다.** 두 단위를 쓰고 있었는데 **아무 데도 안 적혀 있었고** 화면 쪽이 틀리게
  계산됐다 — 상품을 `상품코드만`으로 세서 저축은행 상품이 92개로 보였다(참값 **183**.
  기관명 30종 아래 기관코드가 58개이고 하나저축은행 세 법인이 전부 코드 `240001` 을
  쓴다). 행의 정체도 확정했다 — `(기관코드, 상품코드, 단리/복리, 적립방식)` 이고 원천이
  **남김없이** 설명한다(79=79 · 297=297). `rows` 에 세 필드가 안 담겨 있었는데
  `kor_co_nm`(#15) · `join_way`(#22) 때와 **같은 자리 세 번째**다. **지표 분모는 행
  그대로 두어** 게이트·#10 을 안 건드렸다(주 판정 = 커버리지 무변동, 통과). 저축은행
  확정률이 80.2 → **70.8%** 로 움직였는데 **후퇴가 아니라 다른 12개를 답한 상태**다 —
  1위가 바뀌면 그리디 경로가 갈라진다. `0018` 이 1순위로 고른 것은 커버리지이고
  확정률은 그 부산물이다. 기준선 재등록은 `0019` 절차대로 **편향 방향을 "모른다" 고
  적고** 예측을 먼저 커밋했다 (2026-08-31)

- `0032-tax-rates-are-pinned-to-statute.md` — **세율은 조문 좌표에 핀을 박고, 바뀌었는지만
  기계가 본다.** `config/tax-2026.json` 이 `"확인_상태": "미확인"` 이었고 화면의 **모든
  세후 숫자**가 그 위에 서 있었다. 법제처 API 로 조문 다섯을 대조하니 **틀린 값 하나 ·
  빠진 값 하나**가 나왔다 — 조합등예탁금이 `1.4%` 로 적혀 있는데 조문은 *2026-01-01
  이후 가입분 100분의 5*(2025년까지 가입분 비과세 시절의 값을 적어 뒀다), 비과세종합저축
  일몰 **2028-12-31** 은 아예 없었다. **그 오류가 곧 "자동 파싱 금지" 의 근거다** — 가입
  시기로 세율이 갈리는 조문이라 정규식이 어느 숫자를 집을지 알 수 없고 **틀려도 조용하다**
  (`problem.md` §7 이 LLM 에 대해 막은 것과 같은 자리). 확인은 **2단**이다 —
  목록 2.5KB 로 판정하고 바뀐 법령만 본문 1.3MB(515배)를 긁는다. **별도 DAG 를 안 만든다** —
  세율이 쓰이는 자리가 월 1회 계산 하나뿐이라 H1 의 첫 태스크로 넣고 **최대 20일 지연을
  받아들인다** (2026-08-31)

- `0033-after-tax-sorting-cannot-reorder.md` — **세후 정렬은 세전 정렬과 순서가 같다.**
  로드맵 E7 이 재려던 것이 **구조적으로 0** 이었다 — 공시 API 에 **상품별 과세 유형이
  없어서** 비과세 여부가 상품 속성이 아니라 **사용자 상태**로 들어오고(`calculate.py:818`),
  한 실행에서 전 상품이 같은 세율을 받는다. 세후 = 세전 × 상수이므로 순서가 보존된다 —
  은행권 79행·저축은행 297행 × 비과세 켜고 끄고 **넷 다 `세전순 == 세후순`**(세율 집합
  크기 1). **그래서 E7 의 선행은 E3(세율표 검증)가 아니라 A2(상품설명서)였다** —
  `problem.md` §4 의 예시(A 3.85 일반과세 vs B 3.50 비과세)를 재현하려면 상품별 과세
  유형이 필요하고 그건 설명서에만 있다. 예시는 **지우지 않고** "지금 데이터로는 재현
  불가능" 을 §4 에 적었다 — 틀린 주장이 아니라 데이터가 없는 주장이다. **정렬 효과 0이어도
  세후 표시는 유지한다** — 사용자가 보는 숫자이자 선호 가중치의 단위가 세후 %p 다
  (`0030`) (2026-08-31)

- `0034-ad-rules-exist-but-bind-the-seller.md` — **최고금리 단독 표기 제한은 실재한다.
  다만 의무 주체가 판매업자다.** `0026` 의 반증 조건이 *"실재한다고 보지만 조문을
  확인하지 않았다"* 로 열려 있었다. 법제처 API 로 3단을 읽었다 — 법 §22④3가(*"이자율의
  범위ㆍ산정방법 (…) 을 명확히 표시하지 아니하여 (…) 오인하게 하는 행위"* 금지) →
  시행령 §18·§20(고시 위임) → 감독규정 §17①3가(**예금성 상품 광고에 "이자율ㆍ수익률
  각각의 범위 및 산출기준"**)·§19①1(**"소비자에 따라 달라질 수 있는 거래조건을
  누구에게나 적용될 수 있는 것처럼 오인하게 만드는 행위"** 금지). **`0017` 강제 1(범위)은
  조문 문구와 직접 대응하고, 강제 2(남은 조건 수)는 조문이 금지하는 결과를 막는 우리
  구현이다** — "최대값을 쓰면 최소값을 함께" 를 명시한 조문(별표 5 3.가.5)은 **투자성
  상품 전용**이었다. **예상 밖의 것 하나** — 공정위 예규가 *'세전'인지 '세후'인지 누락*을
  부당한 표시·광고로 본다. CLI 는 이미 지키는데 **assert 가 없다**(A12 후보 · F4 위험).
  조문 6개를 `config/ad-sources.json` 에 핀으로 박았다 — **세율 스냅샷과 파일을 나눴다**
  (바뀌면 고칠 곳이 config 값이 아니라 **화면 계약**이다). `admrul`(행정규칙)은 응답
  구조가 법령과 달라 별도 경로를 만들었다. **적용 대상 여부는 확정하지 않고 사람에게
  올렸다** — 법 §22① 이 판매업자가 아닌 자의 광고를 금지한다(이슈 #31) (2026-08-31)

- `0035-say-what-this-screen-is-and-hold-the-seller-standard.md` — **화면이 자기가
  무엇인지 말하고, 판매업자 기준을 그대로 지킨다.** `0034` D5 가 사람에게 올린 결정을
  사람이 **A·B 병기**로 닫았다(둘은 배타적이 아니다). **A안 → assert A13** — 화면 맨
  위에 *"금융감독원 공시를 계산한 결과이며 판매·중개하지 않고 특정 상품을 권하지
  않습니다"*. **"광고가 아니다" 라고는 쓰지 않는다** — 법적 판정을 우리가 내릴 것이
  아니고 #31 이 아직 열려 있다. **B안 → assert A12** — 금리 화면에 `세후` 라벨.
  **만들면서 `0034` D3 가 틀린 것을 찾았다** — *"CLI 는 이미 지키고 있다"* 는
  `calculate.py` 목록 헤더만 보고 쓴 말이고, **사용자가 실제로 보는 `ask_loop` 최종
  화면에는 `세후` 가 한 번도 없었다**(`0019` 부산은행과 같은 모양 — 코드는 아는데
  사용자가 못 읽는 자리). 음성 검사로 헤더를 빼니 **A12·A13 각각 156건 불통과**가
  나오는 것까지 확인했고, 은행권·저축은행 · `--prefs` 까지 **A1~A13 전부 통과**다.
  문구는 `calculate.NOTICE` 한 곳이고 세율은 config 에서 읽는다 (2026-08-31)

- `0036-report-shows-where-the-number-came-from.md` — **비교 리포트는 "이 숫자가
  어디서 왔나" 를 적고, 합이 안 맞는 자리를 숨기지 않는다.** `problem.md` §3 이 약속한
  칸 14개 중 **4개가 없었다** — 조건별 %p 둘 · 못 채운 이유(공시 문구 원문) · 배타 그룹.
  **원천에는 다 있었다**(추출 항목 242개 중 `rate` 238 · `evidence` 242) — `evaluate()`
  가 `met`/`unmet`/`unknown` 을 **이름만** 남기며 버렸다. 그 목록은 A11·질문 루프가 읽어서
  **그대로 두고** `why` 를 더했다. 리포트는 **금리를 다시 계산하지 않는다** — 이미 계산된
  값의 차 두 개만 쓴다. **만들면서 칸 이름이 틀린 것을 찾았다** — `안 답한 조건` 칸이
  조건별 합보다 1.00%p 컸고, 그 몫은 **답했지만 중복 적용 여부가 불명한 것**이었다
  (계산기가 낙관 쪽에만 넣는다 · `0022`). 그래서 칸을 **확실히 받는 우대 / 불확실한 우대**
  로 고쳤다. **최대값의 가정도 적는다** — 웰뱅 라이킷 적금은 같은 배타 그룹 세 조건으로
  `+12.00%p` 였다(하나만 받으면 7.00%p 다). 리포트도 화면 계약을 진다(A1·A3·A12·A13) —
  음성 검사로 A13 불통과 4건을 확인했고, 은행권·저축은행 시드 200 · 선호 있음까지
  **A1~A13 전부 통과**다 (2026-08-31)

나머지 확정 항목(위 §이미 정해진 것)은 아직 정식 기록으로 옮기지 않았다.
